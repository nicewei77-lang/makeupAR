# 맞춤 메이크업 — 유저 플로우 재정의 (제품화 기준선)

작성일: 2026-06-30 KST
대상: makeupAR 맞춤 마스크 기능을 `302-group5-final-project` (`apps/mobile`, RN+Expo+Tamagui)의 얼굴분석 흐름에 통합할 때의 **유저 플로우/화면 기준선**.

이 문서는 A(유저 플로우)만 정의한다. B(디자인 시스템/색·컴포넌트)와 C(302 Tamagui 구현)는 이 문서를 기준선으로 삼아 별도로 진행한다.

---

## 1. 원칙

1. **clickless, don't make me think** — 화면마다 명확한 주 액션 하나. 의미 없는 클릭(네비/단계 이동/생성 트리거)은 전부 자동화로 제거.
2. **의도적 클릭은 남긴다** — 촬영 셔터처럼 사용자가 "준비됐을 때 내가 누른다"가 더 편한 액션은 유지. 자동 촬영 금지(통제감/예측가능성).
3. **기능·표현력·조정 능력 100% 보존 — 없애는 것은 없다.** 4부위 마스크 생성/조정/AR 적용 로직, 텍스처·팔레트·마스크 id, **모든 색/농도/finish(매트·크림·글로스)/광택/질감/모양 세부조정**은 그대로 유지. 재정의는 *보기 좋고 편하게 재배치(기본 자동값 + 접기)*하는 것이지 **컨트롤을 삭제하는 것이 아니다.** 단, 노출 방식만 바뀐다.
   - 예외적으로 사용자 경로에서 빼는 것은 **검증/디버그 전용**(진하게 보기·경계 보기·Debug·hot 검증색·Entry 카운터)과 **개발 언어/내부 명칭**(Generate/검증/MediaPipe)뿐. 이것들은 제품 표현 기능이 아니다.
4. **AR 화면 ↔ 세부조정 왕복 유지(중요)** — 실제 AR로 내 얼굴에 올라간 걸 보고 다시 조정하는 루프는 핵심 가치라 **절대 제거하지 않는다**. 오히려 더 편하게 만든다(실시간 AR 위에서 바로 조정).
5. **제품 언어만** — `검증/Generate/Debug/MediaPipe/hot색/Entry카운터` 같은 개발 언어·도구는 사용자 경로에서 제거.

---

## 2. 골격: 6단계 → 체감 4화면 + 결과

기존 마법사 단계(start/align/capture/extract/adjust/apply)는 유지하되 사용자 체감은 압축한다.

```
[302 얼굴분석 결과] —"메이크업 입혀보기"—▶
  ① 얼굴 맞추기   (start+align+capture 통합 / 셔터 1클릭)
  ② 분석 중       (extract+생성 통합 / 클릭 0, 자동)
  ③ 꾸미기        (adjust / 자동룩 적용됨, 선택 조정)
  ④ 입혀보기      (save+apply 통합 / "입혀보기" 1클릭)
  ▣ AR 결과       (실시간 적용 + ③로 되돌아가는 루프)
```

**기본값 수용 시 의도적 클릭 = 2회** (셔터 + 입혀보기). 조정/재조정은 모두 선택.

---

## 3. 화면별 정의 (와이어프레임)

표기: `주 액션` = 그 화면의 단 하나의 메인 / `선택` = 안 해도 진행됨 / `자동` = 클릭 없이 일어남.

### ① 얼굴 맞추기
```
요소:    카메라 프리뷰 + 가이드 오벌 + 안내 한 줄("기기 안에서만 처리")
         하단 셔터 버튼
자동:    얼굴/카메라 신호 안정 감지 → 셔터 활성화
주 액션: 셔터 누르기 → 촬영 → 자동으로 ②로 진행
제거됨:  정렬 게이트 4개(얼굴추적/방향/카메라/프레임) 노출,
         "얼굴 정렬 시작" / "촬영으로 이동" 네비 버튼, shot grid
```
> 정렬 상태는 가이드 오벌 색/문구로만 암시(예: 안정되면 셔터 점등). 게이트 수치는 숨김.

### ② 분석 중
```
요소:    촬영된 얼굴 + 로딩 인디케이터 + "내 얼굴 분석 중…"
자동:    입술 등 4부위 경계 추출 + 마스크 후보 생성 (전부 자동)
주 액션: (없음 — 자동)
제거됨:  "MediaPipe 후보 생성" 버튼, provider 명칭, "네 지역 후보" 같은 내부 용어
```
> 실패 시에만 "다시 시도" 1버튼 노출.

### ③ 꾸미기  (핵심 인터랙션)
```
요소:    [ 큰 미리보기 ]
         부위 탭:  립 · 블러셔 · 브로우 · 아이라이너   (한 번에 하나)
         색:       ● ● ● ● ●   (부위별 전체 팔레트)
         농도:     ▮▮▮▮▮──   슬라이더
         ── 세부 조정 ▾ ──     (기본 접힘 / 펼치면 부위별 전체 컨트롤)
              · finish: 매트 · 크림 · 글로스
              · 광택/질감: 글로스량 · 커버리지 · 질감량 · 경계 부드러움
              · 모양(부위별 동적):
                  립    → 입꼬리·윗입술·밑입술·안쪽채움·위치
                  브로우 → 두께(detail)·좌우퍼짐·상하위치·간격·각도·아치…
                  블러셔/아이라이너 → 해당 부위 파라미터
         하단: "입혀보기"
자동:    진입 시 추천 룩 + candidate 자동선택이 이미 적용된 상태 (아무것도 안 만져도 됨)
주 액션: "입혀보기" → ④
선택:    부위 전환 / 색 / 농도 / (펼쳐서) finish·광택·질감·모양 전부 / "다시 촬영"
보존:    finish·tuning(광택/커버리지/질감/경계)·candidate·전체 색 팔레트 = 삭제 아님,
         세부 조정 안에 살아있음. 기본은 자동값, 원하면 펼쳐서 변경.
숨김(자동): provider 선택은 노출 안 함(제품 결정), 단 내부 로직/값은 유지.
```
> **세부 조정은 부위별 동적 패널**이다. 립의 5개 모양 필드만으로 고정하면 full-face 후 brow의 다수 파라미터를 잃으므로, 선택된 부위에 맞는 필드를 보여준다.
> **candidate 자동선택**은 "품질 저하 없는 기본 1개를 명시적 로직으로 고르는 것"이다(예: 사용자 맞춤 tight 우선). 자동=고정이 아니라 좋은 기본값이며, 세부 조정에서 변경 가능하게 남긴다.

### ④ 입혀보기
```
요소:    단일 진행 로딩 ("AR에 적용 중…")
자동:    저장 → AR 전송 → 얼굴 추적 확인 (내부 3게이트를 하나의 로딩으로 표현)
주 액션: (없음 — 자동 진행)
실패시:  "다시 시도" / "촬영부터 다시"  (이때만 노출)
제거됨:  저장/전송/AR확인 게이트 pill을 사용자에게 분리 노출
```
> "AR 화면 확인 전에는 적용 완료로 표시하지 않는다"는 내부 규칙은 유지(과장 금지).

### ▣ AR 결과  (왕복 루프 — 보존 + 개선)
```
요소:    실시간 AR(내 얼굴에 메이크업 적용된 화면)
         상단: "메이크업 적용됨"
         하단 끌어올리는 시트(기본 접힘):
              켜기/끄기 · 농도
              "세부 조정"  → ③ 꾸미기의 부위탭·색·finish·광택·질감·모양 전 컨트롤을
                            이 시트 안에서 그대로 사용 (능력 동일, 위치만 AR 위)
         "이 룩 제품 보기"  → 302 RAG 쇼핑 연결
주 액션: 보기(확인). 만족하면 끝.
보존:    ③ 꾸미기로 돌아가 재조정하는 능력 + ③의 모든 조정 컨트롤 (핵심 가치)
개선:    화면을 떠나지 않고, 실시간 AR 위에서 하단 시트로 바로 재조정 →
         얼굴에 올라간 상태를 보면서 색/농도/세부조정 즉시 반영
제거됨:  "진하게 보기" / "경계 보기" / "Debug" / hot 검증색
```

> 개선 핵심: 기존엔 "조정 화면으로 돌아가기"가 별도 화면 전환이었다(App.tsx:4584의 onReopenGenerate→adjust). 재정의에서는 **AR을 유지한 채 하단 시트로 인라인 조정**해 "보면서 고치기"를 더 매끄럽게 한다. 별도 화면으로 완전히 나가는 경로도 남겨 둘 수 있다.

---

## 4. 보존 vs 숨김/제거

| 분류 | 항목 |
| --- | --- |
| **보존(로직/에셋/표현력/조정) — 삭제 없음** | 4부위 마스크 생성 파이프라인, 텍스처·전체 색 팔레트·마스크 id, 모양 조정(입꼬리·윗입술·밑입술·안쪽채움·위치 및 부위별 필드), **finish(매트·크림·글로스)**, **광택/커버리지/질감/경계 tuning**, runtime candidate(선택 가능), AR 적용·ack 로직, **AR↔세부조정 왕복 루프** |
| **재배치(능력 유지, 위치/기본값만 변경)** | finish·tuning·candidate·색을 "세부 조정 ▾"(부위별 동적) 안으로 + 기본 자동값 적용. extract·생성·정렬·단계이동 *트리거*는 자동 실행(이건 조정 능력이 아니라 버튼). provider는 UI 미노출이되 내부 로직/값 유지 |
| **제거 (제품 기능 아님 — 검증/디버그/개발 언어만)** | Debug 버튼, 검증 토글(진하게/경계 보기), hot 검증색, Entry/exit 카운터, "검증/Generate/MediaPipe" 개발 언어, 죽은 compact-controls 코드(App.tsx:3243 `showCompactControls=false`) |

---

## 5. 현재 코드 매핑 (참고)

| 재정의 화면 | 현재 코드 |
| --- | --- |
| ① 얼굴 맞추기 | wizard start+align+capture ([App.tsx:4186](rn/MakeupARValidation/App.tsx#L4186), [4207](rn/MakeupARValidation/App.tsx#L4207), [4234](rn/MakeupARValidation/App.tsx#L4234)) |
| ② 분석 중 | wizard extract ([App.tsx:4315](rn/MakeupARValidation/App.tsx#L4315)) |
| ③ 꾸미기 | wizard adjust ([App.tsx:4355](rn/MakeupARValidation/App.tsx#L4355)) — 현재 lip 전용, full-face 후 4부위 탭으로 확장 |
| ④ 입혀보기 | wizard apply ([App.tsx:4455](rn/MakeupARValidation/App.tsx#L4455)) |
| ▣ AR 결과 | GeneratedRuntimeAppliedBanner ([App.tsx:4550](rn/MakeupARValidation/App.tsx#L4550)) |

---

## 6. 다음 단계

1. 1단계 full-face 통합 완료 후 ③ 꾸미기에 4부위 탭이 붙는다.
2. B: 검은 계열 색 토큰·버튼·컴포넌트를 디자인 시스템으로 정립(Claude Design).
3. C: 위 화면 정의를 302 `apps/mobile`에 Tamagui로 구현(기본 Codex).
