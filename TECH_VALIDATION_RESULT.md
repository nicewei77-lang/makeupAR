# Tech Validation Result

Date: 2026-06-21

Latest re-check: 2026-06-22 11:18 KST

Latest document cleanup: 2026-06-22 KST

Latest E7.03 contract reset: 2026-06-22 KST

Latest E7.03 Phase 1 UI/evidence hygiene implementation: 2026-06-22 KST

Latest E7.03 Phase 1 real-device build/log/capture-tooling check: 2026-06-22 14:25 KST

Latest E7.03 Phase 1 screenshot/LogBox cleanup: 2026-06-22 14:27 KST

Latest E7.03 phase-wide time-saving plan update: 2026-06-22 KST

Latest E7.03 Phase 2 ARFace authored atlas MVP implementation/build/install check: 2026-06-22 15:24 KST

Latest E7 reference-driven UV atlas pre-gold setup: 2026-06-22 KST

Latest E7.03 P2 synchronized reference capture: 2026-06-22 23:34 KST

Latest E7.03 A/B UV atlas fast gate: 2026-06-23 KST

Latest E7.03 P8 runtime sweep implementation prep: 2026-06-23 KST

Latest E7.03 P8 runtime sweep build/log/visual review: 2026-06-23 KST

Latest E7.03 smooth-mask validation app cleanup/user acceptance: 2026-06-23 KST

Latest workspace share cleanup/document structure pass: 2026-06-23 KST

Latest E7 lip-first pre-AR calibration Phase 1 contract: 2026-06-25 KST

Latest E7 lip-first pre-AR calibration Phase 2 Boundary Fusion contract: 2026-06-25 KST

Latest E7 lip-first pre-AR calibration Phase 3 UV Projection preparation: 2026-06-25 KST

Latest E7 M1 buildless lip package audit/repair: 2026-06-25 KST

Latest E7 M1 local/offline face parsing integration: 2026-06-25 KST

Latest E7 M1 color/gradient confidence gate: 2026-06-25 KST

Latest E7 M1 user adjustment buildless gate implementation: 2026-06-26 KST

Latest E7 M1 failure-mode classification deferral: 2026-06-26 KST

Latest E7 M1 calibration package contract clarification: 2026-06-26 KST

Latest E7 M1 rejected manual lip marking lockout: 2026-06-26 KST

Latest E7 user-painted gold raw references: 2026-06-26 KST

Latest E7 M1 gold-reference mask derivation experiment plan: 2026-06-26 KST

Latest E7 M3 phone-disconnected prep scope: 2026-06-26 KST

Latest E7 overnight multi-agent launch contract: 2026-06-26 KST

Latest E7 Section 14B multi-agent mask derivation run: 2026-06-26 KST

Latest E7 overnight no-stop provisional M1/M3A artifacts: 2026-06-26 KST

Latest E7 buildless runtime candidate source-stage/log analysis setup: 2026-06-26 KST

Latest E7 local CV tooling setup: 2026-06-27 KST

Latest E7 MediaPipe isolated retry: 2026-06-27 KST

Latest E7 MediaPipe candidate evaluation: 2026-06-27 KST

Latest E7 Personalized Lip Generate web beta/RN-Unity source-stage: 2026-06-27 KST

Latest E7 Personalized Lip Generate web beta user-review repair: 2026-06-27 KST

Latest E7 in-app Generate immediate adjustment sync: 2026-06-27 KST

Latest E7 in-app Generate fix-build candidate source/web/RN/Unity ack prep: 2026-06-27 KST

Latest E7 Full Face Region Generate complete implementation plan: 2026-06-27 KST

Latest E7 Full Face Region Generate phone-disconnected pre-Xcode scope update: 2026-06-27 KST

Latest E7 Full Face Region Generate Claude external critic usage update: 2026-06-27 KST

Latest E7 Full Face Region Generate pre-Xcode implementation gate: 2026-06-27 KST

Latest E7 Full Face Region Generate Unity batchmode smoke retry: 2026-06-27 KST

Latest E7 Full Face Region Generate phone-connected build/install/launch: 2026-06-27 14:18 KST

Latest E7 in-app Generate capture-first app-flow plan addendum: 2026-06-27 KST

Latest E7 in-app Generate forced wizard/native Vision pre-Xcode implementation: 2026-06-27 KST

Latest E7 in-app Generate native MediaPipe pre-Xcode unblock: 2026-06-27 KST

Latest E7 in-app Generate real-device review fix plan: 2026-06-27 KST

Latest E7 in-app Generate prebuild gate pass / UnityFramework ack sync: 2026-06-27 KST

Latest E7 eye/skin/brow Vision-MediaPipe-FaceParsing provider recovery checkpoint: 2026-06-27 KST

Latest E7 in-app Generate fix-build iPhone build/install/launch: 2026-06-28 00:38 KST

Latest E7 in-app Generate fix-build v2 plan after screenshot review: 2026-06-28 KST

Latest E7 in-app Generate fix-build v2 prebuild bug audit: 2026-06-28 KST

Latest E7 in-app Generate user-flow truth audit: 2026-06-28 KST

Latest E7 in-app Generate picky-user UI audit: 2026-06-28 KST

Latest E7 in-app Generate user-perspective reliability audit: 2026-06-28 KST

Latest E7 in-app Generate tri-agent reliability/build-minimization audit: 2026-06-28 KST

Latest E7 in-app Generate file-profile build minimization audit: 2026-06-28 KST

Latest E7 AR lip release-quality stabilization plan hardening: 2026-06-29 KST

Latest E7 AR lip release-quality stabilization buildless implementation: 2026-06-29 KST

Latest E7 AR lip release-quality UnityFramework freshness gate hardening: 2026-06-29 KST

## Current Session Snapshot

This snapshot is the default entry point for future Codex sessions. Read this section first, then lazy-load only the milestone-specific plan or research document needed for the current task.

Current boundary:

- Current active roadmap is `docs/roadmaps/active/E7_FULL_FACE_REGION_GENERATE_COMPLETE_IMPLEMENTATION_PLAN_KO.md`. It supersedes the lip-only personalized Generate plan by expanding the current product-quality slice to `lip`, `blush`, `brow`, and `eyeliner`: CLI/local experiment loops first, web app only as pre-build UI/logic verification, saved mask packages before AR entry, RN/Unity runtime consumption setup, and real-device evidence after the pre-Xcode build gate. External mask priors are allowed as local-only silver drafts, while face parsing and color/gradient confidence remain helper/evaluation signals rather than runtime primary trackers or standalone gold. Claude may be used as an external critic only: Codex prepares a concrete context packet, Claude critiques risks/fallbacks/missing checks, and Codex remains responsible for repo implementation and evidence-gated decisions. The plan requires all four regions to be completed or explicitly recorded as `pre-xcode-ready` / `partial` / `blocked`; eyeliner must produce at least one candidate through reference photo, external prior, parametric curve, or soft lashline fallback rather than being dropped for difficulty. Build/install/launch evidence alone is not Green/product-quality-ready.
- Unity MCP setup is connected for local editor tooling. `com.unity.ai.assistant@2.0.0-pre.1` is installed in the Unity package manifest/lock, Codex global MCP server `unity-mcp` points to `~/.unity/relay/relay_mac_arm64.app/Contents/MacOS/relay_mac_arm64 --mcp`, and Unity Project Settings > AI > Unity MCP shows Unity Bridge `Running`. Verification on 2026-06-29: relay `--help` worked, MCP initialize/list discovered the enabled Unity tools, and `Unity_GetConsoleLogs` returned `success=true` with 2 warnings / 0 errors. Evidence: `evidence/logs/unity-mcp-setup-20260629.log`, `evidence/logs/unity-mcp-editor-open-20260629-retry.log`, and `unity/MakeupARUnityValidation/Logs/relay.txt`. Limitation: this Codex app turn did not dynamically expose the newly registered Unity MCP namespace via tool search; reload/new session may be needed for native tool calls, and sandboxed terminal probes cannot access the Unity `/tmp/unity-mcp-*` socket without host-level execution.
- E7 in-app Generate prebuild gate is now closed for the next user-approved Xcode build, but still not device-proven. The fake RN rounded-rectangle lip preview was removed; RN now displays only native `file://` preview PNGs rendered from each saved `generated_lip_package.json` boundary. A new `npm run e7:prebuild:full` gate verifies fixture package shape, local-only privacy, non-bbox lip polygon, nonempty UV alpha, RN preview contract, native preview export, RN ack-gated apply flow, Unity source ack persistence, package-local UnityFramework freshness, and native Vision/MediaPipe package checks. UnityFramework regeneration initially hit a stale Unity licensing client, then disk exhaustion; after clearing regenerable build caches and rebuilding, `scripts/build_m3_unityframework.sh` succeeded and synced both RN reference and package-local frameworks. Verification passed: RN TypeScript, Jest, ESLint, targeted `git diff --check`, UnityFramework verification log `evidence/logs/m3-repro-artifact-verification-e7-prebuild-ack-sync-retry-20260627-234721.log`, and `npm run e7:prebuild:full` with `23 pass / 0 fail / 0 warn`. No RN/Xcode app build, install, launch, runtime AR transition, or real-device visual acceptance was run in this checkpoint.
- E7 Full Face Region Generate is now implemented through the first phone-connected build/install/launch gate. `evidence/e7-region-generate/session-20260626T195853Z/` records the local-only experiment, selected policies, contact sheets, scorecards, adjustment axes, composite payload, and `pre_xcode_gate.md/json`. Region statuses remain `lip=pre-xcode-ready`, `blush=pre-xcode-ready`, `brow=pre-xcode-ready`, and `eyeliner=pre-xcode-ready`; this means package generation and RN/Unity source handoff are prepared and the app can be installed/launched, not that any region is visually accepted on-device. Web review shell, RN full-face package post, Unity product-region parser/overlay acceptance, and Unity Resource UV mask installation are committed. Verification passed for Python compile, Unity asset install, runtime asset JSON validation, RN TypeScript, web lint/typecheck/build, git diff checks, and Unity batchmode smoke. The smoke initially hit an existing Unity project lock, then the stale Unity/AssetImportWorker processes were closed and `E7FullFaceRegionPackageSmoke.RunFromCommandLine` passed with all four regions parsed/dispatched. UnityFramework regeneration/sync passed after installing the missing Xcode iOS platform support; the first RN CLI device run hit the known simulator-UDID fallback path, then direct `xcodebuild` passed, `devicectl` installed `com.makeupar.rnvalidation`, and `devicectl` launched it with the process still present on-device. Runtime visual evidence, face attachment, motion/expression/blink/yaw checks, FPS/frame-time, latency, memory, thermal, and human cosmetic-boundary acceptance remain required before any Green/product-quality-ready claim.
- E7 in-app Generate real-device review fix has now passed the user-approved iPhone build/install/launch gate, but the full Generate user flow and visual quality are still not accepted. Before build, `npm run e7:prebuild:full` passed with `23 pass / 0 fail / 0 warn`, RN TypeScript passed, and Jest passed with `9 passed`. The RN CLI path still hit the known `No simulator available with udid "undefined"` fallback issue, so the build used direct `xcodebuild`; the first direct attempt generated ReactCodegen outputs but failed on missing generated files, and the immediate second direct attempt succeeded. `devicectl` installed bundle `com.makeupar.rnvalidation`, launched it on `위승철의 iPhone` (`6F504EE9-BABC-5F6F-A186-C734E04CA625`), and a process check found `MakeupARValidation.app/MakeupARValidation` alive. Evidence summary: `evidence/logs/e7-inapp-generate-fix-build-20260628-0038-summary.md`. This proves build/install/launch only; the next gate is actual on-device flow evidence for align -> capture -> extract -> blending selection -> adjust -> save and AR run, including `generated_lip_mask_applied.latest.json` and user-visible mask/AR quality.
- E7 in-app Generate fix-build v2 is now buildless/source implemented and prebuild-gated, but still not iPhone visual accepted. The app-side fix adds shared `curve_densified_v1` adjusted boundary smoothing for package/UV/native preview, stronger adjustment deltas with generatedMaskId/UV diagnostics changes, smooth Swift preview curves, separate "현재 사진으로 다시 생성" vs "다시 촬영", explicit apply states `idle/saving/posting/waitingAck/applied/blocked/timeout`, timeout/retry/reason UI, stale apply reset on retake/provider/candidate/adjust changes, and post-ack AR validation controls for mask ON/OFF, strong mode, color, opacity, and boundary/debug. A prebuild bug audit also fixed two buildless-visible issues before the next Xcode run: blending/adjustment preview images now use full-face `contain` rendering instead of cropped `cover`, and the device evidence pull helper no longer mutates disk on `--dry-run` or reports a partial pull as fully `pulled`. A user-flow truth audit found and fixed a deeper calibration mismatch: the wizard required an n-shot capture set, but generation/blendshape assist used only the neutral shot. RN now calls the native provider for every captured shot, keeps neutral as the boundary anchor, persists per-shot provider summaries in `captureSetShotResults`, and writes capture-set blendshape summaries into `blendshapeAssist` / `sourceFaceState`. A picky-user UI audit then ran two UI-sensitive subagents through the Generate -> AR validation flow at least three times each and fixed the most likely "it did not move to AR / I cannot tell if it captured / the controls look technical" pain points: user-facing copy no longer exposes raw capture/package/ack wording, the Apply step reads as a waiting/progress screen, post-ack title changes to `AR 립 검증`, AR controls are larger and labeled `마스크 ON`, `진하게 보기`, `경계 보기`, and `농도`, capture feedback says saved/current next shot in user language, and candidate/adjustment previews are larger. Unity RNBridge source now accepts controls-only reapply without resending raw texture, preserves the generated mask texture id, logs sanitized blocked reasons, and persists validation control evidence in generated-mask ack files. Verification passed: RN TypeScript, RN Jest `15 passed`, RN ESLint, `packages/lip-generate-core` typecheck/test, `git diff --check`, `python3 -m py_compile scripts/e7_inapp_generate/pull_ios_generated_evidence.py`, dry-run no-output-side-effect check, and `npm run e7:prebuild:full` with `33 pass / 0 fail / 0 warn`. Unity editor generated-mask smoke was attempted earlier but blocked by Unity licensing channel initialization (`LicenseClient-wiseungcheol` timeout in `evidence/logs/e7-generated-lip-mask-smoke.log`), so Unity runtime behavior is source/static-gate covered only in this checkpoint. A local-only pull helper now exists at `scripts/e7_inapp_generate/pull_ios_generated_evidence.py` to copy/inspect latest generated package, saved record, generated apply ack, and capture summary from app Documents; the latest device check still showed the iPhone as `unavailable`, so no fresh Documents pull has succeeded after this fix. Do not claim device success yet; the next gate is user-approved Xcode/iPhone build plus actual capture -> extraction -> blending -> adjustment -> save/apply ack -> AR validation control visual evidence.
- A second user-perspective reliability audit found additional "feels fake or stuck" risks before the next iPhone build and fixed them at source/buildless level. Capture now has a timeout if Unity never returns the reference-capture event, and Unity capture events include a `framePreviewUri` so RN can show the actual saved `frame.png` instead of only a dark live-camera shield. A blocked provider path now offers `다른 방식 선택` instead of trapping the user, missing native save support is treated as blocked rather than a fake JS-memory success, apply timeout screens expose `저장/적용 재시도` and `촬영부터 다시`, and AR validation controls now wait for a matching `generated_lip_mask_applied` control ack or show a delayed-confirmation notice. Blendshape copy was softened to `표정 보조` so it does not overpromise geometry change, and adjustment/apply copy no longer exposes `look`, `finish`, `candidate`, `provider blockedReason`, or raw ack wording in the user flow. Verification passed: RN TypeScript, RN Jest `16 passed`, RN ESLint, `git diff --check`, and `npm run e7:prebuild:full` with `35 pass / 0 fail / 0 warn`. Unity batchmode smoke was attempted at `evidence/logs/e7-generated-lip-mask-smoke-20260628-user-audit.log` but was stopped after the same Unity Licensing Client channel loop; no C# compile pass or device visual proof should be claimed from that attempt.
- A tri-agent reliability/build-minimization audit then checked the same in-app Generate slice from code QA, gate/test, and build-time angles. Source fixes now reject late `e7_reference_capture` events unless `captureSetId`, `captureShotKind`, and `capturePairId` match the current pending shot; ignore stale `generated_lip_mask_applied` acks after retake/reset unless they match the current pending package/control check; and drop stale async candidate generation results when provider, capture set, or adjustment changed mid-request. New RN tests cover late capture ack, stale apply ack after retake, timeout/retry, and control ack confirmation. A new `npm run e7:build-plan` command classifies the current diff and framework state before phone builds; it currently reports `skip-unityframework-run-rn-xcode-only` because only RN/tooling/docs changed and both RN reference/package-local `UnityFramework.framework` artifacts are hash-synced with required capture/apply strings present. The same tool audits Unity assets and found `17` SmoothRegionMasks resources with `0` no-static-reference resources; apparent old Unity resources are still referenced by RN selectors, registries, scripts, or tooling, so deletion is deferred until those references are intentionally removed and Unity import/compile passes. Verification passed: RN TypeScript, RN Jest `18 passed`, RN ESLint, `npm run e7:build-plan -- --no-report`, `npm run e7:prebuild:full -- --no-report` with `34 pass / 0 fail / 0 warn`, and `git diff --check`. No Unity rebuild, RN/Xcode build, device install/run, or new visual acceptance was performed in this audit.
- A follow-up E7 build-minimization file-profile audit is now buildless/source complete. `scripts/e7_build/decide_minimum_build.mjs` now classifies Unity files into `product-runtime-required`, `product-runtime-fallback`, `product-runtime-reference`, `legacy-validation-debug`, `editor-only`, and `xr-simulation-debug`, writes the full file-level profile into `evidence/logs/e7-build-plan/latest/minimum-build-decision.json`, and supports `--changed-file=<path>` simulations without touching real files. Current audit output is `unityAssetProfiles=41`, `SmoothRegionMasks=17`, `legacyValidationResources=9`, `productRequiredOrFallbackResources=8`, and `excludeReadyResources=0`. Simulated checks route a legacy validation mask change to `skip-product-phone-build-legacy-debug-resource-only`, a fallback `lip-smooth-mask-v1.png` change to `run-unityframework-build`, RN `App.tsx` to `skip-unityframework-run-rn-xcode-only`, and Unity `RNBridge.cs` to `run-unityframework-build`. No Unity resources were moved or deleted in this step because RN `LIP_RUNTIME_CANDIDATE_OPTIONS`, old Compact HUD/recipe path, Unity legacy prefix acceptance, and registry/scripts references still make `excludeReadyResources=0`. `docs/runbooks/E7_BUILD_MINIMIZATION_RUNBOOK_KO.md` records the required-include, exclude-candidate, and still-blocked lists; `docs/runbooks/E7_PREBUILD_GATE_RUNBOOK_KO.md` records the new build-plan decision. Verification passed: `npm run e7:build-plan`, RN TypeScript, RN Jest `18 passed`, RN ESLint, `npm run e7:prebuild:full -- --no-report` with `34 pass / 0 fail / 0 warn`, and `git diff --check`. Unity batchmode smoke was attempted at `evidence/logs/e7-build-minimization-unity-smoke-20260628.log`, but Unity licensing initialization failed waiting for `LicenseClient-wiseungcheol`; treat this as environment/license blocked, not C# compile proof. No UnityFramework rebuild, Xcode/iPhone build, device install/run, or visual acceptance was performed.
- E7 AR lip release-quality stabilization is now planned in `docs/roadmaps/active/E7_FULL_FACE_REGION_GENERATE_COMPLETE_IMPLEMENTATION_PLAN_KO.md` section 18, with fast/strong execution guardrails added on 2026-06-29. The next implementation must raise the current "mask attaches but is rough/laggy" state toward app-release candidate quality by isolating root causes one by one: Unity overlay timing, per-frame mesh rebuild/GC, copied child mesh substrate, UV mask resolution/antialiasing, preview-vs-runtime boundary mismatch, shader threshold/feather behavior, adjustment +/- direction, stale first-screen state, and AR validation control ack/UX. Do not bundle all fixes into one untraceable pass; each loop needs minimal buildless checks, automatic mask/adjustment/stale-event metrics, and then iPhone visual/log evidence before any release-quality claim. If Unity licensing blocks batchmode/compile, record it as environment-blocked; Unity runtime/shader/material diffs still require a fresh UnityFramework before any final phone-build claim.
- E7 AR lip release-quality stabilization is now buildless/source implemented for the first L/Q/U/S/A pass, but still blocked before final phone-build proof by Unity licensing and required UnityFramework regeneration. L1/L2/L3 changes move `E3RegionMaskOverlay` sync to `LateUpdate`, subscribe to `ARFaceManager.trackablesChanged`, reuse mesh vertex/UV/index buffers, rebuild topology only when counts change, remove per-frame normal/bounds recalculation, and emit overlay sync phase/frame/trackables sequence/duration/worst/count/topology metrics through RNBridge acks/logs. Q1/Q2 changes raise generated UV masks to 512 by default, rasterize directly in UV space with 2x2 antialiasing, preserve inner-mouth hole metrics, and record `edgeBandRatio`, `innerHolePositiveRatio`, `alphaBoundingBoxTexels`, and `previewVsUvRoundTripDelta`. U1 fixes `verticalOffset +` to move upward and shader upper/lower tightness so `+` includes more/expands. S1/A1 harden first-screen and control state: default lip is not auto-enabled, unmount clears Unity overlay, control payload visibility matches `maskVisible`, and generated control acks require matching request id/revision before showing "반영되었습니다". Verification passed on 2026-06-29 KST: RN TypeScript, RN Jest `26 passed`, RN ESLint, `packages/lip-generate-core` typecheck/test, `npm run e7:build-plan -- --no-report`, `npm run e7:prebuild:full -- --no-report` with `36 pass / 0 fail / 0 warn`, and `git diff --check`. Unity batchmode smoke was attempted at `evidence/logs/e7-generated-lip-mask-smoke-20260629.log` but Unity Licensing Client channel initialization failed (`LicenseClient-wiseungcheol-6000.3.18` timeout), so there is no C# compile/device runtime proof yet. Build-plan currently reports `decision=run-unityframework-build` because Unity runtime/shader files changed and RN reference UnityFramework is missing; do not claim final iPhone success until `bash scripts/build_m3_unityframework.sh` succeeds or the Unity licensing block is resolved.
- Follow-up freshness gate hardening now prevents a stale package-local UnityFramework from passing the next pre-Xcode gate after the AR lip runtime/shader changes. `scripts/e7_build/decide_minimum_build.mjs` now requires the packaged framework to contain `overlaySyncPhase` plus generated-mask control request strings, not only the older generated-mask ack/capture strings. `scripts/e7_prebuild_gate/check_e7_prebuild_gate.mjs` applies the same stricter string check. Current verification on 2026-06-29 KST: `/opt/homebrew/bin/node --check` passed for both scripts, `git diff --check` passed, and `npm run e7:build-plan -- --no-report` reports `decision=sync-or-rebuild-unityframework-before-xcode`, `reason=rn_reference_unityframework_missing`. `npm run e7:prebuild:full -- --no-report` now intentionally stops at `35 pass / 1 fail / 0 warn` on `unity.framework_contains_ack_persistence` until `bash scripts/build_m3_unityframework.sh` regenerates/syncs a framework containing the new overlay sync and control-request runtime strings. This is a build freshness block, not device visual evidence.
- Baseline E7 in-app Generate real-device review before the source/buildless fix found that capture/extract/save were partially real, but the product flow was not ready for another build gate without fixes. Device Documents evidence under `evidence/logs/e7-device-pull-20260627-post-user-review/` shows actual capture files, ARFace mesh/UV/blendShapes, native Vision `lipBoundary2D`, a raw RGBA UV mask package, and `saved_local_only`; however the saved package remains `runtimeReady=false`, only Vision is recorded in `providerResults`, adjustment values are all zero for that run, `roundTripPreview` is pending, and no persisted `generated_lip_mask_applied` Unity ack exists. User-visible failures are routed to `docs/roadmaps/active/E7_FULL_FACE_REGION_GENERATE_COMPLETE_IMPLEMENTATION_PLAN_KO.md` section 13.8: hardcoded alignment checks, confusing multi-button capture, overlapping neutral/mouthClosed shots, missing mask overlay in Adjust, wrong Compare wording, separate Save page, no save-and-apply ack gate, no AR lip runtime transition, and debug/log surfaces that can obscure face review. The old 17.2 Xcode Gate Prompt is superseded; the next build gate must validate the 13.8/17.3 fixes on-device before any product-quality-ready claim.
- E7 eye/skin/brow Vision-MediaPipe-FaceParsing local sample recovery is complete for buildless comparison, not product-quality Green. The latest comparison target is `evidence/e7-reference-atlas/capture_pairs/pair_face_20260627T091334Z_06/`. ARFace eye remains a projected `eye-smooth-mask-v1.png` UV prior and ARFace skin remains a filled face mesh surface; both are useful baselines, not semantic recognition. Apple Vision initially failed inside the sandbox with ANE model load / `Unspecified error`, but sandbox-out local Swift execution generated `vision_face_landmarks.json` with confidence `1.0`, eye point counts `6/6`, brow point counts `6/6`, and face contour count `17`. MediaPipe still fails in Codex shell because the Tasks FaceLandmarker cannot open the macOS GL/Metal helper service, and legacy `mp.solutions.face_mesh` is unavailable in the current wheel; however the same `.venv`, same local frame, and same `face_landmarker.task` succeeded from a GUI-capable Terminal session and generated `mediapipe_face_landmarks.json` with `478` landmarks, eye point counts `16/16`, brow point counts `10/10`, and face oval count `36`. Face parsing was recovered through an isolated `/private/tmp/makeupar-faceparsing-py312` Python 3.12 Torch venv, using the existing local BiSeNet runner and `79999_iter.pth` checkpoint; it generated `face_parsing_labels.json` with status `silver`, eye pixels `7,245`, skin pixels `673,811`, brow pixels `32,521`, and upper/lower lip pixels `16,640/20,551`. The comparison builder regenerated `contact_sheet_eye_skin_brow.png` with Vision and MediaPipe eye/brow rows `available`, Vision/MediaPipe skin rows `approximate`, ARFace/color rows `approximate`, and face parsing eye/skin/brow rows `available`. Evidence and explanation are recorded in `docs/product/e7-region-detection-comparison-report-2026-06-27/README.md` plus `evidence/e7-region-detection-comparison/pair_face_20260627T091334Z_06/summary.md/json`. This proves local single-frame provider sample generation only; it is not iPhone runtime visual acceptance, not motion/expression stability, and not product-quality readiness.
- E7 eyeliner final candidate experiment is complete for buildless/local-only app-implementation handoff, not product-quality Green. `scripts/e7_eyeliner_candidate_experiment/run_eyeliner_candidate_experiment.py` consumed the existing `pair_face_20260627T091334Z_06` frame, `mediapipe_face_landmarks.json`, and same-pair ARFace export without rerunning live MediaPipe, Xcode, or iPhone actions. The latest run is `evidence/e7-eyeliner-candidate-experiment/experiment-20260627T140808Z/` and generated 8 MediaPipe/parametric candidates plus 3 same-frame legacy eye-prior baselines, full-frame/eye-crop/left/right/UV round-trip contact sheets, `scorecard.json`, `selected_policy.json`, `adjustment_axes.json`, `app_handoff_ui_notes.md`, and `summary.md`. Human visual review preferred `asset-fit-wing-local-style-v0` because product eyeliner should fill through the outer corner; `mp-upper-balanced-v0` remains the clean MediaPipe upper-eyelid tracking/reference anchor, and `mp-tail-only-v0` remains the safety fallback. Implementation handoff is MediaPipe upper eyelid landmarks + asset-fit wing style preset + ARFace UV runtime substrate + slider/quick-button adjustment, with color/edge only as optional snap helper and face parsing only as offline/evaluation helper. The Korean report and committed artifact mirror are under `docs/product/e7-eyeliner-candidate-experiment-report-2026-06-27/`. Blink/yaw/squint, runtime UV sharpness, real-device AR visual acceptance, FPS/frame-time, latency, memory, and thermal remain deferred to the phone-connected session.
- Primary path: E7.03 v2.1 Boundary Engine Quality Experiment. Phase 0 Contract Reset is complete. Phase 1 Validation UI/Evidence Hygiene is implemented, JS-verified, real-device build/log smoke-verified, and user-provided iPhone screenshots confirm Clean / Compact HUD / Full Debug mode visibility. Phase 2 ARFace authored atlas MVP is implemented with manual vertex labels, topology/UV audit metadata, RN-selectable atlas variants, and preserved E3/E4/procedural baselines. Phase 2 is JS/lint verified, UnityFramework build/sync verified, and RN real-device build/install verified; runtime launch/visual evidence is still pending because the iPhone was locked during launch.
- Reference-driven UV atlas P2 synchronized capture is complete for input collection only. The validation app now exports clean frames plus same-moment ARFace mesh/UV/screen projection data, suppresses Unity face renderers/candidate masks/debug panels during capture, uses unique timestamped `pair_face_*` capture IDs for repeated attempts, and places the capture button away from the front camera. Three copied P2 capture folders are available under `evidence/e7-reference-atlas/capture_pairs/`; `pair_face_20260622T143334Z_03` is the recommended clean reference frame for coordinate-paired gold selection. User-painted gold raw references now exist under `evidence/references/e7-user-gold-raw-20260626/`, but no selected gold asset has been promoted through UV back-projection, round-trip render, P3/P4 acceptance, MediaPipe runtime, or E7.4 work.
- A no-build A/B UV atlas fast gate was run from the two user-authored mask sets on `pair_face_20260622T143300Z_01` and `pair_face_20260622T143334Z_03`. After fixing the fast-gate back-projection sampling density, `lip`, `cheek`, and `eye` all produce continuation signals, so the offline decision is `continue`. This justifies preparing a small P8 runtime sweep candidate set, but it does not mark E7.03 Green.
- P8 runtime sweep build/log/visual evidence is collected for the default reference UV runtime path. The frozen runtime candidate set lives under `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/` with exactly 9 reference UV variants (`core`, `balanced`, `soft-wide` for `lip`, `cheek`, and `eye`), and RN/Unity support `rendererMode="e7-reference-uv-atlas"` / `candidateId="arface-reference-uv-atlas"`. The latest iPhone runtime stream confirms `lip`, `cheek`, and `eye` all sample ARFace UV probability atlases with `tracking_render`, mesh/UV counts `1220/6912/1220`, and `usedFallback=false`. User-provided Compact HUD photos confirm the masks appear face-attached, but all three regions remain Yellow because the silhouettes are still jagged/rough and the full white debug face surface is not Q3 overlay-ready makeup rendering.
- E7.03 smooth-mask validation app cleanup is accepted for the current optimization goal. Candidate/variant selection UI, old capture/reference controls, and stale visual debug paths were removed from the active app surface. The current runtime defaults to one `smooth-region-mask` path with three batched layers (`lip`, `cheek`, `eye`) and per-layer `enabled` flags. HUD status is inside the bottom `Regions` panel instead of floating over the face. User-provided acceptance screenshots are stored at `evidence/screenshots/e7-smooth-mask-accepted-2026-06-23/` and show visible, natural, subtle makeup on the face with Compact HUD reporting `active=lip,cheek,eye`, mesh `v=1220/i=6912/uv=1220`, FPS `52.2-60.2`, and recipe latency `36.0ms`. User visual feedback: the overall makeup is still hard to see, the eye boundary is somewhat lacking, and this validation stage ideally should make the covered region more unmistakable; however, the current result is accepted and the remaining visibility/boundary issue should be handled together with the next cosmetic expression/modeling work. This closes the current app cleanup/optimization goal by user acceptance, but it does not mark E7.03 Green or prove product-quality cosmetic rendering.
- E7 Lip Sample Pack v0 is installed and runtime-reviewed on the real iPhone for the first lip-only `daily` / `gloss` / `texture` sample sweep under explicit E7.3 Yellow-risk conditions. RN posts v2 `lookId` payloads for `lip_daily`, `lip_gloss`, and `lip_texture` with exactly three layers while only `lip` is enabled by default; Unity smooth-mask rendering receives `coverage`, `finish`, `textureAmount`, `roughness`, `specular`, `specularPower`, and `glossBoost` as material/shader inputs. Buildless RN verification passed before the device build (`npm test -- --runInBand --watchman=false`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`), UnityFramework build/sync passed (`evidence/logs/e7-lip-sample-pack-v0-unityframework-20260625-150509.log`), RN iPhone build/install/launch passed (`evidence/logs/e7-lip-sample-pack-v0-rn-ios-run-20260625-151236.log`), and the RNUnityView timing patch follow-up reinstall passed (`evidence/logs/e7-lip-sample-pack-v0-rn-ios-run-unityviewfix-20260625-152046.log`). Runtime video review of `/Users/wiseungcheol/Downloads/ScreenRecording_06-25-2026 15-25-02_1.mov` confirms camera/tracking/mesh are alive, but the result is `continue with revise`: lip boundary is too broad, HUD blocks visual tuning, gloss/texture are weak alpha/proxy effects, and slider drag is unstable. Use `docs/roadmaps/archive/E7_LIP_SAMPLE_PACK_V0_RUNTIME_REVIEW_CONTEXT_KO.md` only if lip sample tuning/review context is explicitly reopened.
- RNUnityView timing patch durability follow-up is complete for buildless reproducibility: `rn/MakeupARValidation/scripts/apply-rn-unity-timing-fix.js` reapplies the local `RNUnityView.mm` window/non-zero-bounds initialization patch, `package.json` runs it on `postinstall`, and `ios/Podfile` runs it during Podfile evaluation so `pod install` reasserts the patch. Verification in this session: first `node scripts/apply-rn-unity-timing-fix.js` patched the package-local file, repeated runs and `npm run postinstall` reported already patched, `ruby -c ios/Podfile` returned `Syntax OK`, and targeted ESLint passed for the new script. No UnityFramework rebuild, RN iPhone build, or runtime/device evidence was run for this buildless durability cleanup.
- E7 lip-first pre-AR boundary calibration Phase 0 is complete as a documentation-only contract: `docs/roadmaps/archive/E7_LIP_BOUNDARY_PRE_AR_CALIBRATION_SPIKE_PLAN_KO.md` defines the plan to analyze heavily before AR runtime, then use lightweight ARFace UV/vertex tracking during runtime. This does not mark E7.3 Green and does not include code, build, runtime, or device evidence.
- E7 lip-first pre-AR boundary calibration Phase 1 calibration flow contract is complete: the archived boundary plan defines neutral / open-close / smile / pucker / yaw capture flow, required input signals, `lip-calib-*` local package schema, raw-frame/no-upload policy, and Phase 2 Boundary Fusion / Phase 3 UV Projection handoff. This is documentation-only and does not mark E7.3 Green, start E7.4/E7.5/E7.6, or include Unity/iPhone build evidence.
- E7 lip-first pre-AR boundary calibration Phase 2 Boundary Fusion is ready for contract/stub handoff: the archived boundary plan fixes input readiness, signal priority, confidence/rejection rules, `lip-tight-auto-v0` / `lip-tight-user-v0` / `lip-safe-v0` candidate output rules, `fusionSummary.json` / `fusionSummary.md` shape, and the Phase 3 UV Projection handoff fields. A buildless local stub exists at `scripts/e7_lip_boundary_fusion/prepare_fusion_summary.py` to read a `lip-calib-*` package and emit summary contracts only. No raw frames were added, no upload/runtime inference/build was run, and this does not mark E7.3 Green.
- E7 lip-first pre-AR boundary calibration Phase 3 UV Projection preparation is ready: the archived boundary plan fixes the `fusionSummary.json` -> same-moment capture pair -> screen-space lip reference mask -> ARFace UV -> screen round-trip input/output contract, missing-input rules, and expected artifacts (`lip_probability.png`, `lip_coverage.png`, `lip_unknown.png`, `lip_debug_votes.png`, `lip_variants.json`, `round_trip_overlay.png`, `summary.json`, `summary.md`). A buildless local prep/execution stub exists at `scripts/e7_lip_uv_projection/prepare_one_frame_round_trip.py`; it can report `ready|partial|blocked` from missing inputs or produce the offline artifact set when a real `fusionSummary.json`, same-moment `frame.png` / `arface_export.json`, and accepted lip reference mask are supplied. The current one-frame execution state is superseded by the M1 audit/repair record below; any same-frame round-trip output is projection sanity only, not boundary quality proof. No Unity/iPhone build, live face parsing/Core ML runtime, shader/material/runtime candidate change, upload, or raw-frame long-term storage was done, and this does not mark E7.3 Green.
- E7 M1 Buildless Personalized Lip Package audit/repair is now complete for buildless evidence only and remains `partial`, not ready. The previous `m1-lip-package-20260625T112757Z` folder was audited as partial/not-ready, and the repaired output folder is `evidence/e7-lip-m1-packages/m1-lip-repair-20260625T120000Z/`. Mesh draft artifacts exist, but visual audit marks the mesh draft too broad for reference/gold. The prior `manual_polygon_codex_visual_reference_v0` lip marking and all derived masks from it are now explicitly rejected by user decision as wrong marking; they must not be used as gold, reference, candidate seed, UV projection input, or M1 readiness evidence, and their metadata should carry `rejected_by_user_wrong_marking`. The user has now provided and approved a separate human-painted gold raw reference set under `evidence/references/e7-user-gold-raw-20260626/` with `manifest.json`; every file in that folder is `acceptedAsGold=true` for offline reference, candidate generation, candidate scoring, and mask derivation experiments. The Section 14B multi-agent run generated buildless mask-derivation artifacts under `evidence/e7-lip-mask-derivation/experiment-20260625T212637Z/` using only the two direct lip gold sources, produced the required gold-derived candidate families, `contact_sheet.png`, `mask_derivation_scores.json`, `selected_policy.json`, and `mask_derivation_summary.md`, and passed tester integrity checks. Its decision remains `partial_needs_review`, not `ready_for_coordinate_pairing`, because `gold_exact_binary` and `gold_clean_morphology` are effectively tied with score margin `0.0000773822`, optional Vision/parsing/inner-mouth signals were not used, and no same-moment ARFace coordinate pairing was performed. These user-painted gold assets are separate from the rejected polygon lineage. A gold asset still needs coordinate-space and same-moment ARFace export pairing before it can satisfy M1 UV projection readiness. Apple Vision lip contour extraction is implemented as a buildless M1 pre-filter signal; `evidence/e7-lip-m1-packages/m1-lip-apple-vision-20260625T132216Z/` records `visionContourAvailable=1`, confidence `1`, outer/inner contour point counts `14/6`, and a contour overlay for the same neutral capture frame, but any package there that reuses the rejected manual mask remains invalid for reference use. Local/offline face parsing is now downloaded and integrated for the same neutral capture as a silver M1 signal: `evidence/e7-lip-m1-packages/m1-lip-face-parsing-20260625T2255Z/` records `faceParsingSilver=1`, upper/lower lip label pixels `7325/9842`, inner-mouth label pixels `0`, and a face parsing overlay, but its fused masks derived from the rejected manual marking are invalid for gold/reference use. Color/gradient confidence is now implemented as a buildless PIL/numpy confidence-only gate and verified under `evidence/e7-lip-m1-packages/m1-lip-color-gradient-20260625T000000Z/`: neutral capture `colorGradientConfidence.status=computed`, `colorGradientComputed=1`, `lowContrastWarning=true`, `shadowWarning=false`, and `specularWarning=false`; this is not a boundary source. New user adjustment observations record frequent insufficient horizontal width / missed corners, frequent lower-lip spill, possible upper-lip spill, and vertical-position dissatisfaction; the lip boundary/candidate plans therefore supersede legacy `tightness` / `upperLowerBalance` / `cornerShrink` with `cornerReach`, `upperLipTightness`, `lowerLipTightness`, and `verticalOffset`. The buildless user adjustment gate is now implemented in code: `prepare_user_adjustment_candidates.py` creates 12 derived candidates/contact sheet/review template, `prepare_m1_lip_package.py --user-adjustment-review` applies only a valid `user_confirmed` selection to `lip-tight-user-v0`, and fusion/audit reject legacy-only, unconfirmed adjustment params, and rejected reference masks. Failure-mode classification is intentionally deferred from the M1 ready gate until boundary quality reaches an initial useful level for edge-case taxonomy; missing failure-mode tags should be recorded as future eval follow-up, not as an M1 blocker. Verification used `/tmp` smoke outputs only; no retained package is promoted as real user-approved evidence. Remaining M1 required gates are still missing or partial: human review of the 14B tie, coordinate-paired user gold reference selection, real user-confirmed adjustment evidence for the retained package, pucker/expression capture coverage, blendshape/face-state, coordinate-space audit, visibility/front-most handling, held-out/eval plan, inner-mouth exclusion acceptance, corner falloff, and full upper/lower split acceptance. The repaired, Apple Vision, face parsing, color/gradient, user-adjustment, and Section 14B implementation checks all keep M1 `partial`; one-frame UV round-trip remains `same_frame_self_reconstruction` projection sanity only with `boundaryQualityProof=false`, `coordinateSpaceAuditStatus=pending`, `visibilityConfidence=partial`, and runtime variants not ready. This does not mark M1 ready, E7.3 Green, runtime readiness, or any M2/build gate.
- The overnight no-stop continuation replaced human-review stops with explicit `auto_provisional` artifacts and pushed all buildless lip-document work that did not require new capture/build/device evidence. Optional-signal derivation reran under `evidence/e7-lip-mask-derivation/experiment-20260625T214159Z/`, generating 13 candidate families with Vision/parsing/color auxiliary signals; it still ended `partial_needs_review` because the top `hybrid_conservative` policy is valid for only one of two gold references and score margin is tiny. Coordinate preview artifacts live under `evidence/e7-lip-coordinate-pairing-preview/preview-20260625T213931Z/`; they reject `A_1` for this same-moment pair (`roundTripIoU=0.0000`, visually below mouth/chin) and show `user-gold-lip-mask-gray-20260625-163204` as lip-local with `roundTripIoU=0.9664`. The auto-selection artifact `evidence/e7-lip-auto-selection/selection-20260625T214943Z/selected_policy_override.json` provisionally selects `user-gold-lip-mask-gray-20260625-163204` + `gold_exact_binary` for the next buildless step only, without claiming human review. A new M1 package was generated at `evidence/e7-lip-m1-packages/m1-lip-auto-selected-20260625T215327Z/` with Apple Vision, face parsing, color-gradient, fusion, UV round-trip, audit, and provisional addendum artifacts; it remains `m1Decision=partial`, `phase2Status=partial`, `phase3ExecutionStatus=partial`, `boundaryQualityProof=false`, `coordinateSpaceAuditStatus=pending`, `visibilityConfidence=partial`, and `heldOutEvalPlanStatus=missing`. Provisional user-adjustment artifacts under `evidence/e7-lip-user-adjustment-provisional/adjustment-20260625T215720Z/` selected `ua-07-vertical-up-015` (`verticalOffset=-0.15`) as an automated suggestion only; it is not `user_confirmed`. M1 gate and queue artifacts now exist under the auto-selected package (`m1_gate_matrix.json`, `m1_gate_matrix.md`, `next_automation_queue.json`) with 38 gates counted as `auto_provisional=3`, `partial=15`, `blocked_by_missing_capture=8`, `blocked_by_build_gate=8`, `deferred=4`. M3A draft prep artifacts exist under `evidence/e7-lip-runtime-sweep-prep/prep-20260625T215807Z/`; RN `npm test -- --runInBand --watchman=false`, `tsc --noEmit`, and `npm run lint` passed, but all runtime candidates are `runtimeReady=false` / `blocked_by_m1_m2`. Reference review pack, coordinate audit, and missing-scenario capture manifest were produced under `evidence/e7-lip-reference-review-pack/review-20260625T220546Z/`, `evidence/e7-lip-coordinate-audit/audit-20260625T220638Z/`, and `evidence/e7-lip-missing-scenario-captures/manifest-20260625T220600Z/`. The coordinate audit is still partial because `coordinateSpaceValidated=false`, named `clipW` or documented clip-space semantics are missing, front-most/depth/per-triangle visibility fields are missing, and `blendShapes.available=false`. No UnityFramework/Xcode/iPhone build, app install, device capture, runtime evidence, M1 ready claim, E7.3 Green, or Lip G/Y/R decision was made.
- E7 M3A buildless runtime candidate source-stage and log-analysis setup is implemented for validation only. `scripts/e7_lip_runtime_sweep/prepare_validation_runtime_candidates.py` projects the provisional M1 package (`evidence/e7-lip-m1-packages/m1-lip-auto-selected-20260625T215327Z/`) onto `pair_face_20260622T143334Z_03`, writes `evidence/e7-lip-runtime-sweep-prep/validation-candidates-20260626Tsetup/runtime_candidate_registry.json`, and source-stages three Unity Resource masks: `e7-lip-validation-tight-auto-v0`, `e7-lip-validation-tight-user-v0`, and `e7-lip-validation-safe-v0`. The same run folder now also contains `input_readiness.json`, `adjustment_controls_contract.json`, `sweep_evidence_schema.json`, `runtime_sweep_checklist.md`, `prior_evidence_summary.md`, `static_verification.md`, and `summary.md` for M3B handoff. The registry records `userAdjustmentContractFields`, candidate-level `userAdjustmentParams`, `userAdjustmentStatus`, `userAdjustmentAppliedToMask=false`, `userAdjustmentRuntimeShaderProbe=true`, and `runtimeReady=false`; `lip-tight-user-v0` and `lip-safe-v0` point to the auto-provisional `ua-07-vertical-up-015` params (`verticalOffset=-0.15`) but not to real `user_confirmed` evidence. RN Compact HUD can choose `base`, `auto`, `user`, and `safe` candidates while preserving `lookId` as the lip sample ID, and it can probe/log `cornerReach`, `upperLipTightness`, `lowerLipTightness`, and `verticalOffset`; Unity shader applies those fields as a lightweight validation-only sampling/threshold probe, while the baked mask texture itself is still not regenerated live. Unity `RNBridge` and `E3RegionMaskOverlay` now consume/log candidate IDs, mask fields, and the four adjustment fields; `E3RegionMaskOverlay` selects the recipe mask texture/threshold/feather for material sampling and passes the adjustment floats into shader properties. `scripts/e7_lip_runtime_sweep/analyze_runtime_sweep_log.py` now checks a captured full runtime log against the candidate registry and emits JSON/Markdown wiring evidence summaries without making Lip G/Y/R or readiness claims. Verification passed: candidate prep script run with `--run-id validation-candidates-20260626Tsetup`, `python3 -m py_compile scripts/e7_lip_runtime_sweep/prepare_validation_runtime_candidates.py scripts/e7_lip_runtime_sweep/analyze_runtime_sweep_log.py`, JSON registry/contract lint, synthetic-log analyzer smoke returned `wiring_ready`, RN `npm test -- --runInBand --watchman=false`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`, `git diff --check`, and Unity batchmode import/compile logs (`evidence/logs/e7-lip-runtime-candidate-unity-import-20260626.log`, `evidence/logs/e7-lip-user-adjustment-runtime-wiring-unity-import-20260626.log`, `evidence/logs/e7-lip-final-buildless-setup-unity-import-20260626.log`, `evidence/logs/e7-lip-runtime-adjustment-shader-probe-unity-import-20260626.log`) with no C# compile errors. Build-gated evidence now exists: `scripts/build_m3_unityframework.sh` regenerated/synced `UnityFramework.framework` successfully with `evidence/logs/m3-repro-unity-export-e7-lip-runtime-sweep-20260626-165445.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-lip-runtime-sweep-20260626-165445.log`, and `evidence/logs/m3-repro-artifact-verification-e7-lip-runtime-sweep-20260626-165445.log`; direct Xcode real-device fallback built `MakeupARValidation.app` after RN CLI failed on deleted simulators (`evidence/logs/e7-lip-runtime-sweep-rn-ios-run-20260626-165445.log`, `evidence/logs/e7-lip-runtime-sweep-rn-ios-xcodebuild-direct-20260626-165445.log`); `devicectl` installed and launched bundle `com.makeupar.rnvalidation` on `위승철의 iPhone` (`evidence/logs/e7-lip-runtime-sweep-rn-ios-install-20260626-165445.log`, `evidence/logs/e7-lip-runtime-sweep-rn-ios-launch-20260626-165445.log`). Limitations are explicit: `lip-tight-auto-v0` and `lip-tight-user-v0` currently produce the same UV asset because no retained `user_confirmed` adjustment has been applied to the mask, every candidate remains `runtimeReady=false` / `validation_only_m1_m2_partial` until captured runtime sweep logs and visual evidence prove otherwise, and no ARFace runtime sampling analysis, visual evidence, M1/M2-ready claim, E7.3 Green, or Lip G/Y/R decision was made.
- E7 lip candidate generator fast experiment is complete for buildless review only. `scripts/e7_lip_candidate_generator/build_lip_boundary_candidates.py` generated the five planned candidates (`parsing_curve_smooth`, `vision_curve_fill`, `vision_color_snap`, `hybrid_curve_safe`, `hybrid_curve_balanced`) from the clean female sample plus Apple Vision, face parsing, color/gradient, and ARFace export inputs. The final run is `evidence/e7-lip-candidate-generator/experiment-20260626Tmediapipe-v4/`, with hard masks, soft alpha masks, overlays, trace JSON, UV preview images, contact sheets, comparison table, loop notes, next camera checklist, and `candidate_registry_draft.json`. MediaPipe was verified as importable in `.venv` and the Face Landmarker model exists, but the first comparison run failed in the Codex shell during `FaceLandmarker` native graph execution (`exitCode=-6`, `gl_context_nsgl` pixel format failure, `graph_service.h:139`, `DrishtiMetalHelper` stack). An isolated MediaPipe-only retry in the same Codex shell under `evidence/e7-lip-candidate-generator/mediapipe-retry-20260626T152244Z/` failed the same way for four variants, proving the issue was not obvious path/model/input corruption. The same `.venv` and same retry script then succeeded when launched through the macOS Terminal app; successful artifacts are under `evidence/e7-lip-candidate-generator/mediapipe-gui-terminal-20260626T154247Z/` with `faceCount=1`, `landmarkCount=478`, `positivePixels=15154`, `mediapipe_landmark_overlay.png`, `mediapipe_lip_curve_overlay.png`, and `mediapipe_lip_curve_mask.png`. The narrowed blocker is therefore the non-GUI Codex shell's ability to create the GL/Metal context required by MediaPipe FaceLandmarker; GUI Terminal is a valid manual comparison path, but not yet a reliable automated buildless step. This does not block the five core candidates. No upload, package install, Unity/Xcode/iPhone build, new capture, runtime-ready claim, M1-ready claim, E7.3 Green, or Lip G/Y/R decision was made.
- E7 MediaPipe candidate evaluation is complete for buildless review only. `scripts/e7_lip_candidate_generator/evaluate_mediapipe_candidate.py` compared the successful GUI Terminal MediaPipe mask against the existing five core candidates without rerunning or changing the core generator. Evaluation artifacts live under `evidence/e7-lip-candidate-generator/mediapipe-evaluation-20260626T160852Z/` and include `input_inventory.json`, `mediapipe_vs_core_metrics.json`, `mediapipe_vs_core_metrics.md`, `mediapipe_vs_core_contact_sheet.png`, `mediapipe_lip_crop_contact_sheet.png`, `mediapipe_visual_review.md`, `mediapipe_integration_recommendation.md`, `mediapipe_environment_caveat.md`, and `summary.md`. With the same gold exact binary reference, MediaPipe scored gold IoU `0.856425`, precision `0.989970`, recall `0.863922`, upper recall `0.807881`, and lower recall `0.936011`; the best core candidate remains `hybrid_curve_safe` with gold IoU `0.906866`. Decision is `use_as_helper_signal`: MediaPipe is useful as a geometry prior / centerline-inner-mouth hint / smoothing and sanity-check signal, but it should not become a standalone 6th candidate yet. GUI Terminal manual execution remains acceptable for this comparison result, but MediaPipe still must not be a required Codex-shell automation gate. No core candidate regeneration, upload, new capture, Unity/Xcode/iPhone build, runtime-ready claim, M1-ready claim, E7.3 Green, or Lip G/Y/R decision was made.
- E7 local CV tooling setup is complete for buildless lip-candidate review work. The existing `.venv` now has reproducible pins in `requirements-cv.txt`: `opencv-python-headless==4.13.0.92` and `scikit-image==0.26.0`. `scripts/e7_lip_candidate_generator/check_cv_dependencies.py` smoke-verifies OpenCV contour extraction and scikit-image morphology/contour extraction without reading evidence files; verification passed with Python `3.13.2`, OpenCV module `4.13.0`, scikit-image `0.26.0`, and numpy `2.5.0`. `build_lip_boundary_candidates.py` now records package versions in `input_signal_report.json` and avoids importing MediaPipe during generic package-status collection. This is tooling setup only: no upload, new capture, Unity/Xcode/iPhone build, runtime evidence, M1-ready claim, E7.3 Green, or Lip G/Y/R decision was made.
- Phase 2 adds the runtime-selectable `e7-arface-authored-atlas` candidate for `lip`, `cheek`, and `eye`. This is validation-only manual labeling plus evidence metadata, not face parsing, product segmentation, product-quality makeup, or AI/backend work.
- E7.03 v2.1 now has phase-wide time-saving rules, not only a P2 shortcut: prefer no-build data/registry/offline changes first, JS-only validation second, one real-device install for many candidate/variant/region sweeps third, and UnityFramework/Xcode rebuild only when renderer logic, schema, shader/material contract, package/framework sync, or native integration changes.
- E7.3 visual evidence has been reviewed from the user-provided screen recording. ARFace mesh/UV is live on-device, but the current procedural candidate is not visually acceptable for `lip` or `eye`; `cheek` lacks a clean E7 candidate sample. Overall E7.3 remains Yellow because the ARFace substrate is viable, but the current candidate masks block E7.4.
- The old E7.03 Green bar of "better than E3/E4 baseline" or "demo-plausible" is superseded. E7.03 Green now requires Q3 overlay-ready validation for `lip`, `cheek`, and `eye`, with runtime/visual evidence and region-separated scoring.
- Conservative alternative: run additional M7 re-entry verification if the team wants to replace the accepted M7 Yellow lifecycle risk with formal 3-cycle evidence.
- Renderer path: if the accepted M7 risk remains acceptable, stay in validation/hardening mode inside E7.3; do not enter E7.4/E7.5/E7.6 until E7.03 v2.1 evidence supports the next boundary, or until the team explicitly accepts the remaining E7.3 risk.
- Workspace/share state: source-first cleanup is complete for the current handoff pass. Current checked size is `24M` total (`.git` about `22M`; source/doc/script working tree about `2M`). Root active docs are limited to `AGENTS.md`, `TECH_VALIDATION_TEST_PLAN.md`, and `TECH_VALIDATION_RESULT.md`; `docs/roadmaps/active/` now keeps only the current lip candidate generator fast-spike plan, while older E7 plans live under `docs/roadmaps/archive/`; team onboarding/share requirements live in `docs/runbooks/TEAM_SHARE_REQUIREMENTS_KO.md`; ignored evidence/build/dependency/editor outputs are expected to be absent after `scripts/cleanup_local_generated.sh --profile share`.

Current status:

- M0-M6: Green.
- M7: Yellow / skipped by decision / risk accepted. Do not promote M7 without formal 3-cycle re-entry evidence.
- M8 foundation closeout: Yellow overall because M7 remains risk-accepted.
- E1 AR Alignment: Green for validation.
- E2 Trackable Lifecycle Diagnostics: Green.
- E3 Region Mask: Green for validation only; masks are broad debug masks.
- E4 Texture Sample: Green for validation only; texture samples are procedural/debug quality.
- E5 AI Feature Readiness Snapshot: Green for no-inference handoff; decision recording was explicitly waived only for E5.
- E6 Engine Decision: Yellow; continue the RN + Unity + ARKit direction, but do not claim product-v1 readiness.
- E7.0/E7.1 Preflight: Green for preflight only.
- E7.2 Baseline Instrumentation: Green for baseline instrumentation; decision recording was explicitly waived only for E7.2.
- E7.3 Region Precision: Yellow overall / blocked from E7.4. Legacy procedural E7 video review remains `lip` Red, `cheek` Yellow / insufficient E7-candidate sample, and `eye` Red. P8 reference UV runtime sweep improves attachment for all three regions, but current P8 region decisions are `lip` Yellow, `cheek` Yellow, and `eye` Yellow pending silhouette/material cleanup and scenario-specific motion review.
- E7.03 P2 synchronized reference capture: captured / ready for user gold-mask authoring input; this does not change E7.3 region G/Y/R or mark E7.03 Green.
- E7.03 A/B UV atlas fast gate: Continue / P8 Build Gate already consumed for the first runtime sweep. `lip`, `cheek`, and `eye` all showed useful same-frame and cross-frame UV transfer; this remains offline viability evidence, not runtime Green evidence.
- E7.03 P8 runtime sweep: build/install/runtime-log/representative-photo evidence collected after Build Gate approval. The runtime path is viable, Compact HUD is visible, and that historical P8 build used Clean as a mask-hidden reference view, but no region is Green yet. Current Lip Sample Pack v0 changes Clean to preserve the makeup overlay while hiding RN UI.
- E7.03 smooth-mask validation app cleanup: accepted for the current validation app optimization goal. The accepted screenshots show a usable, non-obstructed Compact HUD and a natural-enough visible overlay for this validation step. User feedback records that the overall makeup is still subtle/hard to see and the eye boundary needs improvement, but this is acceptable for now and should be resolved with cosmetic expression/modeling. Full E7.3 Region Precision remains Yellow because Q3 overlay-ready motion/scenario evidence is still incomplete.
- E7 Lip Sample Pack v0: implementation/static checks, real-device build/install/launch, and user-provided runtime video review are complete for `lip_daily`, `lip_gloss`, and `lip_texture`. Decision is `continue with revise`: preserve the wiring/build path, but revise HUD/slider, lip boundary model, and renderer structure before adding more samples. This is conditional exploration only and does not promote E7.3, E7.4, E7.5, full E7, or product readiness.
- E7 lip-first pre-AR boundary calibration Phase 0: complete for documentation/routing only.
- E7 lip-first pre-AR boundary calibration Phase 1: Phase 1 calibration flow contract complete.
- E7 lip-first pre-AR boundary calibration Phase 2: Phase 2 Boundary Fusion ready for contract/stub handoff.
- E7 M1 Buildless Personalized Lip Package v0: partial / honestly bounded. Audit, reproducible polygon-mask repair, fusion summary, and one-frame UV projection artifacts exist under `evidence/e7-lip-m1-packages/m1-lip-repair-20260625T120000Z/`, but the prior manual polygon lip marking is explicitly rejected by the user as wrong and must not be reused as gold/reference/candidate seed. The separate user-painted gold raw reference set is available at `evidence/references/e7-user-gold-raw-20260626/` and may be used for offline reference, candidate generation, candidate scoring, and mask derivation experiments. Apple Vision contour extraction is implemented and smoke-verified under `evidence/e7-lip-m1-packages/m1-lip-apple-vision-20260625T132216Z/`. Local/offline face parsing is integrated under `evidence/e7-lip-m1-packages/m1-lip-face-parsing-20260625T2255Z/`, but any fused masks derived from the rejected manual marking are invalid for reference use. Color/gradient confidence is implemented under `evidence/e7-lip-m1-packages/m1-lip-color-gradient-20260625T000000Z/` with `colorGradientComputed=1`, but it records `lowContrastWarning=true`. User adjustment tooling is implemented and smoke-verified with a temporary valid review: no-review audit keeps `userAdjustment_required_not_confirmed`, valid-review smoke removes user-adjustment failures while overall M1 remains `partial`. Failure-mode classification is deferred to future eval after the engine boundary reaches an initial useful level, so it no longer blocks M1. M1 is not ready because the remaining required gates are missing or partial: coordinate-paired user gold reference selection, real retained user-confirmed adjustment evidence, pucker capture, blendshape/face-state, expression captures, coordinate-space audit, inner-mouth/corner/upper-lower acceptance, triangle visibility/front-most handling, and held-out/eval frame plan.
- E7 M3A Buildless Runtime Sweep Prep: source-staged / partial. The validation-only runtime candidate registry and Unity Resource masks exist for `lip-tight-auto-v0`, `lip-tight-user-v0`, and `lip-safe-v0`; RN/Unity schema/logging can carry candidate IDs, mask parameters, and the four user-adjustment probe fields. This is not M3B: no device runtime evidence, Lip G/Y/R, or runtime-ready claim exists.
- E7 Lip Candidate Generator Fast Experiment: buildless review artifacts generated under `evidence/e7-lip-candidate-generator/experiment-20260626Tmediapipe-v4/`. The five core candidates exist with masks/alpha/overlays/traces/UV previews/contact sheets. MediaPipe failed in the Codex shell but succeeded from the macOS Terminal app with the same `.venv`; successful comparison artifacts live under `evidence/e7-lip-candidate-generator/mediapipe-gui-terminal-20260626T154247Z/`. This is not M1 ready, runtime ready, E7.3 Green, or Lip G/Y/R.
- E7 Full Face Region Generate complete implementation plan is now the active execution contract. It expands the prior lip-only Generate flow to `lip`, `blush`, `brow`, and `eyeliner`: CLI/local experiment loops first, external mask prior as silver draft only, face parsing/color confidence as helper/evaluation signals, web app as a short pre-build UI/logic verification shell, saved mask package before AR entry, RN/Unity runtime consumption setup, and pre-Xcode build gate package. This session is phone-disconnected: Xcode/iPhone build/install/launch and final real-device evidence are deferred. Eyeliner is mandatory and must produce at least one candidate through reference/photo/prior/parametric fallback if the normal path is weak. This is a documentation/routing update only; it does not claim runtime ready, E7.3 Green, product-quality-ready, or any region G/Y/R.
- E7 Full Face Region Generate implementation gate is complete for pre-Xcode only. `scripts/e7_region_generate/build_region_candidates.py` generated four selected region packages and `composite_apply_payload.json`; `web/lip-generate-beta` now has a full-face region review/adjust/save shell; RN `App.tsx` can post a four-layer `e7_full_face_region_generate_v0` payload; Unity `RNBridge` and `E3RegionMaskOverlay` accept `lip`, `blush`, `brow`, and `eyeliner`; Unity Resources contain `e7-lip-balanced-uv-v0`, `e7-blush-balanced-uv-v0`, `e7-brow-balanced-uv-v0`, and `e7-eyeliner-minimal-safe-uv-v0`; `E7FullFaceRegionPackageSmoke` passed in Unity batchmode after closing the prior project-locking Unity processes. The committed gate package is `evidence/e7-region-generate/session-20260626T195853Z/pre_xcode_gate.md`. All four regions are `pre-xcode-ready`, not Green. The next gate is the user-approved Xcode/iPhone build and real-device evidence pass.
- E7 Personalized Lip Generate Phase A-C buildless/web beta gate and RN/Unity source-stage are now `partial`. Shared TypeScript contracts, fixture inventory, local-only lip Generate server, and the React/Vite web beta exist. The local server smoke generated real Vision and MediaPipe fixture masks, ARFace UV textures, round-trip overlays, and personalized package JSON under ignored `evidence/e7-lip-generate-server/`; smoke status is `partial` with both providers reporting `uvMaskReady=true` and `roundTripReady=true`, while `runtimeApplyReady=false`. The web beta was exercised in a real browser against the local HTTP server at `http://127.0.0.1:8791`: Generate produced 2D mask / UV texture / round-trip previews, 4-Way Compare produced Vision+Off, Vision+Blendshape Assist, MediaPipe+Off, and MediaPipe+Blendshape Assist cards, and adjustment controls propagated through request/package/runtime payloads. The web beta UI was revised from developer-console wording to a step-based flow with Korean labels for sample selection, mask generation, 4-way comparison, validation checks, and collapsed Debug payload; curated browser evidence is stored at `evidence/screenshots/e7-lip-generate-web-beta-2026-06-27/`. Runtime payloads now keep PNG previews for browser inspection but use local-only `raw_rgba_base64` plus texture dimensions for Unity dynamic texture ingestion, avoiding an ImageConversionModule dependency. Build-before-device hardening also added runtime privacy flags (`localOnly=true`, `offDeviceUpload=false`, `longTermRawFrameStored=false`) through the RN/Unity apply path, sanitized Unity failure logs so raw/base64 payloads are not printed, marks generated-mask apply events `blocked` when no UV-backed runtime texture is actually applied, restricts the local artifact endpoint to fixture/run artifact roots, narrows CORS to the web beta origin, and redacts future base64/raw-frame-like request fields before writing `request.json`. RN source-stage adds the in-app Generate HUD with Provider, Blendshape Assist Off/On, and adjustment controls; its current generated package is still synthetic/buildless and remains `runtimeReady=false`. Unity source-stage adds generated-mask JSON ingestion, local raw RGBA texture registration, and reuse of the existing smooth-region-mask apply path; the prior Unity Safe Mode compile errors were cleared in the Editor after the raw RGBA change. A new Editor-only smoke at `Makeup AR Validation/Run E7 Generated Lip Mask Smoke` verified raw RGBA texture registration, RNBridge generated-mask payload parsing, blocked no-face application status, and sanitized malformed-payload failure logging; local log snippet is stored at `evidence/logs/e7-generated-lip-mask-editor-smoke-20260627.log`. Device runtime application is still not verified. Verification passed for web build/typecheck/lint, shared-core typecheck/tests, RN Jest/TypeScript/lint, Python py_compile, local server smoke, local artifact deny/allow checks, Unity Editor GUI compile/Safe Mode exit, and Unity Editor generated-mask smoke. This proves the buildless web gate can exercise mask generation and 2D->UV->screen round-trip, and that RN/Unity source wiring can parse/register generated raw mask payloads in Editor, but it is not RN/Unity device runtime dynamic mask proof, not on-device Vision provider proof, not MediaPipe native integration proof, not E7.3 Green, and not Lip G/Y/R.
- E7 Personalized Lip Generate web beta user-review repair is buildless/browser verified and remains `partial`. Vision no longer uses a straight polygon/source-mask copy path for the beta server; it now generates `generationMethod=vision_curve_fill` by smoothing Apple Vision contour points into a closed curve without OpenCV/scikit-image correction. MediaPipe also regenerates from landmark points instead of copying a fixed mask. Adjustment values now feed the real 2D mask generation and therefore the UV texture and round-trip overlay; local smoke measured non-zero mask deltas after adjustment (`Vision=6763` pixels, `MediaPipe=8275` pixels). The React beta now updates the mask-on-face photo preview immediately from the current slider state using the same boundary adjustment formula, and stale save no longer dead-ends the flow: pressing save regenerates the latest local-only package first, then saves it through `POST /api/lip-mask/save`. Browser verification confirmed the preview SVG path changed immediately after `cornerReach=0.35`, and the saved record stored `cornerReach=0.35`. The active plan now supersedes the earlier bottom paged HUD assumption: the future iPhone app must use a capture-first forced wizard, must preview the app UI as a functionless web shell before RN migration, must use current-frame native Vision/MediaPipe providers rather than fixture replay, must support n-shot capture and blendshape assist off/on comparison, and must keep debug/log surfaces out of the face judgment area. This is still not iPhone runtime dynamic-mask proof, not on-device Vision proof, not MediaPipe native proof, not E7.3 Green, and not Lip G/Y/R.
- E7 in-app Generate forced wizard/native Vision pre-Xcode implementation is buildless/source-stage verified and remains `partial`, not "Xcode build only" complete. The web beta now opens with a functionless iPhone app wizard shell and keeps the functional server beta available for real Generate/adjust/save verification. RN `App.tsx` now defaults to a capture-first forced wizard (`Start -> Align -> Capture -> Extract -> Compare -> Adjust -> Save -> Runtime`) instead of the old validation HUD; later steps stay locked until prior gates are reached, n-shot capture buttons send `CaptureE7ReferenceFrameJson` with `captureSetId` / `captureShotKind`, adjustment changes mark generated candidates stale, save is blocked until regenerate, and debug is behind a small drawer rather than the face-center HUD. A new JS projection helper builds a raw RGBA UV mask package from native boundary + ARFace screen/UV export and sends the existing Unity `ApplyGeneratedLipMaskJson` payload after local save. iOS source now includes `E7NativeLipBoundaryProviders`: native Vision reads the current capture `frame.png` plus `arface_export.json` from app Documents, extracts Vision lip landmarks, returns ARFace export/blendshape data to RN, and saves generated package records locally. MediaPipe is intentionally not faked: the native provider adapter returns `blockedReason=mediapipe_ios_dependency_or_task_model_not_bundled` until a local iOS MediaPipe dependency/model is added. Verification passed: RN TypeScript, RN Jest forced-wizard tests, RN ESLint, web lint/typecheck/build, shared lip-generate-core typecheck/tests, local server smoke (`Vision adjusted delta=6763`, `MediaPipe adjusted delta=8275`, both UV/round-trip/save partial), `plutil -lint` for the Xcode project, `ruby -c ios/Podfile`, and Unity `E7GeneratedLipMaskSmoke.RunFromCommandLine` with log `evidence/logs/e7-generated-lip-mask-rn-wizard-unity-smoke-20260627.log`. This is not Xcode/iPhone build evidence, not real on-device Vision runtime evidence, not native MediaPipe completion, not product-quality-ready, and not Lip G/Y/R. The next blocker is choosing and bundling a local iOS MediaPipe provider/model or explicitly accepting Vision-only build scope before the Xcode gate.
- E7 in-app Generate native MediaPipe pre-Xcode unblock is now `pre-xcode-ready`, still not product-ready. The previous native MediaPipe blocker was removed by adding `MediaPipeTasksVision` / `MediaPipeTasksCommon` `0.10.35` through CocoaPods, running `pod install`, bundling the local `face_landmarker.task` model as an iOS app resource, and replacing the blocked adapter with a conditional native current-frame Face Landmarker path. The provider reads the captured `frame.png` and `arface_export.json`, runs MediaPipe in image mode, maps the standard MediaPipe outer/inner lip landmark indices into `frame_image_pixel_top_left`, returns ARFace export/blendshape metadata, and keeps a truthful blocked fallback only if the module/resource is missing. Unity capture export now preserves `captureSetId`, `captureShotKind`, requested purpose, and a `quality` summary in `arface_export.json`, `capture_summary.json`, and RN capture events, so n-shot calibration can be traced through native extraction and saved packages. Generated packages now include `captureSetId`, `providerResults`, `blendshapeAssist`, `adjustment`, and local-only privacy flags; Unity runtime payloads also carry `captureSetId`. Verification passed: `pod install`, `xcodebuild -workspace MakeupARValidation.xcworkspace -scheme MakeupARValidation -showBuildSettings` only, `plutil -lint` project, `ruby -c ios/Podfile`, RN TypeScript/Jest/lint, web lint/typecheck/build, shared lip-generate-core typecheck/tests, Python py_compile, local server smoke (`Vision adjusted delta=6763`, `MediaPipe adjusted delta=8275`, both UV/round-trip/save partial), saved package metadata inspection, `git diff --check`, and Unity `E7GeneratedLipMaskSmoke.RunFromCommandLine` with logs `evidence/logs/e7-generated-lip-mask-rn-wizard-mediapipe-prexcode-20260627.log` and `evidence/logs/e7-generated-lip-mask-rn-wizard-capture-contract-prexcode-20260627.log`. Xcode build/install/launch was intentionally not run; therefore real iPhone native Vision/MediaPipe execution, camera permission, face attachment, latency/FPS/memory/thermal, and human visual acceptance remain the next gate. No upload, long-term raw frame storage, Android, commercial SDK, server generation, live face parsing/Core ML runtime, Green/product-quality-ready claim, or Lip G/Y/R decision was made. The next required action is user-approved Xcode build/install/run.
- E7 in-app Generate UX correction is buildless/source verified after user review. The web app shell now models the intended product flow more clearly: extraction chooses exactly one provider (`Vision` or `MediaPipe`), the compare step shows multiple candidates derived from the selected provider, every post-start step has a back action, and after capture the shell switches from live-camera language to captured-frame review. The adjust step now prioritizes a large mask-on-face preview in the phone mock instead of a small thumbnail. RN `App.tsx` now mirrors the provider-selection contract by generating candidates only for the selected provider, adds a wizard back button, hides the live Unity camera view behind a captured-frame review shield after the capture step, and prepares a large adjustment preview slot using `framePreviewUri` returned by the native provider. Native iOS now returns `framePreviewUri=file://.../frame.png` for Vision/MediaPipe provider results. Adjustment is no longer delayed or save-blocked in the RN source path: when a lip adjustment changes, RN rebuilds the selected provider's candidates from the already-extracted native boundary and ARFace export, applies the same lip-boundary adjustment formula before UV projection, clears stale state, and keeps save enabled for the current package. Verification passed: web typecheck/lint/build, real-browser preview/save check, local server smoke, shared-core typecheck/tests, RN TypeScript/Jest/lint, and `git diff --check`. This still does not prove the iPhone actually displays the captured image or generated mask preview; that remains part of the Xcode/iPhone runtime gate.
- Full E7 visual product-readiness: incomplete.

Active roadmap doc:

- `docs/roadmaps/active/E7_FULL_FACE_REGION_GENERATE_COMPLETE_IMPLEMENTATION_PLAN_KO.md`: active full face region Generate plan plus the 2026-06-27 capture-first app-flow addendum and section 13.8 real-device review fix plan. Use for the current work to generate, compare, adjust, save, and prepare `lip`, `blush`, `brow`, and `eyeliner` masks through web app shell approval, functional web beta logic verification, RN forced wizard migration, Unity capture bundle export, native current-frame Vision/MediaPipe provider integration, saved package consumption, synthetic/buildless checks, and real-device fix gates. It remains local-only/no-upload. The old "Xcode build only remains" prompt is superseded until 13.8/17.3 fixes pass; do not run a new Xcode/iPhone build/install/launch without user approval and a clear fix-build gate.
- Archived roadmap docs: `docs/roadmaps/archive/` now holds older E7 master, region precision, reference UV atlas, lip boundary calibration, lip sample review, performance, cosmetic-rendering, external-prior, and AR-engine background plans. Open them only when that lane is explicitly reopened.
- Deferred docs to create only when needed: `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` before E7.4, and `E7_DEMO_LOOK_RECIPE_SPEC.md` only if recipe values or preset behavior need a fixed contract.

Default reading route:

- Always read `AGENTS.md` and this snapshot.
- Read `TECH_VALIDATION_TEST_PLAN.md` only when changing the stable validation contract or checking milestone/evidence rules.
- For E7.3 region precision, reopen `docs/roadmaps/archive/E7_VISUAL_PRODUCT_READINESS_SPIKE_PLAN.md` / `docs/roadmaps/archive/E7_REGION_PRECISION_SUBSPIKE_PLAN.md` only if needed, then read the `docs/roadmaps/research/E7_AXIS1_*` reports.
- For E7.03 boundary work outside the current lip-first lane, reopen `docs/roadmaps/archive/E7_REGION_PRECISION_SUBSPIKE_PLAN.md` only if the Q3/E7.3 parent boundary needs re-checking.
- For the current full face region Generate implementation, read `docs/roadmaps/active/E7_FULL_FACE_REGION_GENERATE_COMPLETE_IMPLEMENTATION_PLAN_KO.md`.
- For lip-first pre-AR boundary calibration, reopen `docs/roadmaps/archive/E7_LIP_BOUNDARY_PRE_AR_CALIBRATION_SPIKE_PLAN_KO.md` only if that lane is explicitly needed; keep heavy analysis in calibration/offline and keep AR runtime on lightweight ARFace UV/vertex tracking.
- For reference-driven UV atlas work before gold mask authoring, reopen `docs/roadmaps/archive/E7_REFERENCE_DRIVEN_UV_ATLAS_PLAN_KO.md` only when that lane is explicitly needed.
- For team-facing E7.4/E7.5 cosmetic rendering scope review, reopen `docs/roadmaps/archive/E7_COSMETIC_RENDERING_TEAM_CHECK_PLAN_KO.md`; for implementation planning, create/update `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` and read the `docs/roadmaps/research/E7_AXIS2_*` reports.
- For E7.6 performance, reopen `docs/roadmaps/archive/E7_PERFORMANCE_EVIDENCE_SUBSPIKE_PLAN.md`.
- Read base research or benchmark reports only for fallback, SDK comparison, licensing, or architecture decisions.

Stop rules:

- Do not mark M7 Green unless formal 3-cycle re-entry evidence is collected.
- Do not proceed to E7.4 or E7.6 from E7.2 logs or E7.3 build/install logs alone.
- Do not mark any E7.3 region Green without real-device runtime compare logs and representative visual evidence for that region.
- Do not mark E7.03 Green under the old "baseline-better" or "demo-plausible" standard. Green now requires Q3 overlay-ready validation for all three in-scope regions.
- Do not treat face parsing as E7.03 live runtime or product/release asset source, do not treat ML Kit Face Mesh as an iPhone runtime candidate, and do not introduce a separate camera session that conflicts with ARKit.
- Do not claim full E7, visual product readiness, product-v1 readiness, or product-quality makeup from E7.0/E7.1 preflight or E7.2 instrumentation logs.
- Do not treat E3 as product-quality makeup accuracy; it proves validation-level region independence only.
- Do not treat E4 as product-quality makeup fidelity; it proves validation/debug texture sample dispatch and visual distinction only.
- Do not treat E5 or E7.2 recording waivers as general waivers for future milestones.
- Do not start AI model inference, backend upload, recommendation logic, commercial SDK integration, Android work, raw camera frame storage, or product implementation until a plan explicitly reaches that scope.

Evidence media policy:

- Do not save screen recordings by default.
- Save a recording only when motion, elapsed time, or a continuous scenario is the core evidence.
- When a recording is captured, prefer long-term retention of metadata, contact sheets, and representative frames. The raw recording may be deleted after decision review or extraction unless a milestone explicitly requires keeping it.

Key evidence:

- E7.3 region precision implementation/build/video review: `evidence/logs/m3-repro-unity-export-e7-region-precision-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-region-precision-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-region-precision-2026-06-22.log`, `evidence/logs/e7-region-precision-rn-ios-run-default-candidate-2026-06-22.log`, `evidence/logs/e7-region-precision-summary-2026-06-22.md`, `evidence/logs/e7-region-precision-video-analysis-2026-06-22.md`, `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/contact-sheet-1fps.jpg`.
- E7.03 Phase 1 UI/evidence hygiene real-device check: `evidence/logs/m3-repro-unity-export-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/m3-repro-artifact-verification-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-rn-ios-run-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-runtime-console-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-capture-tooling-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-display-info-2026-06-22-141533.log`, `evidence/screenshots/e7-phase1-ui-display-info-2026-06-22-141533.json`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/clean-view.jpg`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/compact-hud.jpg`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/full-debug.jpg`.
- E7.03 Phase 2 ARFace authored atlas MVP build/install check: `evidence/logs/m3-repro-unity-export-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/m3-repro-artifact-verification-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/e7-phase2-atlas-2026-06-22-151537-rn-ios-device.log`.
- E7.03 P2 synchronized reference capture: `evidence/logs/m3-repro-unity-export-2026-06-22-230402.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-2026-06-22-230402.log`, `evidence/logs/m3-repro-artifact-verification-2026-06-22-230402.log`, `evidence/logs/e7-reference-capture-rn-ios-run-2026-06-22-233049.log`, `evidence/logs/e7-reference-capture-runtime-console-2026-06-22-233202.log`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143300Z_01/`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143305Z_02/`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143334Z_03/`.
- E7.03 A/B UV atlas fast gate: `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/summary.md`, `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/summary.json`, plus per-region `round_trip/`, `cross_frame/`, `uv_overlap/`, `candidate_preview/`, and `atlases/` artifacts. Earlier fast-gate runs are superseded because they used sparse sampling or were debug/smoke runs.
- E7.03 P8 runtime sweep implementation prep: frozen Unity Resources under `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/` include 9 probability PNGs, 9 PNG-byte Resources, and `runtime_candidates.json`. Static checks passed: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check`. This is implementation/static evidence only, not real-device runtime evidence.
- E7.2 build/runtime: `evidence/logs/m3-repro-unity-export-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/e7-baseline-rn-ios-run-ack-2026-06-22.log`, `evidence/logs/e7-baseline-runtime-console-ack-2026-06-22.log`, `evidence/logs/e7-baseline-summary-2026-06-22.md`, `evidence/screenshots/e7-baseline-status-panel-2026-06-22.jpg`.
- E7.0/E7.1 preflight: `evidence/logs/e7-unity-process-cleanup-2026-06-22.log`, `evidence/logs/m3-repro-unity-export-e7-preflight-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-preflight-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-preflight-2026-06-22.log`, `evidence/logs/e7-build-install-run-2026-06-22.log`, `evidence/logs/e7-runtime-event-preflight-2026-06-22.log`.
- E5 no-inference snapshot: `evidence/logs/e5-ai-feature-readiness-runtime-2026-06-21.log`, `evidence/screenshots/e5-ai-feature-readiness-rn-status-2026-06-21.jpg`.
- E4 texture samples: `evidence/logs/e4-texture-samples-runtime-2026-06-21.log`, `evidence/screen-recordings/e4-texture-samples-matte-shimmer-blush-2026-06-21.mp4`, `evidence/screenshots/e4-texture-samples-comparison-2026-06-21.jpg`.
- E3 region mask: `evidence/logs/e3-region-mask-layer-dispatch-2026-06-21.log`, `evidence/screen-recordings/e3-region-mask-lip-cheek-eye-2026-06-21.mp4`, `evidence/screenshots/e3-region-mask-debug-colors-2026-06-21.jpg`.
- M6 Unity -> RN events: `evidence/logs/m6-face-state-fix-runtime-2026-06-21.log`, `evidence/screen-recordings/m6-unity-to-rn-events-2026-06-21.mp4`, `evidence/screenshots/m6-rn-face-lost-latest-event-2026-06-21.jpg`, `evidence/screenshots/m6-rn-face-tracked-recipe-event-2026-06-21.jpg`.

## E7.3 Region Precision

Status: Yellow overall / blocked from E7.4. Implementation/build/install evidence is present and the screen recording confirms ARFace mesh/UV candidate mode runs on-device, but the legacy procedural candidate fails visual precision for `lip` and `eye`; `cheek` remains Yellow because the recording does not include a clean E7 candidate sample. P8 reference UV atlas runtime evidence is now collected for `lip`, `cheek`, and `eye`; all three remain Yellow because attachment improved but silhouette/material quality and required motion scenarios are not Q3 overlay-ready yet.

Decision:

- E7.03 Phase 0 Contract Reset is complete. `docs/roadmaps/archive/E7_REGION_PRECISION_SUBSPIKE_PLAN.md` v2.1 is the archived E7.03 boundary engine plan; reopen it only when the parent E7.3 boundary needs re-checking.
- E7.03 v2.1 Phase 1 Validation UI/Evidence Hygiene is implemented in the RN validation UI, JS-verified, and smoke-verified through a fresh real-device build/install/launch/runtime-log pass.
- E7.03 v2.1 Phase 2 ARFace authored atlas MVP is implemented in Unity and RN, with manual vertex label groups for `lip`, `cheek`, and `eye`, topology/UV audit evidence metadata, RN-dispatchable atlas variants, and preserved E3/E4 baseline plus current procedural candidate paths.
- E7 reference-driven UV atlas pre-gold setup was initialized locally before P2 capture: RN display/evidence wording identifies the current atlas path as a manual heuristic baseline, and `evidence/e7-reference-atlas/` contains the ignored local skeleton, manifest schema, manifest, and earlier pending `pair_lip_0001` capture-pair README. Current P2 evidence uses the newer common `pair_face_*` capture folders.
- E7.03 P2 synchronized reference capture is complete for input collection: clean `frame.png`, same-moment `arface_export.json`, `projected_mesh_overlay.png`, and `capture_summary.json` were exported and copied for three unique `pair_face_*` attempts. The best current drawing input is `pair_face_20260622T143334Z_03`.
- P2 remains an input-acquisition step only. It does not accept a gold mask, prove UV round-trip, change region G/Y/R, start MediaPipe runtime, or promote E7.03 Green.
- E7.03 A/B UV atlas fast gate is complete for offline viability only. After changing the script default to 1px back-projection sampling, `lip`, `cheek`, and `eye` all pass the fast gate; this keeps the reference-driven UV path alive and allows preparing a small P8 runtime sweep candidate set behind a Build Gate.
- E7.03 P8 runtime sweep implementation and first iPhone runtime pass are complete. It freezes 9 fast-gate-derived reference UV variants (`core`, `balanced`, `soft-wide` per region), adds `e7-reference-uv-atlas` as the primary runtime sweep renderer, keeps `e3e4-baseline`, `e7-arface-uv-candidate`, and `e7-arface-authored-atlas` as compare-only paths, and records real-device runtime compare logs plus representative Compact HUD photos.
- The previous "better than E3/E4 baseline" / "demo-plausible" Green standard is superseded by Q3 overlay-ready validation for `lip`, `cheek`, and `eye`.
- `TECH_VALIDATION_TEST_PLAN.md` was not changed for Phase 0 because no stable validation contract correction was required.
- Keep E3/E4 baseline renderer available as `e3e4-baseline`.
- Keep the previous E7 validation candidate renderer as `e7-arface-uv-candidate`; it remains a rejected/negative procedural baseline unless future evidence says otherwise.
- Add the Phase 2 authored atlas renderer as `e7-arface-authored-atlas`, default-selected for E7.3 atlas validation.
- Keep ARFace mesh/UV as a viable runtime substrate, but reject the current procedural candidate mask output for `lip` and `eye` until the candidate basis is replaced or tuned.
- Continue RN + Unity + ARKit validation only. Do not claim product-readiness, product-quality makeup, M7 Green, E7.4/E7.5/E7.6 readiness, AI/backend/upload, MediaPipe live runtime, SDK, or Android scope from this result.

Confirmed implementation:

- Unity `E3RegionMaskOverlay` now supports baseline-vs-E7 candidate modes plus `e7-arface-authored-atlas` while preserving the E3/E4 centroid baseline path.
- E7 candidate mode uses ARFace mesh availability plus UV-aware candidate evidence fields and face-local mesh regions for `lip`, `cheek`, and `eye`.
- Phase 2 authored atlas mode uses manual face-local vertex label groups (`lip_ring`, `cheekbone_soft_cheek`, `eyelid_band`) and RN-selectable variants for tight/balanced/wide or high/extended atlas sweeps.
- P8 reference UV atlas mode uses frozen probability atlas assets generated from `fast-gate-20260622T160307Z`. Unity samples the selected atlas through ARFace UV centroids using runtime-loaded Resources textures, while RN can sweep `core`, `balanced`, and `soft-wide` variants per `lip`, `cheek`, and `eye` in Compact HUD or Full Debug.
- Runtime comparison/evidence metadata now includes candidate id, variant id, atlas version, label map, label group, topology audit status, vertex/UV/index counts, labeled vertex counts, config hash, fallback flag, and fallback reason.
- P8 reference UV atlas metadata also logs source frame count, gold mask count, silver reference count, UV resolution, threshold, feather UV pixels/normalized value, combine mode, morphology, and calibration score summary in the Unity runtime comparison stream.
- Unity emits E7 comparison/state fields for renderer mode, mask source, tracking state, state action, mesh/UV counts, baseline triangle count, candidate triangle count, and applied triangle count.
- Tracking, Limited, lost, and recovered states now map to explicit actions such as render, short hold, fade, extended hide, and recovered restore.
- RN sends `rendererMode`, `candidateId`, and `variantId`, exposes a Baseline/E7 UV/Atlas segmented control, defaults E7.3 to `Atlas`, and surfaces phase/mask/UV/triangle/state/atlas fields in the E7 status panel and event summaries.
- RN recipe dispatch preserves the existing `ApplyRecipeJson` path and `recipe_applied` event while adding atlas metadata at the recipe/layer/ack/evidence levels.
- RN validation UI now supports `Clean`, `Compact HUD`, and `Full Debug` modes so large logs can be hidden during visual region review.
- RN P2 capture UI now starts in `Clean`, hides Unity face renderers/candidate masks/debug overlays for clean-frame capture, uses unique timestamped `pair_face_*` IDs for repeated attempts, disables the capture button while an export is pending, and places the capture button near the bottom of the screen so tapping does not cover the iPhone front camera.
- `Compact HUD` shows candidate id, selected region, tracking/face count, mesh counts, FPS/frame-time, state action, and recipe latency without covering the full AR view.
- `Full Debug` exposes evidence metadata lines for plan/version, evidence mode, candidate id, region, tracking state, face count, mesh counts, blendshape field status, FPS/frame-time, latency, state action, privacy flags, orientation/device fields where available, and latest Unity event.
- Candidate catalog is visible in Full Debug for planning hygiene. Dispatchable validation candidates are `e3e4-baseline`, current procedural `e7-arface-uv-candidate`, and Phase 2 `e7-arface-authored-atlas`; vertex/blendshape, Apple Vision, MediaPipe, parsing, and hybrid candidates remain pending metadata only.
- RN validation app suppresses the React Native dev LogBox warning overlay so the bottom warning bar does not cover AR evidence or recipe controls. Console/runtime evidence logging remains available through captured device logs.
- Current validation cleanup path fixes the renderer to `smooth-region-mask`, removes candidate/variant UI from the user-facing app, keeps `lip`/`cheek`/`eye` enabled by default, supports independent ON/OFF region toggles, and sends exactly three recipe layers in one RN -> Unity batch.
- Unity rejects legacy/non-batch recipe payloads, unsupported renderer modes, missing/mismatched texture samples, missing/mismatched smooth mask texture ids, and invalid region names instead of silently falling back to an old or wrong region. If parsing fails, Unity clears current recipes and hides overlays to avoid stale masks.
- Compact HUD status now lives inside the bottom `Regions` panel, so it no longer covers the center of the face during validation.

Evidence:

- Phase 1 JS verification: `./node_modules/.bin/tsc --noEmit` passed from `rn/MakeupARValidation`.
- Phase 1 lint verification: `npm run lint` passed from `rn/MakeupARValidation`.
- Phase 1 LogBox cleanup JS verification after screenshot review: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- Phase 1 JS-only pass intentionally did not run `bash scripts/build_m3_unityframework.sh`, `xcodebuild`, `npm run ios`, or a real-device build.
- Phase 2 JS verification after atlas implementation: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- P2 synchronized capture JS verification after capture UI fixes: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- Reference-driven UV atlas pre-gold setup verification: `python3 -m json.tool evidence/e7-reference-atlas/manifest.json` passed; `python3 -m json.tool evidence/e7-reference-atlas/manifest.schema.json` passed; `find evidence/e7-reference-atlas -maxdepth 3 -type f` lists `README.md`, `manifest.json`, `manifest.schema.json`, and `capture_pairs/pair_lip_0001/README.md`; `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check` passed. This verification is no-build/no-device only.
- Phase 2 diff hygiene: `git diff --check` passed after reverting the Unity scene reserialization produced by the build.
- Phase 1 real-device follow-up regenerated UnityFramework with `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase1-ui-2026-06-22-141533.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-phase1-ui-2026-06-22-141533.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data.
- Phase 2 UnityFramework regeneration/sync used `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase2-atlas-2026-06-22-151537.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-phase2-atlas-2026-06-22-151537.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data and copied Unity Data.
- P2 UnityFramework regeneration/sync used `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-2026-06-22-230402.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-2026-06-22-230402.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data and copied Unity Data.
- Phase 1 RN iOS build/install/launch: `evidence/logs/e7-phase1-ui-rn-ios-run-2026-06-22-141533.log` records the Debug build using device id `00008140-000924DE21BB801C`, successful build, install, bundle id `com.makeupar.rnvalidation`, and successful launch on `위승철의 iPhone`.
- Phase 2 RN iOS build/install: `evidence/logs/e7-phase2-atlas-2026-06-22-151537-rn-ios-device.log` records successful Debug build and install on `위승철의 iPhone`; launch was denied because the device was locked, so this is build/install evidence only.
- P2 RN iOS build/install/launch after unique capture-id and bottom-button fix: `evidence/logs/e7-reference-capture-rn-ios-run-2026-06-22-233049.log` records successful Debug build, install, and launch on `위승철의 iPhone`.
- P2 runtime console: `evidence/logs/e7-reference-capture-runtime-console-2026-06-22-233202.log` records three `reference_capture_requested` / `reference_capture_exported` pairs with exported capture IDs `pair_face_20260622T143300Z_01`, `pair_face_20260622T143305Z_02`, and `pair_face_20260622T143334Z_03`.
- P2 copied capture artifacts: each of `pair_face_20260622T143300Z_01`, `pair_face_20260622T143305Z_02`, and `pair_face_20260622T143334Z_03` contains `frame.png`, `arface_export.json`, `projected_mesh_overlay.png`, and `capture_summary.json` under `evidence/e7-reference-atlas/capture_pairs/`.
- P2 artifact verification: all three captures have `frame.png` and `projected_mesh_overlay.png` at `1179x2556`, `trackingState=Tracking`, mesh counts `1220` vertices / `6912` indices / `1220` UVs, `screenVertices=1220`, `annotationFrameClean=true`, `hudIncludedInFrame=false`, `meshOverlayIncludedInFrame=false`, `candidateOverlayIncludedInFrame=false`, and `blendShapes.available=false`.
- P2 visual review: `pair_face_20260622T143300Z_01/frame.png`, `pair_face_20260622T143305Z_02/frame.png`, and `pair_face_20260622T143334Z_03/frame.png` are clean frames with no white face model, colored candidate mask, Unity debug panel, or RN capture controls visible. `pair_face_20260622T143300Z_01/projected_mesh_overlay.png` and `pair_face_20260622T143334Z_03/projected_mesh_overlay.png` visually align to the same frame; `_03` is the best current gold-mask drawing candidate because the face is frontal and calm.
- M1 buildless lip package audit/repair evidence: `evidence/e7-lip-m1-packages/m1-lip-repair-20260625T120000Z/audit_report.json`, `audit_report.md`, `input_manifest.json`, `lip_mesh_draft.png`, `lip_mesh_draft_overlay.png`, `lip_mesh_draft_meta.json`, `lip_reference_mask.png`, `lip_reference_mask_meta.json`, `fusionSummary.json`, `fusionSummary.md`, `summary.json`, `summary.md`, `lip_probability.png`, `lip_coverage.png`, `lip_unknown.png`, `lip_debug_votes.png`, `lip_variants.json`, and `round_trip_overlay.png`; the manual polygon reference inside this lineage is now rejected-by-user evidence only and must not be reused as gold/reference/candidate seed. Apple Vision contour evidence is in `evidence/e7-lip-m1-packages/m1-lip-apple-vision-20260625T132216Z/apple_vision_lip_contour.json`, `apple_vision_lip_contour_overlay.png`, `fusionSummary.json`, and `m1_summary.json`. Local/offline face parsing evidence is in `evidence/e7-lip-m1-packages/m1-lip-face-parsing-20260625T2255Z/face_parsing_labels.json`, `face_parsing_lip_mask.png`, `face_parsing_upper_lip_mask.png`, `face_parsing_lower_lip_mask.png`, `face_parsing_inner_mouth_mask.png`, `face_parsing_overlay.png`, `face_parsing_confidence.json`, `lip_candidate_generation.json`, `fusionSummary.json`, and `m1_summary.json`; it records `status=silver`, upper/lower lip label pixels `7325/9842`, inner-mouth pixels `0`, and face parsing output only. Decision is still `partial`; artifact existence, Vision availability, face parsing availability, same-frame IoU, and any mask derived from the rejected marking are explicitly not success criteria.
- User-painted gold raw references: `evidence/references/e7-user-gold-raw-20260626/README.md`, `manifest.json`, `contact-sheet.png`, `combined-soft-alpha-projection.png`, `user-gold-lip-mask-gray-20260625-163204.png`, `user-gold-lip-render-20260625-163240.png`, `A_1.png`, and `A_0.png`. User decision: all files in this folder are human-painted gold reference assets for offline reference, candidate generation, candidate scoring, and mask derivation experiments. They are local-only and separate from the rejected polygon lineage; M1 UV projection readiness still requires coordinate-space and same-moment ARFace export pairing for the selected asset.
- Fast gate script verification: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py` passed.
- Fast gate run: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/e7_reference_atlas/fast_uv_atlas_gate.py` completed with overall decision `continue` and output root `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/`.
- Fast gate region metrics: `lip` same-frame IoU `0.9720` / `0.9693`, cross-frame IoU `0.8193` / `0.8325`, UV core/union `0.7046`; `cheek` same-frame IoU `0.9912` / `0.9906`, cross-frame IoU `0.8363` / `0.8302`, UV core/union `0.7106`; `eye` same-frame IoU `0.8604` / `0.8457`, cross-frame IoU `0.6948` / `0.7129`, UV core/union `0.8060`.
- Fast gate visual review: `lip_candidate_preview.png`, `cheek_candidate_preview.png`, and `eye_candidate_preview.png` show plausible face-attached transfer between A/B for offline viability. This supports candidate preparation for runtime sweep, but still needs real-device visual/runtime evidence before any E7.03 Green claim.
- P8 runtime sweep static implementation verification: frozen Resources were generated from `fast-gate-20260622T160307Z` into `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/`; `runtime_candidates.json` lists `rendererMode="e7-reference-uv-atlas"`, `candidateId="arface-reference-uv-atlas"`, 2 source frames, 2 gold masks, UV resolution `512x512`, and 9 promoted variants. Static checks passed with no Unity/RN device build: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check`.
- P8 UnityFramework regeneration/sync after Build Gate approval used `TIMESTAMP=e7-p8-runtime-sweep-2026-06-23-013524 bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-p8-runtime-sweep-2026-06-23-013524.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-p8-runtime-sweep-2026-06-23-013524.log` records synced RN/package UnityFramework artifacts and Unity Data.
- P8 RN iOS build/install/launch used `npm run ios -- --device "위승철의 iPhone" --no-packager --extra-params DEVELOPMENT_TEAM=9G4K6N63MK` from `rn/MakeupARValidation`: `evidence/logs/e7-p8-runtime-sweep-rn-ios-run-2026-06-23-013524.log` records successful install/launch on device id `00008140-000924DE21BB801C`.
- E7 Full Face Region Generate phone-connected build/install/launch evidence: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-full-face-iphone-20260627-1348.log` records UnityFramework `** BUILD SUCCEEDED **`; `evidence/logs/m3-repro-artifact-verification-e7-full-face-iphone-20260627-1348.log` records synced RN/package UnityFramework artifacts; `evidence/logs/e7-full-face-rn-ios-run-20260627-1410.log` records the RN CLI fallback failure (`No simulator available with udid "undefined"`); `evidence/logs/e7-full-face-rn-ios-xcodebuild-20260627-1430.log` records direct Xcode `** BUILD SUCCEEDED **`; `evidence/logs/e7-full-face-rn-ios-devicectl-install-20260627-1430.log` records install of `com.makeupar.rnvalidation`; and `evidence/logs/e7-full-face-rn-ios-devicectl-launch-20260627-1430.log` records launch. A follow-up `devicectl device info processes` check found the `MakeupARValidation` process on-device. This is build/install/launch evidence only, not visual/runtime acceptance.
- P8 runtime console stream: `evidence/logs/e7-03-p8-runtime-console-2026-06-23-013524.log` captures the full `devicectl --console` stream until manual stop. Compact HUD overlay visibility is logged as `validationViewMode=compact`, `visible=true`, and `faceRenderersSuppressed=false`; later Clean segments intentionally log `visible=false` / `suppressed_for_clean_reference_capture`.
- P8 runtime region compare evidence: the console stream records `lip-uvref-v0-balanced`, `cheek-uvref-v0-balanced`, and `eye-uvref-v0-balanced` with `rendererMode=e7-reference-uv-atlas`, `candidateId=arface-reference-uv-atlas`, `trackingState=Tracking`, `stateAction=tracking_render`, mesh/UV counts `1220/6912/1220`, `uvResolution=512`, `threshold=0.45`, `featherUvPixels=2`, `atlasVersion=e7ref-fastgate-20260622T160307Z-v0`, `topologyAuditStatus=pass_uv_topology_ready`, and `usedFallback=false`.
- P8 representative visual evidence: user-provided Compact HUD photos are stored under `evidence/screenshots/e7-03-p8-runtime-sweep-2026-06-23-013524/`. They show `Ref UV` selected, `candidate=arface-reference-uv-atlas`, Compact HUD visible, and face-attached masks for `lip`, `cheek`, and `eye` across `eye core`, `eye balanced`, and `eye soft-wide` variant checks. Visual preference for `eye` is `balanced` as the current default; `core` is safer but too broken/under-covered for the main candidate, and `soft-wide` is too broad.
- E7.03 smooth-mask cleanup acceptance screenshots: `evidence/screenshots/e7-smooth-mask-accepted-2026-06-23/01-frontal-hud.jpg`, `02-left-angle-hud.jpg`, and `03-right-angle-hud.jpg`. These are the only retained media for this acceptance pass per user request. They show Compact HUD moved to the bottom controls, visible subtle eye/cheek/lip color, `active=lip,cheek,eye`, `focus=eye`, `opacity=0.48`, `intensity=0.58`, `applied=true`, tracking face count `1`, mesh counts `1220/6912/1220`, FPS range `52.2-60.2`, and recipe latency `36.0ms`. User evaluation: acceptable for closing this step, but visually still too subtle overall; the eye boundary remains a little weak; future validation should make region coverage easier to judge, and the remaining issue is deferred into cosmetic expression/modeling.
- Phase 1 runtime console: `evidence/logs/e7-phase1-ui-runtime-console-2026-06-22-141533.log` has `93,186` lines captured through `devicectl --console`; the capture was manually stopped after sufficient evidence, so exit code `130` is capture termination, not an observed app crash.
- Runtime evidence includes UnityFramework load success, Unity root view attachment, M6 Unity -> RN events, E1/E2 tracked face lifecycle, E5 snapshots with `rawCameraFrameStored=false` and `offDeviceUpload=false`, and E7 metric samples.
- Runtime counts from the Phase 1 console stream: `[E7] metric_sample` `15`, `e7_metric_sample` RN events `15`, `[M6] unity_to_rn_send` `1189`, `face_feature_snapshot_created` `1193`, `SessionTracking` `3179`.
- Representative Phase 1 metric sample: tracking face count `1`, mesh `1220` vertices / `6912` indices / `1220` UVs, `hasStableUv=true`, average FPS samples observed at `57.1`, `60.0`, and `60.2` after cold start, memory metric available, thermal still manual-device-heat capped Yellow.
- Capture tooling check: `idevicescreenshot`, `ios-deploy`, `cfgutil`, `tidevice`, `pymobiledevice3`, and `idb` were not installed; `devicectl device` and `xcdevice` exposed no screenshot command. Display metadata was captured instead: active portrait LCD, `1179x2556`, point scale `3`.
- User-provided iPhone screenshots were saved under `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/` and verify the Phase 1 modes at `590x1280`: `clean-view.jpg` shows Clean mode with the large debug panels hidden, `compact-hud.jpg` shows Compact HUD with candidate/region controls, and `full-debug.jpg` shows Full Debug evidence metadata, latest Unity event, face state, snapshot, and candidate catalog.
- The screenshot review exposed the React Native dev warning bar (`Open debugger to view warnings`) covering bottom controls. `LogBox.ignoreAllLogs(true)` was added in `rn/MakeupARValidation/App.tsx` to suppress that UI overlay in the validation app. Per user decision, the follow-up reinstall was stopped/skipped because this was a small JS-only cleanup.
- UnityFramework build/sync: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-region-precision-2026-06-22.log` line `2264` records `** BUILD SUCCEEDED **`.
- Artifact verification: `evidence/logs/m3-repro-artifact-verification-e7-region-precision-2026-06-22.log` records arm64 Mach-O RN/package frameworks and copied Unity Data.
- Real-device RN build/install/launch after E7 default renderer change: `evidence/logs/e7-region-precision-rn-ios-run-default-candidate-2026-06-22.log` records successful build, install, and launch on `위승철의 iPhone`.
- Summary: `evidence/logs/e7-region-precision-summary-2026-06-22.md`.
- User-provided recording analysis: `evidence/logs/e7-region-precision-video-analysis-2026-06-22.md` records 28.46s / 59.45 FPS / 1180x2556 metadata and region verdicts.
- Contact sheet: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/contact-sheet-1fps.jpg`.
- Representative visual evidence:
  - `eye`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_001_01.35s.jpg` shows `E7 UV` + `eye` + `phase=region_precision`, but the mask is broad face/lower-face coverage instead of an eye zone.
  - `cheek`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_015_15.35s.jpg` shows cheek selection, but the metric is `phase=baseline`, `mask=centroid_broad`, so this is not a clean E7 candidate sample.
  - `lip`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_020_20.35s.jpg` and `frame_028_28.35s.jpg` show `E7 UV` + `lip` + `phase=region_precision`, but the mask spills across lower-face/cheek/chin-side skin.

Region G/Y/R:

- Legacy procedural `lip`: Red. E7 procedural candidate mode is live, but the selected `lip` candidate repeatedly paints surrounding lower-face skin in normal tracking frames.
- Legacy procedural `cheek`: Yellow / insufficient E7-candidate sample. Cheek selection is present, but the visible metric is baseline or transition-stale, so no region Green/Red is justified yet.
- Legacy procedural `eye`: Red. E7 procedural candidate mode is live, but the selected `eye` candidate is not confined to an eyeshadow/eye region and covers broad non-eye areas.
- P8 reference UV `lip`: Yellow. Runtime log confirms ARFace UV sampling with fallback disabled, and photos show the lip mask stays mouth-local, but the silhouette is jagged/flat and not Q3 overlay-ready.
- P8 reference UV `cheek`: Yellow. Runtime log confirms tracking render with fallback disabled, and photos show cheek-local masks, but the region is still blocky/low on the face and needs yaw/pitch/near-far scenario review.
- P8 reference UV `eye`: Yellow. Runtime log confirms tracking render with fallback disabled, and photos show eye-local masks across core/balanced/soft-wide checks. `eye balanced` is the best current candidate among the three because it preserves eyelid coverage better than `core`; `soft-wide` is too broad. The remaining blocker is jagged/overbroad silhouette quality, not runtime attachment.

Known limitations:

- The earlier E7.3 screen-recording review still lacks a full runtime console stream; the fresh Phase 1 follow-up has a runtime console stream, but no fresh visual screenshot/recording.
- No `[E7] region_precision_compare` runtime stream was captured during this pass.
- P8 runtime evidence is a first representative pass, not a complete required-scenario sweep. Neutral/front-facing photos are present, but smile/open-close/pucker/yaw for `lip`, near/far/yaw/pitch for `cheek`, and blink/repeated blink/squint/head pitch for `eye` still need structured review before any region can become Green.
- P8 Compact HUD photos still show an opaque white full-face debug surface behind the colored regions. That is useful for validation visibility, but it blocks a Q3 overlay-ready makeup claim until the material/debug-surface behavior is separated from region mask evidence.
- The accepted smooth-mask cleanup screenshots are user-accepted for the current app optimization goal, not a formal E7.03 Green decision. They do not cover the full required motion matrix, diverse lighting/users, E7.4 cosmetic material realism, or the user's noted visibility/eye-boundary weakness.
- Phase 2 atlas runtime launch limitation is superseded for P2 capture only: the app now launches and captures synchronized reference pairs, but this still is not runtime atlas visual scoring and does not change Phase 2 region G/Y/R.
- The reference-driven UV atlas path has captured synchronized P2 inputs and user-painted gold raw references now exist, but `goldMaskWork.status` is still not promoted into the official coordinate-paired atlas pipeline; there is no selected coordinate-paired gold mask, UV back-projection, same-frame round-trip render, P3 acceptance, or P4 validation yet.
- The A/B fast gate used user-authored mask PNGs already present in the capture folders, but it does not normalize `manifest.json` into official multi-frame gold-mask status and does not replace the full P3/P4/P5/P6 pipeline.
- The fast gate excludes `triangleVisibility`, occlusion/depth conflict handling, and blendshape correction because the current export has no triangle visibility records and `blendShapes.available=false`.
- The canonical fast gate summary is `fast-gate-20260622T160307Z`. Earlier debug/sparse-sampling/smoke outputs were removed or superseded and should not be used for decisions.
- P2 export records `blendShapes.available=false` because blendshapes are not exposed in the current capture exporter.
- P2 export records `coordinateSpaceValidated=false` / `pending_projected_mesh_overlay_review`; visual overlay review is positive for `_01` and `_03`, but no automated pixel-level coordinate acceptance threshold has been implemented.
- M1 repair remains partial: Apple Vision contour extraction and local/offline face parsing are implemented for the same neutral frame, and user-painted gold raw references now exist under `evidence/references/e7-user-gold-raw-20260626/`. Required M1 gates are still not implemented/run or not accepted for user-confirmed adjustment, pucker/expression captures, blendshape/face-state, coordinate-paired selected gold/reference mask, coordinate-space audit, visibility/front-most handling, held-out/eval frame plan, inner-mouth exclusion acceptance, corner falloff, and full upper/lower split acceptance. Failure-mode classification is intentionally deferred until boundary quality is strong enough for meaningful edge-case taxonomy. The mesh draft overlay is visibly too broad and cannot be promoted to reference/gold; the rejected polygon-derived reference lineage must not be reused; the face parsing run is a closed-mouth silver signal with `inner_mouth=0`, so it cannot by itself prove inner-mouth exclusion or pucker behavior; same-frame round-trip IoU is projection sanity only, not boundary quality.
- Cheek still needs a clean `phase=region_precision` sample before final region scoring.
- Lost, Limited, and recovered behavior were not exercised in this recording.
- On-screen FPS/frame-time values are visible, but this is not E7.6 performance evidence.
- The candidate is a validation candidate, not product-quality region segmentation or cosmetic rendering.
- Phase 2 manual atlas labels are authored validation heuristics over ARFace mesh/UV data. They are not MediaPipe, Apple Vision, face parsing, product segmentation, or a product-ready makeup asset source.
- Timestamped P2 capture IDs intentionally differ from the original fixed `pair_face_0001` request so repeated capture attempts do not overwrite each other. This is an evidence-management adjustment, not a renderer behavior change.
- E7.3 evidence does not resolve the accepted M7 Yellow lifecycle risk.
- Phase 1 UI modes are visually screenshot-verified from user-provided iPhone captures, and Phase 2 installed the LogBox-suppressed RN build on-device, but post-LogBox visual confirmation is still pending because launch was blocked by device lock. The local toolchain still has no automated iPhone screenshot CLI, and installed `devicectl`/`xcdevice` exposes no screenshot command.
- Non-atlas future candidates shown in the candidate catalog are not runtime implementations; they are pending labels for later E7.03 phases.
- `Full Debug` can still cover part of the AR view by design; use `Clean` or `Compact HUD` for visual evidence capture.
- Phase 1 does not change region G/Y/R: `lip` remains Red, `cheek` remains Yellow / insufficient E7 candidate sample, and `eye` remains Red.
- Phase 1 runtime evidence is dominated by the default `lip` candidate path; it does not provide clean `cheek` or `eye` visual region evidence.
- Phase 2 does not claim M7 Green, E7.4/E7.5/E7.6 readiness, AI/backend/upload readiness, SDK readiness, Android readiness, product readiness, or M7 lifecycle closure.

Next boundary:

- The current smooth-mask validation app cleanup/optimization goal is closed by user acceptance, with screenshots retained as the only media artifact for that pass.
- If continuing E7.3 strictly, use the accepted smooth-mask cleanup as the current usable validation app state and run structured region scenarios before any Green decision.
- If entering the next makeup-modeling scope, first create or update `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` and explicitly record that E7.4 starts on top of an accepted-for-validation but still E7.3-Yellow boundary state.
- The next real proof must be a follow-up runtime comparison with full metadata and representative visual evidence for the missing scenarios: smile/open-close/pucker/yaw for `lip`, near/far/yaw/pitch for `cheek`, and blink/repeated blink/squint/head pitch for `eye`.
- Keep the current procedural ARFace mesh/UV candidate and current manual heuristic baseline as compare-only paths unless future evidence promotes a new reference-driven atlas candidate.
- Do not enter E7.4/E7.5/E7.6 until `lip`, `cheek`, and `eye` have Q3 overlay-ready evidence, or until the team explicitly accepts E7.3 Yellow risk and records that later E7.4/E7.5 evidence is conditional and full E7 visual product-readiness remains capped at Yellow.

## E7.2 Baseline Instrumentation

Status: Green for E7.2 baseline instrumentation.

Confirmed implementation:

- Unity emits `[E7] metric_sample` with FPS, frame-time, worst frame-time, sustained-sub-20 flag, tracking state, mesh counts, active region/texture baseline state, memory availability/values, and thermal/manual-heat fields.
- Unity emits `[E7] metric_unavailable` for unavailable thermal native API evidence and keeps `thermalMetricYellowCap=true`.
- Unity recipe apply logging records RN `sentAtMs`, Unity `appliedAtMs`, and Unity `appliedFrame` while preserving the E3/E4 `ApplyRegionRecipe` path.
- RN sends a log-only ack after receiving `recipe_applied`, so Unity console evidence records RN `receivedAtMs`, `sendToAckLatencyMs`, `unityApplyLatencyMs`, and `unityToRnReceiveLatencyMs`.
- RN UI includes an E7 status panel with latest baseline metric and recipe latency fields.

Runtime summary:

- Runtime log line count: `65,428`.
- `[E7] metric_sample`: `9`.
- `[E7] metric_unavailable`: `1`.
- `[E7] recipe_latency source=unity_applied`: `14`.
- `[E7] recipe_latency source=rn_ack`: `14`.
- Tracking-state metric samples: `7`; average FPS mean/min/max `57.6 / 51.4 / 60.1`; average frame-time mean `17.4 ms`; allocated memory range `64.3-64.9 MB`; reserved memory max `82.2 MB`.
- RN ack latency samples: `14`; RN sent -> RN received mean/min/max `137.6 / 33.0 / 1473.0 ms`; Unity applied -> RN received mean/min/max `28.4 / 7.0 / 208.0 ms`.

Known limitations:

- First metric and latency samples include cold-start overhead and must not be used as final E7.6 performance evidence.
- Native thermal API integration is not configured; thermal is manual-device-heat evidence with a Yellow cap.
- E7.2 relies on a user-provided representative frame instead of a screen recording. This waiver is scoped only to E7.2.

## Workspace Document State

Active root documents:

- `AGENTS.md`: repository working rules, milestone scope boundaries, document policy, and evidence/cleanup requirements.
- `TECH_VALIDATION_TEST_PLAN.md`: stable validation contract. Do not update for progress/status unless correcting the contract.
- `TECH_VALIDATION_RESULT.md`: current snapshot, latest milestone decisions, and next boundary.

History archive:

- Full historical details, requirement matrices, special observations, environment/device snapshots, and the full evidence index were moved to `docs/roadmaps/archive/TECH_VALIDATION_HISTORY_2026-06-22.md`.
- Use the archive only when auditing old milestone evidence or reconstructing past decisions.

Document policy:

- New `M*_..._PLAN.md` or `E*_..._PLAN.md` files are temporary session plans.
- After a session completes, absorb the result into this file and delete temporary plans unless the user asks to keep them.
- Put reusable procedures in `docs/runbooks/`.
- Keep roadmap/research docs under `docs/roadmaps/`.

## Cleanup And Token Management

Local cleanup policy:

- Use `bash scripts/cleanup_local_generated.sh --profile balanced --dry-run` before applying cleanup.
- The balanced profile removes large reproducible generated artifacts while preserving `node_modules`, iOS `Pods`, Unity `Library`, logs, screenshots, and existing evidence directories. Use `--profile share` before handing the directory to teammates; it removes ignored evidence, dependency installs, editor caches, and build products.
- To prevent workspace growth, keep `evidence/`, `unity-builds/`, RN `node_modules`, iOS `Pods`, Xcode DerivedData/build folders, Unity `Library`/`Logs`/`UserSettings`, raw recordings, and full build logs out of source control and remove them with the cleanup profiles when they are no longer active evidence/debug state.
- `scripts/build_m3_unityframework.sh` now defaults to temporary DerivedData plus temporary Unity/Xcode full logs and keeps only the small artifact verification summary. Use `BUILD_LOG_MODE=full` or `KEEP_DERIVED_DATA=1` only for a specific evidence/debug pass.
- After cleanup, the next real-device Unity/RN session must regenerate UnityFramework with `bash scripts/build_m3_unityframework.sh`.
- Detailed cleanup procedure lives in `docs/runbooks/LOCAL_WORKSPACE_CLEANUP_RUNBOOK.md`.
- Team onboarding, share execution requirements, directory structure, and local-only values live in `docs/runbooks/TEAM_SHARE_REQUIREMENTS_KO.md`.

Token policy:

- Future sessions should start from `AGENTS.md` plus `TECH_VALIDATION_RESULT.md` > `Current Session Snapshot`.
- Do not load all research reports by default.
- Load only the research axis that matches the current E7 milestone.
- Do not load `docs/roadmaps/active/`, `docs/roadmaps/research/`, or `docs/roadmaps/archive/` as whole folders. Use `docs/roadmaps/README.md` as the menu and open only the selected file.
- For `_KO.md` / `_KR.md` companion pairs, agents should read the `_KO.md` primary implementation plan and skip `_KR.md` unless user-facing wording is specifically needed.
- For broad search, exclude lockfiles and generated project/config files unless the task is dependency or native project troubleshooting: `package-lock.json`, `Podfile.lock`, Xcode `project.pbxproj`, and Unity `ProjectSettings/*.asset`.
- For large code hotspots, use symbol search plus targeted ranges instead of opening whole files by default: `rn/MakeupARValidation/App.tsx`, `unity/MakeupARUnityValidation/Assets/Scripts/RNBridge.cs`, `unity/MakeupARUnityValidation/Assets/Scripts/FaceTrackingStatusReporter.cs`, and `unity/MakeupARUnityValidation/Assets/Scripts/E3RegionMaskOverlay.cs`.

Result growth policy:

- Keep this file as a compact routing and decision record, not a full log dump.
- Record only the latest decision, evidence paths, known limitations, and next boundary. Do not paste full build logs, full console streams, raw JSON dumps, frame lists, or long analysis transcripts into this file.
- When a milestone accumulates old details, move historical evidence lists and superseded narrative into `docs/roadmaps/archive/TECH_VALIDATION_HISTORY_YYYY-MM-DD.md`, then keep only the current high-signal summary here.
- If `Current Session Snapshot` becomes hard to scan, compress older bullets into one status bullet plus archive references before adding new work.
- Prefer metadata, contact sheets, representative frames, and short summary logs over raw recordings or full logs. Keep raw/heavy artifacts local under ignored `evidence/` only while they are active decision evidence.

## Next Milestone Boundary

E1 AR Alignment, E2 Trackable Lifecycle Diagnostics, E3 Region Mask, E4 Texture Sample, E5 AI Feature Readiness Snapshot, E6 Engine Decision, E7.0/E7.1 preflight, and E7.2 Baseline Instrumentation are complete. The 2026-06-23 smooth-mask validation app cleanup/optimization goal is closed by user acceptance: the app now uses the simplified `smooth-region-mask` runtime surface, hides obsolete selector/capture/debug paths from the active UI, keeps HUD evidence in the bottom controls, and stores only the three accepted screenshots under `evidence/screenshots/e7-smooth-mask-accepted-2026-06-23/`. User feedback for the retained screenshots says the makeup is still generally hard to see and the eye boundary is somewhat lacking, but this is acceptable for closing this step and should be addressed with cosmetic expression/modeling. This closes the current app optimization task, not the full E7.03 quality gate. E7.3 remains Yellow overall because Q3 overlay-ready motion/scenario evidence for all three regions is incomplete, and full E7 visual product-readiness is not complete. M0-M6 remain Green. M7 remains Yellow / skipped by decision / risk accepted.

Next boundary decision:

- Full-face region Generate phone-connected gate: start from `evidence/e7-region-generate/session-20260626T195853Z/pre_xcode_gate.md` plus the 2026-06-27 build/install/launch logs listed above. Unity batchmode smoke, UnityFramework export/sync, direct RN/Xcode iPhone build, device install, launch, and process-presence checks have passed. The next step is runtime visual/evidence capture: region-separated visual acceptance for `lip`, `blush`, `brow`, and `eyeliner`, plus neutral/smile/open-close/yaw/blink scenarios, face attachment, FPS/frame-time, latency, memory, thermal, and log evidence. Until that evidence exists, keep all four regions at `pre-xcode-ready` and do not claim Green/product-quality-ready.
- Lip-first calibration path: M1 is still `partial`, not M2-ready, but all currently available buildless work has been advanced beyond the original Section 14B run. The current provisional package is `evidence/e7-lip-m1-packages/m1-lip-auto-selected-20260625T215327Z/`; use its `m1_gate_matrix.json`, `next_automation_queue.json`, and `m1_summary.json` as the next local entry point. The next true blockers are no longer “run more unattended agents” but missing evidence: user-confirmed adjustment, open-close/smile/pucker/yaw and held-out captures, blendshape/face-state export, named coordinate/viewport/clipW/depth/front-most visibility fields, coordinate audit pass, and accepted inner-mouth/corner/upper-lower behavior. `evidence/e7-lip-missing-scenario-captures/manifest-20260625T220600Z/capture_queue.md` is the next phone-connected capture queue. Validation-only M3A source-stage artifacts now exist under `evidence/e7-lip-runtime-sweep-prep/validation-candidates-20260626Tsetup/` and Unity Resources, but M3B/runtime/Lip G/Y/R remain build-gated and blocked by incomplete M1/M2 inputs. Keep this lip-only, local-only, no upload, no live face parsing/Core ML runtime, and no UnityFramework/Xcode/iPhone build until explicitly approved.
- Primary path: current validation app optimization is closed; next work should either continue E7.3 scenario hardening from the accepted smooth-mask state, or explicitly open E7.4 cosmetic modeling with a new `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` and a recorded Yellow boundary risk.
- Conservative path: run additional M7 re-entry verification if the team wants to replace the accepted M7 Yellow risk with formal 3-cycle lifecycle evidence.
- Renderer path: do not enter E7.4/E7.5/E7.6 until E7.03 v2.1 evidence supports Q3 overlay-ready validation for all three regions, or until the team explicitly accepts the remaining E7.3 Yellow risk, records the boundary, and treats later renderer/demo evidence as conditional with a Yellow cap.
