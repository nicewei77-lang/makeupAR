# Tech Validation Result

Date: 2026-06-21

Latest re-check: 2026-06-22 11:18 KST

Latest document cleanup: 2026-06-22 KST

Latest E7.03 contract reset: 2026-06-22 KST

Latest E7.03 Phase 1 UI/evidence hygiene implementation: 2026-06-22 KST

Latest E7.03 Phase 1 real-device build/log/capture-tooling check: 2026-06-22 14:25 KST

Latest E7.03 Phase 1 screenshot/LogBox cleanup: 2026-06-22 14:27 KST

Latest E7.03 phase-wide time-saving plan update: 2026-06-22 KST

Latest E7.03 Phase 2 ARFace authored atlas MVP implementation/build/install check: 2026-06-22 15:24 KST

Latest E7 reference-driven UV atlas pre-gold setup: 2026-06-22 KST

Latest E7.03 P2 synchronized reference capture: 2026-06-22 23:34 KST

Latest E7.03 A/B UV atlas fast gate: 2026-06-23 KST

Latest E7.03 P8 runtime sweep implementation prep: 2026-06-23 KST

Latest E7.03 P8 runtime sweep build/log/visual review: 2026-06-23 KST

Latest E7.03 smooth-mask validation app cleanup/user acceptance: 2026-06-23 KST

Latest workspace share cleanup/document structure pass: 2026-06-23 KST

Latest iPhone AR black-screen hotfix build/install: 2026-06-25 KST

Latest E7 lip makeup validation v1 no-build implementation: 2026-06-25 KST

Latest E7 lip makeup validation v1 build gate attempt: 2026-06-25 KST

Latest E7 lip-first photo feedback no-build tuning: 2026-06-25 KST

Latest E7 lip atlas UV back-projection tightening: 2026-06-25 KST

Latest E7 user-drawn mask iPhone packaging/install attempt: 2026-06-25 KST

Latest E7 runtime photo feedback UX/shader correction: 2026-06-25 KST

Latest E7 runtime photo feedback build/install attempt: 2026-06-25 KST

Latest E7 matte/gloss/gradient no-build tuning: 2026-06-25 KST

Latest E7 matte/gloss/gradient UnityFramework-only build/sync: 2026-06-25 KST

Latest E7 lip v2 boundary-first Vision gate correction: 2026-06-26 KST

Latest E7 runtime Vision UnityFramework build/sync and RN device attempt: 2026-06-26 KST

Latest E7 runtime Vision screen-space mask no-build correction: 2026-06-26 KST

Latest E7 runtime Vision screen-space mask UnityFramework build/sync and RN signing gate: 2026-06-26 KST

Latest E7 final ARFace-attached lip atlas fallback UnityFramework build/sync and RN signing gate: 2026-06-26 KST

Latest E7 lip soft-SDF A-channel white line / Vision motion diagnostics no-build pass: 2026-06-26 KST

Latest E7 lip soft-SDF multilayer UnityFramework build/sync: 2026-06-26 KST

Latest E7 matte/gradient/gloss user-photo retune and offline preview: 2026-06-26 KST

Latest E7 matte/gradient/gloss retune UnityFramework build/sync and RN signing gate: 2026-06-26 KST

Latest E7 lip finish multi-agent handoff: 2026-06-26 KST

Latest E7 gradient/gloss continuous-ramp no-build retune: 2026-06-26 KST

Latest E7 gradient lip density-field atlas no-build correction: 2026-06-26 KST

Latest E7 gradient lip boundary-risk no-build hard reset: 2026-06-26 KST

Latest E7 gradient lip matte-derived Build Gate / RN device gate: 2026-06-26 KST

Latest E7 gradient lip user acceptance/freeze: 2026-06-26 KST

Latest E7 gloss tint cluster highlight no-build retune: 2026-06-26 KST

Latest E7 gloss tint cluster UnityFramework build/sync and RN signing gate: 2026-06-26 KST

Latest E7 gloss matte-base wet-sheen no-build correction: 2026-06-26 KST

Latest E7 gloss 1px edge-blended lower-lip specular mask correction: 2026-06-26 KST

Latest E7 gloss user-drawn lower-lip specular cluster no-build tweak: 2026-06-26 KST

Latest E7 lip implementation Korean report: 2026-06-27 KST

Latest E7 cheek blush v1 buildless implementation: 2026-06-27 KST

Latest E7 cheek blush runtime Build Gate packet: 2026-06-27 KST

Latest E7 cheek blush completion audit: 2026-06-27 KST

Latest E7 cheek blush pre-build QA approval: 2026-06-27 KST

Latest E7 cheek blush Build Gate execution: 2026-06-27 KST

Latest E7 cheek blush skin-fade feedback correction: 2026-06-27 KST

Latest E7 cheek blush milk-pink/three-stage correction and build gate: 2026-06-28 KST

Latest E7 cheek blush natural edge/color no-build correction: 2026-06-28 KST

## Current Session Snapshot

This snapshot is the default entry point for future Codex sessions. Read this section first, then lazy-load only the milestone-specific plan or research document needed for the current task.

Current boundary:

- Build target convention: future RN/Xcode real-device builds use `202268054(여서진)` via `--udid 00008110-0001794E0CD9801E`; do not use the old `위승철의 iPhone` label.
- Primary path: E7.03 v2.1 Boundary Engine Quality Experiment. Phase 0 Contract Reset is complete. Phase 1 Validation UI/Evidence Hygiene is implemented, JS-verified, real-device build/log smoke-verified, and user-provided iPhone screenshots confirm Clean / Compact HUD / Full Debug mode visibility. Phase 2 ARFace authored atlas MVP is implemented with manual vertex labels, topology/UV audit metadata, RN-selectable atlas variants, and preserved E3/E4/procedural baselines. Phase 2 is JS/lint verified, UnityFramework build/sync verified, and RN real-device build/install verified; runtime launch/visual evidence is still pending because the iPhone was locked during launch.
- Reference-driven UV atlas P2 synchronized capture is complete for input collection only. The validation app now exports clean frames plus same-moment ARFace mesh/UV/screen projection data, suppresses Unity face renderers/candidate masks/debug panels during capture, uses unique timestamped `pair_face_*` capture IDs for repeated attempts, and places the capture button away from the front camera. Three copied P2 capture folders are available under `evidence/e7-reference-atlas/capture_pairs/`; `pair_face_20260622T143334Z_03` is the recommended clean reference frame for user gold-mask drawing. No gold mask, UV back-projection, round-trip render, P3/P4 acceptance, MediaPipe runtime, or E7.4 work has started.
- A no-build A/B UV atlas fast gate was run from the two user-authored mask sets on `pair_face_20260622T143300Z_01` and `pair_face_20260622T143334Z_03`. After fixing the fast-gate back-projection sampling density, `lip`, `cheek`, and `eye` all produce continuation signals, so the offline decision is `continue`. This justifies preparing a small P8 runtime sweep candidate set, but it does not mark E7.03 Green.
- P8 runtime sweep build/log/visual evidence is collected for the default reference UV runtime path. The frozen runtime candidate set lives under `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/` with exactly 9 reference UV variants (`core`, `balanced`, `soft-wide` for `lip`, `cheek`, and `eye`), and RN/Unity support `rendererMode="e7-reference-uv-atlas"` / `candidateId="arface-reference-uv-atlas"`. The latest iPhone runtime stream confirms `lip`, `cheek`, and `eye` all sample ARFace UV probability atlases with `tracking_render`, mesh/UV counts `1220/6912/1220`, and `usedFallback=false`. User-provided Compact HUD photos confirm the masks appear face-attached, but all three regions remain Yellow because the silhouettes are still jagged/rough and the full white debug face surface is not Q3 overlay-ready makeup rendering.
- E7.03 smooth-mask validation app cleanup is accepted for the current optimization goal. Candidate/variant selection UI, old capture/reference controls, and stale visual debug paths were removed from the active app surface. The current runtime defaults to one `smooth-region-mask` path with three batched layers (`lip`, `cheek`, `eye`) and per-layer `enabled` flags. HUD status is inside the bottom `Regions` panel instead of floating over the face. User-provided acceptance screenshots are stored at `evidence/screenshots/e7-smooth-mask-accepted-2026-06-23/` and show visible, natural, subtle makeup on the face with Compact HUD reporting `active=lip,cheek,eye`, mesh `v=1220/i=6912/uv=1220`, FPS `52.2-60.2`, and recipe latency `36.0ms`. User visual feedback: the overall makeup is still hard to see, the eye boundary is somewhat lacking, and this validation stage ideally should make the covered region more unmistakable; however, the current result is accepted and the remaining visibility/boundary issue should be handled together with the next cosmetic expression/modeling work. This closes the current app cleanup/optimization goal by user acceptance, but it does not mark E7.03 Green or prove product-quality cosmetic rendering.
- 2026-06-25 iPhone AR black-screen hotfix: real-device logs on `202268054(여서진)` showed RN overlay rendering but Unity AR camera background failing with `Could not find shader named 'Unlit/ARKitBackground'`. Unity `GraphicsSettings.asset` now always includes ARKit background shaders, `UnityFramework.framework` was regenerated/synced with `bash scripts/build_m3_unityframework.sh`, and the RN app was rebuilt/installed/launched on the same iPhone with bundle id `com.yeoseojin.makeupar.validation202268054`. A package-local RN UnityView initialization patch was made durable through `postinstall`. Final visual/runtime confirmation of the AR screen after tapping `Start AR` is still pending because the last console session stayed on the RN start screen.
- 2026-06-25 E7 lip makeup validation v1 implementation/build-gate attempt is complete for a lip-only Yellow-risk renderer experiment. It adds `matte_lip`, `gloss_lip`, `full_lip`, `gradient_lip`, and `overline_lip` presets, keeps the RN batch to exactly three layers (`lip`, `cheek`, `eye`), generates `lip-style-atlas-v1` as an RGBA style atlas (`R=full`, `G=overline`, `B=gradient`, `A=gloss`), and extends Unity parser/material/shader fields for secondary color, coverage, finish, roughness, specular, gloss, and gradient controls. The latest atlas generator attempts single-frame screen-marking to ARFace UV back-projection first and keeps the old smooth-mask scaffold only as fallback. RN Jest/TypeScript/lint and pre-build `git diff --check` pass. After Build Gate approval, UnityFramework regeneration/sync succeeded with Unity 6000.3.18f1 from the Unity Hub downloads path, producing synced arm64 RN/package frameworks. Follow-up confirmed `202268054(여서진)` is the intended Yeoseojin iPhone and RN CLI maps it to `destination id=00008110-0001794E0CD9801E`. Signed RN install did not complete because no local provisioning profile/account is available for `com.yeoseojin.makeupar.validation202268054`; however, a device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` succeeded for that iPhone target and produced a `188M` app with embedded `108M` UnityFramework. No runtime visual style sweep was collected, and E7.3 remains Yellow.
- 2026-06-25 lip-first photo feedback no-build tuning: user-provided photos showed the eye/shadow path producing broad full-face coverage and the lip path spilling beyond the desired lip boundary. The RN validation UI now starts with `lip` as the only active region, keeps cheek/eye visible but disabled for this stage, exposes lip color buttons plus `Matte`/`Glow` finish buttons and an `Intensity` slider in HUD/Debug, and keeps the three-layer payload contract with cheek/eye disabled. Follow-up atlas regeneration used ARFace UV back-projection from the synchronized `pair_face_20260622T143334Z_03` lip marking; the full-channel footprint shrank from the old smooth-mask-style broad footprint to a projected bbox `left=207 top=292 right=302 bottom=347` with coverage `0.012253`. Unity lip atlas sampling now uses threshold `0.10`, base feather `0.16`, and recipe-clamped effective feather to keep the tighter projected atlas from rendering as a thin outline. Unity also culls `lip-style-atlas-v1` ARFace UV mesh triangles before shader rendering; the no-build guard predicts `2304` source triangles, `267` accepted triangles, and `2037` culled triangles for the current reference frame. The same verifier now projects the atlas back onto the reference frame and records screen round-trip IoU `0.841330`, outside-source ratio `0.140108`, and missing-source ratio `0.025015`; `lip_atlas_roundtrip_overlay.png` visually stays mouth-local on the reference frame. RN summaries surface `src`, `texGt8`, `texBbox`, `cull`, and `cullMode` from Unity `recipe_applied` diagnostics, and RN Jest/TypeScript/lint plus scoped `git diff --check` pass. This is no-build/static evidence only; runtime visual improvement is still pending and E7.3 remains Yellow.
- 2026-06-25 user-drawn mask iPhone packaging/install attempt: the user-provided drawings were extracted into grayscale/falloff Unity Resources (`lip-drawn-style-atlas-v1`, `lip-drawn-mask-v1`, `cheek-drawn-mask-v1`, `eye-drawn-mask-v1`) and wired as the current RN/Unity defaults while keeping runtime `DEFAULT_ACTIVE_REGIONS` lip-only. UnityFramework regeneration/sync succeeded for `TIMESTAMP=e7-user-drawn-mask-build-20260625-2038`, producing synced arm64 RN/package frameworks (`113M`, Unity `Data` `18M`). RN signed install to `202268054(여서진)` failed before install because no development provisioning profile exists for `com.yeoseojin.makeupar.validation202268054`; retries with `-allowProvisioningUpdates` failed because Xcode has no account for team `9G4K6N63MK`, and the local signing identity team `2834LYGHTB` is also not registered in Xcode Accounts. The same bundle id is already installed on the iPhone and was launched successfully via `devicectl`, but that fallback launch is not proof that the newly generated mask assets are on-device.
- 2026-06-25 runtime photo feedback UX/shader correction: user photos showed working matte lip color changes, slight lip-edge undercoverage, incorrect green output from the `Glow` finish, and disabled cheek/eye buttons. No-build corrections now make cheek/eye buttons clickable for placement validation while keeping default startup lip-only, change `gloss_lip` from `screen` to normal alpha blending with stronger specular/gloss parameters, add gloss-only white sheen in `SmoothRegionMask.shader`, and slightly expand/soften the lip style atlas path by changing threshold `0.10 -> 0.08`, atlas feather `0.16 -> 0.18`, and matte/gloss preset feather. A no-build preview/crop was generated under `evidence/e7-reference-atlas/user-drawn-mask-textures-v1/prebuild-verification/`; the first random sparkle attempt was removed because the crop preview showed square-artifact risk. The final threshold metrics show only a small lip expansion (`2814 -> 2922` active atlas pixels, `+3.84%`, bbox width `93 -> 95`), and `verify_lip_style_atlas.py` passes with the new threshold/feather. RN Jest, TypeScript, lint, and `git diff --check` pass. These changes are not yet real-device accepted because the fresh install path is still gated by signing/provisioning.
- 2026-06-25 runtime photo feedback build/install attempt: the full UnityFramework regeneration/sync path succeeded for `TIMESTAMP=e7-runtime-feedback-gloss-toggle-20260625-2137`, producing synced arm64 RN/package frameworks (`113M`, Unity `Data` `18M`). Signed RN install to `202268054(여서진)` did not reach install because Xcode currently sees the phone as `Offline / unavailable` and RN CLI cannot use destination `00008110-0001794E0CD9801E`. A signed generic iOS build also fails at signing because Xcode has no account for team `9G4K6N63MK` and no matching development profile for `com.yeoseojin.makeupar.validation202268054`; the project file team is `5947QNM627`, while the only local code signing identity is `Apple Development: du7030@icloud.com (2834LYGHTB)`. An unsigned RN+iOS compile/link with `CODE_SIGNING_ALLOWED=NO` succeeded and embedded the new UnityFramework, so the remaining blocker is phone availability/signing rather than RN/Unity compile correctness.
- 2026-06-25 matte/gloss/gradient no-build tuning: user screenshots showed matte still too glossy/subtle, gloss lacking narrow white shine, no explicit Gradient button, and makeup colors reading too faint. RN now exposes `Matte`, `Glow`, and `Gradient` lip finish buttons; `gradient_lip` uses high `gradientAmount`; lip/cheek/eye validation opacity/intensity are raised; `matte_lip` is stronger/rougher/non-specular; `gloss_lip` keeps selected color with stronger specular parameters. Unity material scaling is stronger for validation visibility, the shader makes matte flatter/darker, replaces broad gloss whitening with UV-local wet-line/fine streak highlights, and makes gradient density strongest at the inner lip. No-build checks pass: atlas guard, RN Jest (`11` tests), TypeScript, lint, Python compile, and `git diff --check`. This is not real-device acceptance; a fresh UnityFramework/RN install is still required because shader/material changes are native Unity content.
- 2026-06-25 matte/gloss/gradient UnityFramework-only build/sync: per user request, only `UnityFramework.framework` was regenerated and synced; RN CLI/Xcode install/launch was not run. `scripts/build_m3_unityframework.sh` succeeded with `TIMESTAMP=e7-matte-gloss-gradient-20260625-2230`, verified ARKit native links, built the arm64 UnityFramework target, copied Unity `Data`, and synced both RN and package-local framework paths. Artifact verification records both frameworks as `113M` with `18M` Unity `Data` and `Mach-O 64-bit dynamically linked shared library arm64`. This is Green for framework packaging only; real-device visual acceptance remains pending the user's manual Xcode run.
- 2026-06-26 E7 lip v2 boundary-first correction/runtime Vision wiring: RN/Unity pigment multiply changes exist as an implementation candidate, but user correction reset the active validation order to lip boundary first. The initial Apple Vision `vision-lip-boundary-v1` output is invalidated for decision use because the offline helper double-flipped the CGContext preview/mask coordinate space and used the wrong Y conversion default for this image pipeline, producing upside-down/oppositely placed previews. `scripts/e7_reference_atlas/run_vision_lip_boundary_gate.swift` now defaults to `coordinateMode="raw-y"`, removes the extra CGContext flip, writes `gateId="vision-lip-boundary-v2"`, and regenerated corrected evidence under `evidence/e7-reference-atlas/vision-lip-boundary-v2/` from the same four images. A same-frame boundary-only compare helper generated `boundary_compare/atlas_vs_vision_boundary_mouth_crop.png` and metrics on `pair_face_20260622T143334Z_03/frame.png`; corrected Vision v2 is close to the current atlas on that clean frame with IoU `0.878726`, Vision outside-atlas ratio `0.045300`, and atlas-missing-in-Vision ratio `0.083041`. A buildless blend preview generated `blend_preview/alpha_vs_pigment_multiply_sheet.png`; on the same frame, alpha overlay reduces lip luminance std ratio to `0.440485`, while pigment multiply keeps atlas ratio `0.991671` and Vision ratio `0.998322`, supporting the pigment-multiply direction for texture/detail preservation. Per user clarification, runtime Apple Vision is now wired as the lip boundary source candidate: RN sends `maskTextureId=lip-vision-boundary-v1`, Unity iOS plugin `E7VisionLipBoundary.mm` runs `VNDetectFaceLandmarksRequest`, `E7VisionLipBoundaryRuntime.cs` captures the current Unity frame transiently and emits `e7_vision_lip_boundary` with `rawCameraFrameStored=false` / `offDeviceUpload=false`, and `E3RegionMaskOverlay.cs` culls ARFace mesh triangles by the screen-space Vision `outerLips` minus `innerLips` polygon. Runtime diagnostics now surface `maskSource=apple_vision_runtime_lip_landmarks`, `boundaryRenderer=apple_vision_lip_landmark_screen_space`, Vision point counts, image size, and boundary age. No second camera session, Core ML, MediaPipe, native Metal, backend upload, or raw-frame storage was added. No-build verification now passes for RN Jest (`11` tests), TypeScript, RN lint, Swift typecheck, iOS Objective-C++ Vision plugin syntax, shader/static guards, and `git diff --check`. Unity batchmode import/compile after runtime Vision wiring was attempted at `evidence/logs/e7-vision-lip-runtime-unity-batchmode-20260626.log` but did not reach compile because Unity Licensing Client/headless licensing hung during startup; the earlier pre-runtime-Vision Unity batchmode compile remains recorded at `evidence/logs/e7-lip-v2-boundary-pigment-unity-batchmode-20260626.log`. UnityFramework regeneration/sync then succeeded with `TIMESTAMP=e7-vision-lip-runtime-20260626-0245`: Unity iOS export verified `Vision.framework in Frameworks`, the Xcode UnityFramework target recorded `** BUILD SUCCEEDED **`, linked with `-framework Vision`, and synced arm64 RN/package frameworks (`113M`, Unity `Data` `18M`). RN install using the stale `--device "위승철의 iPhone"` label failed because no such physical device exists; future RN/Xcode builds must target `202268054(여서진)` via `--udid 00008110-0001794E0CD9801E`. A UDID retry reached Xcode build/signing but failed before install because no development provisioning profile exists for `com.yeoseojin.makeupar.validation202268054`. No iPhone runtime boundary visual evidence or makeup acceptance has been collected yet.
- 2026-06-26 user screenshot feedback no-build correction for runtime Vision lip boundary: the runtime Vision path no longer relies on a 1x1 white placeholder mask after ARFace triangle culling. `E3RegionMaskOverlay.cs` now builds a per-frame screen-space texture from Vision `outerLips - innerLips`, writes diagnostics as `vision_screen_space_outer_minus_inner`, and the shader can sample `_MaskTex` in screen space via `_UseScreenSpaceMask` so pigment multiply is clipped per pixel instead of by coarse ARFace triangle inclusion. `E7VisionLipBoundaryRuntime.cs` also suppresses Unity region overlays and the Unity debug GUI during its transient `ReadPixels` capture, reducing self-feedback from the currently rendered lip color. This is a no-build implementation correction only; it is not iPhone visual acceptance, does not mark E7.3 Green, and still requires a fresh UnityFramework/RN install plus runtime photo/log evidence on `202268054(여서진)`.
- 2026-06-26 screenshot feedback build/sync attempt for runtime Vision screen-space lip mask: after Build Gate approval, no-build checks passed again for RN Jest (`11` tests), TypeScript, RN lint, scoped `git diff --check`, and Unity DLL-referenced Roslyn compile; full `git diff --check` still fails only on pre-existing dirty scene whitespace in `MakeupARFaceValidation.unity`. Unity/Hub/Licensing process cleanup found no stale Unity build blockers. `UnityFramework.framework` regeneration/sync succeeded with `TIMESTAMP=e7-vision-screen-mask-20260626-0315`, verified `Vision.framework`, `ARKit.framework`, and Unity ARKit native library links, built the arm64 UnityFramework target, and synced both RN/package framework paths (`113M`, Unity `Data` `18M`). `xcrun devicectl` confirmed `202268054(여서진)` is connected, but signed RN install by UDID failed before install because no development provisioning profile exists for `com.yeoseojin.makeupar.validation202268054` and automatic signing is disabled. A device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` succeeded and embedded the new UnityFramework, so compile/link/framework packaging is Green while signed install/runtime visual evidence remains blocked by provisioning. No iPhone runtime boundary photo/log acceptance was collected; E7.3 remains Yellow.
- 2026-06-26 final stabilization attempt after user-reported inverted/delayed Vision lip motion: default lip payloads now use the ARFace-attached `lip-drawn-style-atlas-v1` mask again instead of the live Apple Vision screen-space mask, so the active lip overlay follows ARFace mesh/UV rather than a delayed prior screen capture. The Apple Vision runtime path remains available only as a comparison/debug candidate: if `lip-vision-boundary-v1` is explicitly used, runtime Vision points are transformed `raw-y->flip-y` before screen-space culling/mask creation, stale Vision results expire after `300ms` instead of `1500ms`, capture cadence is reduced to `0.20s`, and Unity logs four transform candidate bboxes (`raw`, `flip-y`, `flip-x`, `flip-xy`) with `selected=flip-y` for device review. No second camera session, Core ML, MediaPipe runtime, native Metal path, backend upload, raw-frame storage, or product-readiness claim was added. After Build Gate approval, `UnityFramework.framework` regeneration/sync succeeded with `TIMESTAMP=e7-arface-lip-fallback-20260626-0349` and synced arm64 RN/package framework paths (`113M`, Unity `Data` `18M`). Signed RN install to `202268054(여서진)` reached Xcode signing but failed before install because no development provisioning profile exists for `com.yeoseojin.makeupar.validation202268054`; a device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` succeeded and embedded the new UnityFramework. Compile/link/framework packaging is Green, but no iPhone runtime photo/log acceptance was collected, so E7.3 remains Yellow.
- 2026-06-26 lip soft-SDF multilayer no-build rendering/stabilization pass: user-provided photos showed the active lip atlas still reading as a hard-edged sticker, with gloss/gradient appearing as one broad block, and the follow-up reference asked for one very thin white horizontal gloss line. `SmoothRegionMask.shader` now uses `_Feather`-scaled 13-tap multi-radius sampling (`FeatherTexelRadius`, near/far texel rings) plus `SoftMaskAlpha` / `CoreMaskAlpha` to split lip rendering into logical shader layers: weak base stain multiply, lip-center/inner density, edge feather band, and a separate gloss pass. The gloss pass now samples the atlas `A` channel as the single lower-center white horizontal line mask, with only `0.5%` lip-color tint, instead of estimating line position from UV or applying a broad glossy wash. `lip-drawn-style-atlas-v1` and `lip-style-atlas-v1` keep their full lip RGB channels, but their `A(gloss)` channel was reduced from a broad `89x20-22px` region to a narrow line (`57x4px` and `58x4px` at `A>8`). RN lip presets now favor higher feather and lower coverage for validation (`matte`, `gloss`, `full`, `gradient`, and `overline`), while Unity lip atlas culling uses a lower threshold (`0.025`) and minimum effective feather (`>=0.22`) so the shader has triangles available for the feather band. The Vision compare path now smooths new Vision results through a short transition, stores ARFace screen bounds at capture time, warps the retained Vision boundary to the current ARFace screen bbox, and bakes the Vision UV mask with a small soft-falloff splat (`vision_arface_uv_baked_outer_minus_inner_soft_falloff`) instead of hard `3x3` pixels so the shader receives a feather-friendly input. HUD/diagnostics expose Vision smoothing/transition fields, `face-local-warp` coordinate status, Vision-capture face motion diagnostics, render-time face motion diagnostics (`visionBoundaryFaceMotionScore`, center shift px, scale delta, risk), and `maskDiag=vision_arface_uv_baked_outer_minus_inner_soft_falloff` so large movement compensation and soft bake status can be judged from iPhone logs. Runtime diagnostics now expose `lipRenderLayerMode=soft_sdf_logical_multilayer` and `glossHighlightMode=single_thin_soft_white_wet_line` only for `gloss_lip`; RN tests verify matte events report `gloss=none`, recipe/render events surface `visionMotion=0.276/medium_face_motion`, `maskDiag=vision_arface_uv_baked_outer_minus_inner_soft_falloff`, and Vision boundary events surface capture smoothing as `smooth=temporal_smooth_transition|large_face_motion_smooth`, `t=0.42/160`, `faceLocal=true`, and `motion=0.390/large_face_motion`. The offline guard now verifies feather `0.22` produces shader-approx near/far radii `3.447px` / `6.377px`, the edge feather band directly with `edgeBandMean=0.5947`, detail preservation with soft matte/gradient luma correlations `0.9608` / `0.9721`, and the soft-white line with `wetLineActivePixels=493`, `wetLineMeanLumaBoost=0.1968`, plus a structural shape guard (`bbox=119x5px`, `aspect=23.8`, `height/lip=0.040`, `width/lip=0.509`, `componentCount=1`) so broad, thick, or fragmented gloss cannot pass silently. Latest checks passed: soft-SDF verifier, atlas guard, runtime-log verifier synthetic pass/expected-fail, RN Jest (`13` tests), TypeScript, RN lint, scoped `git diff --check`, shader/static guard, preview regeneration, Python compile with generated-cache cleanup, and Unity batchmode import/compile in `evidence/logs/e7-soft-sdf-a-channel-line-unity-batchmode-20260626.log` with both lip style atlas textures and `SmoothRegionMask.shader` reimported, `CompileScripts: 3202.976ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. This is no-build/static validation evidence only; a UnityFramework/RN build plus iPhone photo/log review is still required before visual acceptance, and E7.3 remains Yellow.
- 2026-06-26 Vision ARFace UV-bake no-build stabilization pass: the retained `lip-vision-boundary-v1` debug/compare path no longer samples its dynamic Vision mask in live screen UV. When explicitly selected, Unity now transforms/stabilizes the Vision boundary, projects the current ARFace mesh into screen space, barycentrically back-projects the `outerLips - innerLips` area into a transient `512x512` ARFace UV mask, and sets `_UseScreenSpaceMask=0` so `SmoothRegionMask.shader` samples the baked mask in face UV space. Diagnostics now identify the path as `apple_vision_lip_landmark_arface_uv_baked` / `vision_arface_uv_baked_outer_minus_inner_soft_falloff`, append `->arface-uv-bake` to `visionBoundaryCoordinateMode`, and log `vision_lip_boundary_arface_uv_bake` with candidate triangles, hit triangles, hit samples, active UV pixels, sample stride, and `softSplatRadius=3`. The default active lip renderer remains `lip-drawn-style-atlas-v1`; Vision remains debug/compare only. Checks passed: scoped `git diff --check`, Unity DLL-referenced Roslyn compile for all six runtime scripts, Unity batchmode import/compile (`evidence/logs/e7-vision-arface-uv-bake-diag-unity-batchmode-20260626.log`) with `CompileScripts: 2965.738ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`, RN Jest (`11` tests), TypeScript, and RN lint. This is still no-build/static evidence only; no UnityFramework/RN install or iPhone visual acceptance was collected, so E7.3 remains Yellow.
- 2026-06-26 soft-SDF multilayer offline preview evidence: `scripts/e7_reference_atlas/make_lip_soft_sdf_multilayer_preview.py` now creates preview-only evidence from the clean reference frame and `lip_atlas_roundtrip_mask.png`. The generated sheet compares original, hard alpha sticker-risk baseline, soft-SDF matte, soft-SDF gradient, single thin soft-white horizontal-line gloss, and layer diagnostic (`R=soft`, `G=edge`, `B=wet-line`) under `evidence/e7-reference-atlas/lip-style-atlas-v1/soft_sdf_multilayer_preview_20260626/`. `scripts/e7_reference_atlas/verify_lip_soft_sdf_multilayer.py` guards the shader/RN/Unity contract for soft-SDF layers, Vision ARFace UV baking, Vision freshness/transition/face-bbox warp tokens, edge feather band presence, one-line soft-white gloss, and luma/detail checks. Metrics show the hard alpha baseline suppresses lip luminance detail (`lumaStdRatio=0.4046`) while soft-SDF matte/gradient preserve more detail (`0.8313` / `0.8899`) and correlation (`0.9608` / `0.9721`), the edge feather band is active (`edgeBandMean=0.5947`), and the wet-line is narrow, visible, and structurally one horizontal line (`wetLineActivePixels=493`, `wetLineMeanLumaBoost=0.1968`, `lumaCorrelation=0.8531`, `bbox=119x5px`, `aspect=23.8`, `height/lip=0.040`, `width/lip=0.509`, `componentCount=1`). This is offline approximation evidence only, not Unity runtime or iPhone acceptance; E7.3 remains Yellow.
- 2026-06-26 soft-SDF completion audit evidence: `evidence/logs/e7-soft-sdf-completion-audit-20260626.md` maps the user-requested smoothing, transition, large-motion compensation, ARFace/UV stabilization, base stain, inner gradient, edge feather, and thin gloss layers to current source and buildless verification evidence. The audit decision remains `buildless/static-audit`; real iPhone visual acceptance is still missing until a Build Gate-approved UnityFramework/RN install/log/photo pass is collected on `202268054(여서진)`.
- 2026-06-26 soft-SDF runtime acceptance checklist: `evidence/logs/e7-soft-sdf-runtime-acceptance-checklist-20260626.md` freezes the Build Gate path, required Compact HUD photos/log fields, pass/fail notes, out-of-scope items, and post-run log verifier command for the next approved iPhone validation pass. RN `rn_texture_recipe_batch_post` now logs `focusMaskTextureId` and `lipMaskTextureId`, and RN Jest guards that the default lip post remains `lip-drawn-style-atlas-v1` rather than `lip-vision-boundary-v1`. Unity/RN `recipe_applied` diagnostics now expose `maskSoftSampleMode=feather_scaled_13tap_near_far`, `maskFeatherNearRadiusPx`, and `maskFeatherFarRadiusPx`, with RN Compact HUD summaries showing `soft=... featherPx=near/far`. `scripts/e7_reference_atlas/verify_soft_sdf_runtime_evidence.py` now checks runtime diagnostic fields for soft-SDF layer mode, wide feather sample mode/radius, the first active lip recipe staying on ARFace-attached `lip-drawn-style-atlas-v1`, glow wet-line, matte no-gloss, required lip styles, Vision ARFace UV bake status, and Vision face-motion diagnostics; it fails by default if the first active lip recipe is the delayed Vision boundary path, or if logged wide feather radius is too narrow. The main soft-SDF guard reports `runtimeVerifier=guarded` so this post-run verifier stays part of the buildless contract. This keeps the latest buildless shader/diagnostic evidence separate from the still-missing runtime visual acceptance.
- 2026-06-26 soft-SDF multilayer UnityFramework build/sync: after Build Gate approval, pre-build checks passed for RN Jest (`13` tests), TypeScript, RN lint, scoped `git diff --check`, and the soft-SDF verifier (`near=3.447px`, `far=6.377px`, `edgeBandMean=0.5947`, one wet-line component). `UnityFramework.framework` regeneration/sync then succeeded with `TIMESTAMP=e7-soft-sdf-multilayer-20260626-1322` using the Unity Hub downloads binary at `~/Library/Application Support/UnityHub/downloads/6000.3.18f1/Unity.app/Contents/MacOS/Unity`. The export verified `Vision.framework`, `ARKit.framework`, and `MetalPerformanceShaders.framework`; the Xcode `UnityFramework` target recorded `** BUILD SUCCEEDED **`; and both RN/package framework paths were synced as arm64 Mach-O frameworks (`113M`, Unity `Data` `18M`). Evidence logs are `evidence/logs/m3-repro-unity-export-e7-soft-sdf-multilayer-20260626-1322.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-soft-sdf-multilayer-20260626-1322.log`, and `evidence/logs/m3-repro-artifact-verification-e7-soft-sdf-multilayer-20260626-1322.log`. This proves framework packaging only; no RN install/launch, iPhone photo/log review, or E7.3 visual acceptance was collected, so E7.3 remains Yellow.
- 2026-06-26 matte/gradient/gloss user-photo retune: user iPhone screenshots after the soft-SDF build showed the color was too faint, `matte_lip` and `gradient_lip` were visually too similar, and `gloss_lip` still read as an awkward white sticker line. The latest pass restores stronger matte/gradient pigment while keeping soft edges, changes `gradient_lip` to the requested logical composition (`baseMultiply + innerGradientTint + softEdgeFeather`) with a weak outer wash and stronger center/inner tint, and changes gloss from the prior white-line diagnostic to `tinted_soft_lower_wet_line` with much lower additive boost. RN presets, Unity material alpha/brightness scaling, shader layer weights, preview generation, and the soft-SDF verifier were updated. Checks passed: RN Jest (`13` tests), TypeScript, RN lint, atlas guard, soft-SDF verifier (`softMatteLumaStdRatio=0.7951`, `softGradientLumaStdRatio=1.2470`, `softGradientLumaCorrelation=0.9364`, `wetLineMeanLumaBoost=0.0553`, one wet-line component), scoped `git diff --check`, preview regeneration, and Unity batchmode import/compile in `evidence/logs/e7-matte-gradient-gloss-retune-unity-batchmode-20260626.log` with `CompileScripts: 3287.457ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`.
- 2026-06-26 matte/gradient/gloss retune UnityFramework build/sync and RN signing gate: after Build Gate approval, `scripts/build_m3_unityframework.sh` succeeded with `TIMESTAMP=e7-matte-gradient-gloss-retune-20260626-1435`, using the Unity Hub downloads binary and `BUILD_LOG_MODE=full`. Unity export verified `ARKit.framework`, `Vision.framework`, and `MetalPerformanceShaders.framework`; Xcode `UnityFramework` recorded `** BUILD SUCCEEDED **`; both RN and package-local UnityFramework paths were synced as arm64 Mach-O frameworks (`113M`, Unity `Data` `18M`). Build time was `real 134.45s`, reusing the existing Unity `Library/` and Xcode DerivedData cache. RN signed build/install to `202268054(여서진)` / `00008110-0001794E0CD9801E` reached Xcode but failed at the known provisioning gate: no profile for `com.yeoseojin.makeupar.validation202268054` with automatic signing disabled. A device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` succeeded and embedded `UnityFramework.framework` into `MakeupARValidation.app`, proving compile/link/package correctness but not install/runtime visual acceptance.
- 2026-06-26 lip finish multi-agent handoff: user runtime screenshots after the latest retune establish `matte_lip` as accepted/frozen for E7.3 validation, while `gradient_lip` and `gloss_lip` remain open. `IMG_5262.PNG` is the matte freeze reference. `IMG_5264.PNG` shows gradient still has too-clear center/perimeter separation compared with the target reference screenshot, and `IMG_5263.PNG` shows gloss mostly lightens pigment instead of creating localized wet shine. `docs/roadmaps/active/E7_LIP_FINISH_NEXT_HANDOFF_KO.md` now records the multi-agent analysis, root causes, ranked next work, evidence bar, and next-session prompt. Key technical finding: gradient should replace thresholded `gradientMask/fullCore` composition with a continuous density/ramp; gloss should separate base pigment preservation from localized highlight and may need a better atlas `A` mask or view/normal-aware additive pass. Do not touch `matte_lip` unless explicitly reopened.
- 2026-06-26 gradient/gloss continuous-ramp no-build retune: per handoff, `matte_lip` stayed frozen and unchanged. Multi-agent analysis confirmed the gradient fix should be shader-first and gloss should preserve red base before boosting the wet-line; normal/view-direction shading is deferred unless this validation-level pass still looks flat on-device. `SmoothRegionMask.shader` now keeps the legacy matte/full/overline inner density separate, adds `atlasGradientGuide`, `continuousGradientDensity`, `midGradientRamp`, `innerGradientDensity`, and `gradientTintMix` for `gradient_lip`, and rejects the old thresholded `max(gradientMask, centerDensity) * fullCore` gradient path in the guard. `gloss_lip` now has stronger red pigment base, a modest gloss cap boost, lower white mix (`0.24`) in the additive wet-line, and RN/Unity gloss-only preset/material scaling retune; the atlas RGB/A mask assets were not regenerated. Offline preview and guard now check gradient transition width, inner/outer strength ratio, ramp adjacent delta, center boundary jump, wet-line shape, wet-line luma boost, and gloss red-base preservation. Checks passed: `git diff --check`, atlas guard, soft-SDF verifier (`softGradientLumaStdRatio=1.3368`, `softGradientLumaCorrelation=0.9472`, `gradientTransitionWidthToLipWidth=0.9744`, `gradientInnerOuterStrengthRatio=3.2299`, `gradientRampMaxAdjacentDeltaP95=0.0110`, `gradientCenterBoundaryJump=0.1243`, `wetLineMeanLumaBoost=0.0767`, `glossRedBasePreservationRatio=1.0452`), RN Jest (`13` tests), TypeScript, RN lint, Python compile, preview regeneration, and Unity batchmode import/compile in `evidence/logs/e7-gradient-gloss-continuous-ramp-unity-batchmode-20260626.log` with `CompileScripts: 3074.723ms`, `SmoothRegionMask.shader` reimported, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. No UnityFramework/RN real-device build, install, or runtime visual acceptance was run; E7.3 remains Yellow pending Build Gate approval and iPhone photo/log review.
- 2026-06-26 gradient_lip boundary-risk no-build hard reset: user correctly challenged the previous same-color proof because a separate gradient pigment curve could still create an internal layer boundary, while reducing it could make gradient drift away from frozen matte. The active correction removes the gradient-specific color/layer composition from `SmoothRegionMask.shader`: `gradient_lip` now uses the same `matteReferencePigmentColor` as `matte_lip`, and changes only `matteReferenceMaskStrength` through a smooth `singleGradientDensity` scale (`matteDerivedGradientStrength`). RN/Unity material parameters for gradient now match matte brightness/alpha scaling (`brightnessScale=0.9`, alpha scale `0.72..0.92`), with gradient coverage/roughness aligned to matte (`coverage=0.94`, `roughness=1`). The expected AR preview was regenerated at `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_20260626/ar_runtime_expected_sheet.png`; it now records `gradientMattePigmentDeltaMax=0.0` and `gradientMatchesMattePigment=true`, proving the buildless path does not use a different gradient color. Boundary contrast is lower (`innerOuterPigmentRatio=1.1797`, `centerLineMaxJump=0.0279`), but the preview verdict remains `expected_ar_preview_review` because outer-source leak is still above the current guard (`outsideSourceDilatedRatio=0.1439`, similar to matte's broad footprint). Checks passed: Python compile, AR expected preview regeneration, soft-SDF preview regeneration, soft-SDF verifier, RN Jest (`13`), TypeScript, RN lint, and `git diff --check`. This is a buildless expected-render correction only; no UnityFramework/RN build, install, or iPhone visual acceptance was collected.
- 2026-06-26 gradient_lip matte-derived Build Gate / RN device gate: after user approval, Unity batchmode import/compile passed in `evidence/logs/e7-gradient-matte-derived-buildgate-unity-batchmode-20260626.log` with `CompileScripts: 4307.529ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. `scripts/build_m3_unityframework.sh` then succeeded with `TIMESTAMP=e7-gradient-matte-derived-20260626-1805` and `BUILD_LOG_MODE=full`, verifying `ARKit.framework`, `Vision.framework`, and `MetalPerformanceShaders.framework`; Xcode `UnityFramework` recorded `** BUILD SUCCEEDED **`; both RN and package-local frameworks synced as arm64 Mach-O frameworks (`114M`, Unity `Data` `19M`). `xcrun devicectl` saw `202268054(???)` / `00008110-0001794E0CD9801E` as `unavailable`, and RN CLI could not find that UDID before Xcode returned error code `70`, so no signed install/launch/runtime visual evidence was collected. A fallback unsigned generic iOS build succeeded with `CODE_SIGNING_ALLOWED=NO`, producing a `194M` `MakeupARValidation.app` and embedding `UnityFramework.framework` (`114M`, Unity `Data` `19M`), which proves RN compile/link/package correctness but not on-device gradient acceptance.
- 2026-06-26 gradient_lip user acceptance/freeze: user-provided iPhone screenshot `IMG_5268.PNG` confirms the matte-derived same-pigment `gradient_lip` result is accepted for the current E7.3 lip-finish validation. The retained evidence is `evidence/screenshots/e7-gradient-lip-accepted-2026-06-26/IMG_5268.PNG` (`1170x2532`). The HUD shows `active=lip`, `focus=lip`, `finish gradient_lip`, RED `#C21F3A`, intensity `100%`, Tracking, mesh `v=1220/i=6912/uv=1220`, FPS `59.9`, frame `16.7ms`, and latency `21.0ms`. Freeze rule: do not modify `gradient_lip` again unless the user explicitly reopens it. This accepts only the current gradient lip visual for validation; it does not mark full E7.3 Green, product-quality makeup, `gloss_lip`, cheek, or eye as accepted.
- 2026-06-26 gloss_lip clustered-tint no-build retune: user-provided `IMG_5269.PNG` plus the glossy tint reference showed current `gloss_lip` preserved some tint but lacked the short wet shine cluster. `matte_lip` and `gradient_lip` remain frozen and unchanged. The active gloss path now keeps the base lip pigment in the first multiply pass, changes the style-atlas A channel from a single lower line to short clustered highlight strokes (`A>8` pixels `239`, bbox `x=231..284/y=305..334`, max alpha `154` on `lip-drawn-style-atlas-v1`), and the gloss-only additive pass separates `glossSharpMask` from `glossHaloMask`. The blend is still `Blend One One`, but color is scaled through `glossScreenLift = 1 - tintedGlossColor * 0.72` with pink gloss color, so the shader adds reflected light over tint instead of mixing/replacing the lip with white. Unity diagnostics now report `glossHighlightMode=tinted_clustered_wet_gloss`; runtime verifier and RN HUD tests use the same string. Checks passed: bundled Python compile, soft-SDF preview regeneration, debug preview outputs for base/mask/halo/final, soft-SDF verifier with direct atlas A-channel guard (`activePixelsGt8=239`, `softComponentCountGt48=7`, `coreComponentCountGt96=4`, `upperSoftPixelsGt48=21`, `lowerCorePixelsGt96=77`, `wetLineMeanLumaBoost=0.0591`, `wetLineComponentCount=3`, `glossRedBasePreservationRatio=1.0448`), AR expected preview regeneration (`gloss meanAdditiveLuma=0.0728`, max `0.2053`, red base ratio `1.0365`, still `expected_ar_preview_review` because ARFace footprint/outside-source is not runtime acceptance), RN Jest (`13`), `npx tsc --noEmit`, RN lint, `git diff --check`, and Unity batchmode import/compile in `evidence/logs/e7-gloss-cluster-unity-batchmode-20260626.log` with `CompileScripts: 4504.031ms`, changed lip atlas textures and `SmoothRegionMask.shader` reimported, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. No UnityFramework/RN real-device build, install, iPhone photo/log review, or gloss visual acceptance was collected; E7.3 remains Yellow.
- 2026-06-26 gloss_lip runtime Build Gate packet: `evidence/logs/e7-gloss-cluster-runtime-buildgate-20260626.md` records the build question, primary path, compare-only paths, validation contract, evidence matrix, commands, and out-of-scope items for the next approved real-device gloss validation. This is a gate packet only; no UnityFramework/RN device build or runtime visual acceptance has been run for this checkpoint.
- 2026-06-26 gloss_lip clustered-tint UnityFramework build/sync and RN signing gate: after user Build Gate approval, buildless gates re-ran and passed for the current gloss-cluster source: soft-SDF verifier with `glossAtlasMetrics`, RN Jest (`13`), `npx tsc --noEmit` (local RVM `ps` warning only), RN lint, `git diff --check`, and Unity batchmode import/compile in `evidence/logs/e7-gloss-cluster-buildgate-unity-batchmode-20260626.log` with `CompileScripts: 990.195ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. Unity/Hub/Licensing cleanup found no stale process. `scripts/build_m3_unityframework.sh` then succeeded with `TIMESTAMP=e7-gloss-cluster-20260626` and `BUILD_LOG_MODE=full`: Unity export verified `ARKit.framework`, `Vision.framework`, and `MetalPerformanceShaders.framework`; Xcode `UnityFramework` recorded `** BUILD SUCCEEDED **`; RN and package-local frameworks were synced as arm64 Mach-O frameworks (`114M`, Unity `Data` `19M`); build time was `real 136.04s`. `xcrun devicectl` saw `202268054(???)` / `00008110-0001794E0CD9801E` as `unavailable`; RN CLI could not find the UDID in available devices, still invoked `xcodebuild -destination id=00008110-0001794E0CD9801E`, and failed at the known provisioning gate: no iOS App Development profile for `com.yeoseojin.makeupar.validation202268054` with automatic signing disabled (`error code 65`). Evidence logs: `evidence/logs/m3-repro-unity-export-e7-gloss-cluster-20260626.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-gloss-cluster-20260626.log`, `evidence/logs/m3-repro-artifact-verification-e7-gloss-cluster-20260626.log`, and `evidence/logs/e7-gloss-cluster-rn-ios-device-20260626.log`. No install, launch, runtime diagnostics, user screenshot, or gloss visual acceptance was collected; `gloss_lip` remains unaccepted and E7.3 remains Yellow.
- 2026-06-26 gloss_lip matte-base wet-sheen no-build correction: user-provided runtime screenshots `gloss_lip_rejected_1.jpg` and `gloss_lip_rejected_2.jpg` are retained under `evidence/screenshots/e7-gloss-lip-rejected-2026-06-26/` and show the clustered gloss candidate reading as small unnatural pieces rather than clear gloss. The active correction supersedes `tinted_clustered_wet_gloss`: `gloss_lip` now uses the same matte-derived pigment base as `matte_lip`, the RN gloss preset keeps matte-like tint/coverage/feather while using moderate `specular=0.62` / `glossBoost=0.56`, Unity diagnostics report `glossHighlightMode=matte_base_wet_sheen`, Unity material brightness for gloss matches matte (`0.9`), the atlas A channel is a single lower-center soft sheen (`A>8=661`, bbox `x=223..288/y=320..333`, one soft/core component, no upper pieces), and the additive gloss pass uses pink screen-lift reflection over the matte base instead of multiple white chunks. Checks passed: soft-SDF preview regeneration, AR expected preview regeneration, soft-SDF verifier (`wetLineComponentCount=1`, `wetLineMeanLumaBoost=0.0667`, `glossRedBasePreservationRatio=0.9983`), AR expected gloss metrics (`componentCount=1`, `meanAdditiveLuma=0.0503`, `maxAdditiveLuma=0.0765`, `redBasePreservationRatio=1.0`; overall verdict still `expected_ar_preview_review` because footprint/gradient guards are conservative buildless review checks), RN Jest (`13`), `npx tsc --noEmit` (local RVM `ps` warning only), RN lint, `git diff --check`, Python compile, and Unity batchmode import/compile in `evidence/logs/e7-gloss-matte-base-sheen-unity-batchmode-20260626.log` with `CompileScripts: 5393.758ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. No UnityFramework rebuild, RN install, or runtime visual acceptance has been run for this correction; `matte_lip` and accepted `gradient_lip` remain frozen.
- 2026-06-26 gloss_lip 1px edge-blended lower-lip specular mask correction: user clarified the gloss goal is not whitening the lip color, but keeping the tint color while adding only short, thin partial reflections like a wet gel coating; the latest follow-up clarified that the highlight should be smudged into the surrounding lip instead of merely made less white. The active no-build correction keeps the matte-derived tint base, raises RN gloss specular parameters to `specular=0.78`, `specularPower=36`, `glossBoost=0.68`, keeps the atlas A-channel as a single lower-lip 1px specular streak (`A>8=35`, bbox `x=238..272/y=336..336`, one soft/core component, max alpha `253` on `lip-drawn-style-atlas-v1`), and adjusts the gloss pass so the specular core has softer edges while the broad halo is constrained enough not to fragment or brighten the inner mouth line. Checks passed: soft-SDF preview regeneration, soft-SDF verifier (`wetLineActivePixels=237`, `wetLineHeightToLipHeight=0.0238`, `wetLineMeanLumaBoost=0.0679`, `wetLineComponentCount=1`, `glossRedBasePreservationRatio=0.9986`), AR expected preview regeneration (`componentCount=1`, `heightToLipHeight=0.0968`, `meanAdditiveLuma=0.1352`, max `0.1453`, `redBasePreservationRatio=1.0`, verdict still `expected_ar_preview_review`), RN Jest (`13`), TypeScript, RN lint, Python compile, `git diff --check`, and Unity batchmode import/compile in `evidence/logs/e7-gloss-1px-edge-blend-unity-batchmode-20260626.log` (`CompileScripts: 4313.752ms`, no `Shader error` / `error CS`, `Exiting batchmode successfully now!`). No UnityFramework rebuild, RN install, or runtime visual acceptance has been run for this edge-blended 1px correction yet; `matte_lip` and accepted `gradient_lip` remain frozen.
- Phase 2 adds the runtime-selectable `e7-arface-authored-atlas` candidate for `lip`, `cheek`, and `eye`. This is validation-only manual labeling plus evidence metadata, not face parsing, product segmentation, product-quality makeup, or AI/backend work.
- E7.03 v2.1 now has phase-wide time-saving rules, not only a P2 shortcut: prefer no-build data/registry/offline changes first, JS-only validation second, one real-device install for many candidate/variant/region sweeps third, and UnityFramework/Xcode rebuild only when renderer logic, schema, shader/material contract, package/framework sync, or native integration changes.
- E7.3 visual evidence has been reviewed from the user-provided screen recording. ARFace mesh/UV is live on-device, but the current procedural candidate is not visually acceptable for `lip` or `eye`; `cheek` lacks a clean E7 candidate sample. Overall E7.3 remains Yellow because the ARFace substrate is viable, but the current candidate masks block E7.4.
- The old E7.03 Green bar of "better than E3/E4 baseline" or "demo-plausible" is superseded. E7.03 Green now requires Q3 overlay-ready validation for `lip`, `cheek`, and `eye`, with runtime/visual evidence and region-separated scoring.
- Conservative alternative: run additional M7 re-entry verification if the team wants to replace the accepted M7 Yellow lifecycle risk with formal 3-cycle evidence.
- Renderer path: if the accepted M7 risk remains acceptable, stay in validation/hardening mode inside E7.3; do not enter E7.4/E7.5/E7.6 until E7.03 v2.1 evidence supports the next boundary, or until the team explicitly accepts the remaining E7.3 risk.
- Workspace/share state: source-first cleanup is complete for the current handoff pass. Current checked size is `24M` total (`.git` about `22M`; source/doc/script working tree about `2M`). Root active docs are limited to `AGENTS.md`, `TECH_VALIDATION_TEST_PLAN.md`, and `TECH_VALIDATION_RESULT.md`; active E7 plans now live under `docs/roadmaps/active/`; team onboarding/share requirements live in `docs/runbooks/TEAM_SHARE_REQUIREMENTS_KO.md`; ignored evidence/build/dependency/editor outputs are expected to be absent after `scripts/cleanup_local_generated.sh --profile share`.

Current status:

- M0-M6: Green.
- M7: Yellow / skipped by decision / risk accepted. Do not promote M7 without formal 3-cycle re-entry evidence.
- M8 foundation closeout: Yellow overall because M7 remains risk-accepted.
- E1 AR Alignment: Green for validation.
- E2 Trackable Lifecycle Diagnostics: Green.
- E3 Region Mask: Green for validation only; masks are broad debug masks.
- E4 Texture Sample: Green for validation only; texture samples are procedural/debug quality.
- E5 AI Feature Readiness Snapshot: Green for no-inference handoff; decision recording was explicitly waived only for E5.
- E6 Engine Decision: Yellow; continue the RN + Unity + ARKit direction, but do not claim product-v1 readiness.
- E7.0/E7.1 Preflight: Green for preflight only.
- E7.2 Baseline Instrumentation: Green for baseline instrumentation; decision recording was explicitly waived only for E7.2.
- E7.3 Region Precision: Yellow overall / blocked from E7.4. Legacy procedural E7 video review remains `lip` Red, `cheek` Yellow / insufficient E7-candidate sample, and `eye` Red. P8 reference UV runtime sweep improves attachment for all three regions, but current P8 region decisions are `lip` Yellow, `cheek` Yellow, and `eye` Yellow pending silhouette/material cleanup and scenario-specific motion review.
- E7.03 P2 synchronized reference capture: captured / ready for user gold-mask authoring input; this does not change E7.3 region G/Y/R or mark E7.03 Green.
- E7.03 A/B UV atlas fast gate: Continue / P8 Build Gate already consumed for the first runtime sweep. `lip`, `cheek`, and `eye` all showed useful same-frame and cross-frame UV transfer; this remains offline viability evidence, not runtime Green evidence.
- E7.03 P8 runtime sweep: build/install/runtime-log/representative-photo evidence collected after Build Gate approval. The runtime path is viable, Compact HUD is visible, and Clean intentionally hides the mask, but no region is Green yet.
- E7.03 smooth-mask validation app cleanup: accepted for the current validation app optimization goal. The accepted screenshots show a usable, non-obstructed Compact HUD and a natural-enough visible overlay for this validation step. User feedback records that the overall makeup is still subtle/hard to see and the eye boundary needs improvement, but this is acceptable for now and should be resolved with cosmetic expression/modeling. Full E7.3 Region Precision remains Yellow because Q3 overlay-ready motion/scenario evidence is still incomplete.
- E7 lip makeup validation v1: implementation, UnityFramework build/sync, device-targeted unsigned RN Xcode build, follow-up lip-first no-build tuning, offline ARFace UV back-projected lip atlas regeneration, lip atlas mesh culling, and RN runtime-diagnostic surfacing checks are complete for `202268054(여서진)`; signed RN real-device install/runtime remains blocked by local Xcode account/provisioning state. This is lip-only validation renderer work under accepted E7.3 Yellow risk, not E7.3 Green or product-quality makeup evidence.
- E7 user-drawn mask phone check: UnityFramework packaging/sync is complete and the existing installed app can be launched on `202268054(여서진)`, but the fresh RN install did not complete due to signing/provisioning. Do not use the fallback launch as visual acceptance for the newly generated user-drawn mask build.
- E7 runtime photo feedback correction: no-build implementation is complete for gloss color drift, lip-edge slight expansion, and cheek/eye placement-toggle access. A fresh signed install is still required before visual acceptance.
- E7 runtime photo feedback build/install: UnityFramework sync and unsigned RN compile/link are Green, but signed install/launch is blocked because `202268054(여서진)` is offline/unavailable to Xcode and the local Xcode account/provisioning setup does not match `com.yeoseojin.makeupar.validation202268054`.
- E7 matte/gloss/gradient tuning: no-build implementation is complete for stronger makeup visibility, flatter matte, white wet-line gloss highlights, and the new Gradient lip finish UI. Real-device acceptance is pending a fresh UnityFramework/RN install.
- E7 matte/gloss/gradient UnityFramework-only build/sync: Green for framework artifact packaging. The fresh UnityFramework is synced into RN and package-local paths for manual Xcode install, but no on-device runtime result has been collected from this build.
- E7 runtime Vision / lip atlas stabilization: default lip rendering has been moved back to the ARFace-attached `lip-drawn-style-atlas-v1` path to remove live screen-space Vision lag/inversion from the active user path. The Vision screen-space path is retained only as an explicit debug/compare candidate with `raw-y->flip-y` transform, stricter freshness, and 4-way transform bbox logging. UnityFramework build/sync and unsigned RN device-targeted compile/link are Green for this final fallback/transform change. Signed install/runtime evidence is still blocked by local provisioning for `com.yeoseojin.makeupar.validation202268054`, so E7.3 remains Yellow.
- E7 lip soft-SDF multilayer pass: implementation and UnityFramework build/sync are complete for softer atlas edges, logical base/inner/edge/gloss layers, single thin lower wet-line gloss, runtime render-mode diagnostics, Vision temporal transition, Vision face-local bbox warp, and Vision debug ARFace UV baking. Runtime visual acceptance is pending RN install/launch plus iPhone photo/log review.
- E7 matte/gradient/gloss user-photo retune: implementation and UnityFramework build/sync are complete for stronger matte pigment, more distinct Korean-style gradient (`weak outer base + stronger inner tint + soft feather`), and less-white tinted gloss. Signed RN install/runtime visual evidence is still blocked by local provisioning for `com.yeoseojin.makeupar.validation202268054`; unsigned device-targeted RN/Xcode compile/link is Green.
- E7 lip finish handoff decision: `matte_lip` and `gradient_lip` are frozen/complete for current validation; `gloss_lip` is not accepted yet. Continue from `docs/roadmaps/active/E7_LIP_FINISH_NEXT_HANDOFF_KO.md` and avoid modifying matte or gradient unless explicitly reopened.
- E7 gloss_lip user-drawn lower-lip specular cluster tweak: the clustered gloss candidate is rejected by user screenshots, the broader matte-base wet-sheen candidate is superseded, and the later single 1px streak is now replaced by the user-drawn lower-lip cluster. The active `lip-drawn-style-atlas-v1` / `lip-style-atlas-v1` A channel removes the upper stray piece and moves the lower piece up 1px (`A>8=57`, bbox `x=239..267/y=335..339`, max alpha `188`, `upperSoftPixelsGt48=0`, `lowerCorePixelsGt96=21`). AR expected preview regenerated at `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_20260626/expected_gloss_lip.png` with `componentCount=3`, bbox `left=516 top=1249 right=598 bottom=1272`, `meanAdditiveLuma=0.1097`, max `0.1453`, and `redBasePreservationRatio=1.0`; `scripts/e7_reference_atlas/verify_lip_soft_sdf_multilayer.py` passes with the updated cluster guard. RN checks, Python compile, `git diff --check`, Unity batchmode, UnityFramework rebuild, RN install, and runtime visual acceptance have not been rerun for this final tiny position tweak, so `gloss_lip` remains pending user/device screenshot acceptance.
- E7 gradient/gloss continuous-ramp retune: buildless implementation and verification are complete without touching `matte_lip`; runtime visual acceptance is still pending a Build Gate-approved UnityFramework/RN install plus iPhone photo/log review.
- E7 gradient_lip boundary-risk hard reset: buildless implementation/preview verification, Unity batchmode import/compile, UnityFramework build/sync, and unsigned RN/iOS package build are complete for a matte-derived same-pigment gradient candidate. User screenshot acceptance is now collected and `gradient_lip` is frozen for validation. Do not infer broader E7.3 Green or product readiness from this single lip-finish acceptance.
- 2026-06-25 AR black-screen hotfix: build/install complete on `202268054(여서진)`; suspected black-screen root cause fixed by including ARKit background shaders. Do not mark visual recovery complete until AR screen runtime logs or user visual confirmation show the camera feed instead of black.
- Full E7 visual product-readiness: incomplete.

Active temporary docs:

- `docs/roadmaps/active/E7_VISUAL_PRODUCT_READINESS_SPIKE_PLAN.md`: active E7 master spike plan.
- `docs/roadmaps/active/E7_REGION_PRECISION_SUBSPIKE_PLAN.md`: active E7.03 / E7.3 v2.1 boundary engine quality plan. Phase 0 Contract Reset is complete; keep until v2.1 decisions are absorbed into this result doc or the team explicitly closes E7.3 as Yellow.
- `docs/roadmaps/active/E7_REFERENCE_DRIVEN_UV_ATLAS_PLAN_KO.md`: active reference-driven UV atlas implementation plan for the next E7.03 region precision hardening slice; use for pre-gold capture-pair work and later one-frame round-trip gating.
- `docs/roadmaps/active/E7_LIP_BOUNDARY_ENGINE_SPIKE_REPORT_KO.md`: active E7 lip v2 boundary-first report. It records the invalidated v1 Apple Vision coordinate/preview bug, corrected v2 `raw-y` offline boundary gate, UnityFramework build/sync evidence, and the runtime Apple Vision lip boundary implementation candidate that still needs signed RN install plus iPhone runtime evidence before acceptance.
- `docs/roadmaps/active/E7_LIP_FINISH_NEXT_HANDOFF_KO.md`: active multi-agent handoff for the next lip finish pass. It freezes matte, diagnoses gradient/gloss failures from user screenshots, and defines the next-session prompt and evidence bar.
- `docs/roadmaps/active/E7_COSMETIC_RENDERING_TEAM_CHECK_PLAN_KO.md`: team-facing E7.4/E7.5 cosmetic rendering check plan. Use it to align on scope and entry conditions only; it does not override the E7.3 Yellow boundary or authorize renderer implementation by itself.
- `docs/roadmaps/active/E7_PERFORMANCE_EVIDENCE_SUBSPIKE_PLAN.md`: keep for E7.6 performance decision; E7.2 baseline work is complete.
- Deferred docs to create only when needed: `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` before E7.4, and `E7_DEMO_LOOK_RECIPE_SPEC.md` only if recipe values or preset behavior need a fixed contract.

Default reading route:

- Always read `AGENTS.md` and this snapshot.
- Read `TECH_VALIDATION_TEST_PLAN.md` only when changing the stable validation contract or checking milestone/evidence rules.
- For E7.3 region precision, read `docs/roadmaps/active/E7_VISUAL_PRODUCT_READINESS_SPIKE_PLAN.md` E7.3 sections and the `docs/roadmaps/research/E7_AXIS1_*` reports.
- For E7.03 boundary work, read `docs/roadmaps/active/E7_REGION_PRECISION_SUBSPIKE_PLAN.md` first. It supersedes the old demo-plausible E7.3 Green bar with Q3 overlay-ready validation and the v2.1 experiment order.
- For reference-driven UV atlas work before gold mask authoring, read `docs/roadmaps/active/E7_REFERENCE_DRIVEN_UV_ATLAS_PLAN_KO.md` and keep the official pair status pending until a clean frame and synchronized ARFace export are captured from the same runtime moment.
- For team-facing E7.4/E7.5 cosmetic rendering scope review, read `docs/roadmaps/active/E7_COSMETIC_RENDERING_TEAM_CHECK_PLAN_KO.md`; for implementation planning, read `docs/roadmaps/active/E7_VISUAL_PRODUCT_READINESS_SPIKE_PLAN.md` E7.4/E7.5 sections and the `docs/roadmaps/research/E7_AXIS2_*` reports.
- For E7.6 performance, read `docs/roadmaps/active/E7_PERFORMANCE_EVIDENCE_SUBSPIKE_PLAN.md`.
- Read base research or benchmark reports only for fallback, SDK comparison, licensing, or architecture decisions.

Stop rules:

- Do not mark M7 Green unless formal 3-cycle re-entry evidence is collected.
- Do not proceed to E7.4 or E7.6 from E7.2 logs or E7.3 build/install logs alone.
- Do not mark any E7.3 region Green without real-device runtime compare logs and representative visual evidence for that region.
- Do not mark E7.03 Green under the old "baseline-better" or "demo-plausible" standard. Green now requires Q3 overlay-ready validation for all three in-scope regions.
- Do not treat face parsing as E7.03 live runtime or product/release asset source, do not treat ML Kit Face Mesh as an iPhone runtime candidate, and do not introduce a separate camera session that conflicts with ARKit.
- Do not claim full E7, visual product readiness, product-v1 readiness, or product-quality makeup from E7.0/E7.1 preflight or E7.2 instrumentation logs.
- Do not treat E3 as product-quality makeup accuracy; it proves validation-level region independence only.
- Do not treat E4 as product-quality makeup fidelity; it proves validation/debug texture sample dispatch and visual distinction only.
- Do not treat E5 or E7.2 recording waivers as general waivers for future milestones.
- Do not start AI model inference, backend upload, recommendation logic, commercial SDK integration, Android work, raw camera frame storage, or product implementation until a plan explicitly reaches that scope.

Evidence media policy:

- Do not save screen recordings by default.
- Save a recording only when motion, elapsed time, or a continuous scenario is the core evidence.
- When a recording is captured, prefer long-term retention of metadata, contact sheets, and representative frames. The raw recording may be deleted after decision review or extraction unless a milestone explicitly requires keeping it.

Key evidence:

- E7 runtime Vision screen-space mask no-build correction evidence: user-provided screenshots `IMG_5247.PNG` and `IMG_5246.PNG` showed the Apple Vision + GPU multiply lip path still failing the visible lip boundary, with coarse/dark spill near the mouth. The no-build correction changes Unity from triangle-culling plus a 1x1 white runtime mask to a per-frame screen-space Vision mask texture generated from `outerLips - innerLips`, enables shader screen-space mask sampling through `_UseScreenSpaceMask`, keeps GPU pigment multiply, and suppresses Unity region/debug overlays during Vision `ReadPixels` capture. Checks passed: scoped `git diff --check` for `E3RegionMaskOverlay.cs`, `E7VisionLipBoundaryRuntime.cs`, and `SmoothRegionMask.shader`; RN Jest (`11` tests); TypeScript; RN lint; and a Unity DLL referenced Roslyn script compile using Unity 6000.3.18f1 assemblies. Full `git diff --check` still fails on existing dirty `MakeupARFaceValidation.unity` trailing whitespace outside this correction. No UnityFramework/RN build, install, runtime log, or iPhone visual acceptance was run for this checkpoint.
- E7 runtime Vision screen-space mask build/sync and RN signing-gate evidence: `evidence/logs/m3-repro-unity-export-e7-vision-screen-mask-20260626-0315.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-vision-screen-mask-20260626-0315.log`, and `evidence/logs/m3-repro-artifact-verification-e7-vision-screen-mask-20260626-0315.log` record Unity export, UnityFramework `** BUILD SUCCEEDED **`, `Vision.framework` link verification, ARKit native link verification, and synced arm64 RN/package frameworks (`113M`, Unity `Data` `18M`). `evidence/logs/e7-vision-screen-mask-rn-ios-device-20260626-0315.log` records the signed RN install attempt to `202268054(여서진)` / `00008110-0001794E0CD9801E`, which reached Xcode signing and failed because no provisioning profile exists for `com.yeoseojin.makeupar.validation202268054` while automatic signing is disabled. `evidence/logs/e7-vision-screen-mask-xcodebuild-device-nosign-20260626-0315.log` records a successful device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` that embedded the new UnityFramework. This proves compile/link/framework packaging, not install/runtime acceptance.
- E7 final ARFace-attached lip atlas fallback build/signing-gate evidence: user follow-up reported the Vision lip overlay moving as if vertically inverted and heavily delayed. The active RN/Unity default lip mask was switched from `lip-vision-boundary-v1` to `lip-drawn-style-atlas-v1`; Unity and RNBridge default mask validation now match that ARFace-attached atlas path. The retained Vision compare path applies `raw-y->flip-y` before screen-space use, lowers stale tolerance to `300ms`, uses `0.20s` capture cadence, and logs `vision_lip_boundary_transform_candidates` with `raw`, `flip-y`, `flip-x`, and `flip-xy` bbox summaries. Pre-build checks passed: RN Jest (`11` tests), TypeScript, RN lint, scoped `git diff --check`, and a Unity DLL-referenced Roslyn compile for changed/runtime-dependent C# files; Roslyn emitted only existing JSON payload field warnings (`CS0649`). Build evidence: `evidence/logs/m3-repro-unity-export-e7-arface-lip-fallback-20260626-0349.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-arface-lip-fallback-20260626-0349.log`, and `evidence/logs/m3-repro-artifact-verification-e7-arface-lip-fallback-20260626-0349.log` record Unity export, UnityFramework `** BUILD SUCCEEDED **`, `Vision.framework`/`ARKit.framework`/Unity ARKit native link verification, and synced arm64 RN/package frameworks (`113M`, Unity `Data` `18M`). `evidence/logs/e7-arface-lip-fallback-rn-ios-device-20260626-0349.log` records the signed RN install attempt to `202268054(여서진)` / `00008110-0001794E0CD9801E`, which failed before install because no provisioning profile exists for `com.yeoseojin.makeupar.validation202268054` while automatic signing is disabled. `evidence/logs/e7-arface-lip-fallback-xcodebuild-device-nosign-20260626-0349.log` records a successful device-targeted unsigned Xcode build with `CODE_SIGNING_ALLOWED=NO` that embedded the new UnityFramework. This proves compile/link/framework packaging, not install/runtime acceptance.
- E7 lip soft-SDF multilayer no-build evidence: user-provided screenshots `사진 1.jpg` through `사진 5.jpg` showed hard lip edges and broad/sticker-like gloss/gradient behavior; the follow-up reference image requested an extremely thin white horizontal gloss line. The implementation changes are source-only: shader `_Feather`-scaled 13-tap near/far mask softening, soft/core/edge alpha split, capped weak base stain multiply, center/inner gradient density, atlas `A` channel single-line gloss (`57x4px` / `58x4px` at `A>8` for the active style atlases), runtime `lipRenderLayerMode` / `glossHighlightMode=single_thin_soft_white_wet_line` diagnostics, lower lip atlas culling threshold, higher minimum feather, RN preset feather/coverage retuning, Vision temporal transition, Vision capture-face-bbox to current-face-bbox warp, and Vision render-time face motion diagnostics (`visionBoundaryFaceMotionScore`, `visionBoundaryFaceCenterShiftPx`, `visionBoundaryFaceScaleDelta`, `visionBoundaryFaceMotionRisk`). Latest verification passed with RN Jest (`13` tests, including matte `gloss=none`, `visionMotion=0.276/medium_face_motion`, and `motion=0.390/large_face_motion` diagnostics), TypeScript, RN lint, scoped `git diff --check`, shader/static guard search, shader feather radius guard (`near=3.447px`, `far=6.377px`), edge feather preview guard (`edgeBandMean=0.5947`), detail-preservation guards (`softMatte=0.9608`, `softGradient=0.9721`), soft-white wet-line guards (`wetLineActivePixels=493`, `wetLineMeanLumaBoost=0.1968`, `thinWetLine=0.8531`), structural one-line guard (`bbox=119x5px`, `aspect=23.8`, `height/lip=0.040`, `width/lip=0.509`, `componentCount=1`), and atlas guard (`newGt8=3026`, `acceptedTriangles=275`, `roundTripIoU=0.7952`). No UnityFramework build, RN install, or iPhone runtime visual acceptance was collected for this checkpoint.
- E7 matte/gradient/gloss user-photo retune evidence: after the `e7-soft-sdf-multilayer-20260626-1322` UnityFramework build, user iPhone screenshots showed the validation retune had become too light, matte/gradient were not distinct enough, and gloss was too white/awkward. The latest retune raises matte/gradient coverage and material alpha, keeps edge feather, changes gradient to a weak full-lip base plus high-contrast inner tint that fades outward, and changes gloss diagnostics to `tinted_soft_lower_wet_line`. Updated checks passed: RN Jest (`13`), TypeScript, RN lint, atlas guard, soft-SDF verifier (`softMatteLumaStdRatio=0.7951`, `softGradientLumaStdRatio=1.2470`, `softGradientLumaCorrelation=0.9364`, `wetLineMeanLumaBoost=0.0553`, `wetLineComponentCount=1`), scoped `git diff --check`, preview regeneration, and Unity batchmode import/compile in `evidence/logs/e7-matte-gradient-gloss-retune-unity-batchmode-20260626.log`. Build evidence: `evidence/logs/m3-repro-unity-export-e7-matte-gradient-gloss-retune-20260626-1435.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-matte-gradient-gloss-retune-20260626-1435.log`, and `evidence/logs/m3-repro-artifact-verification-e7-matte-gradient-gloss-retune-20260626-1435.log` record Unity export, UnityFramework `** BUILD SUCCEEDED **`, ARKit/Vision/MetalPerformanceShaders link verification, synced RN/package arm64 frameworks (`113M`, Unity `Data` `18M`), and `real 134.45s`. `evidence/logs/e7-matte-gradient-gloss-retune-rn-ios-device-20260626-1435.log` records the signed RN install failure at the known missing provisioning profile gate for `com.yeoseojin.makeupar.validation202268054`; `evidence/logs/e7-matte-gradient-gloss-retune-xcodebuild-device-nosign-20260626-1435.log` records a successful device-targeted unsigned Xcode build embedding `UnityFramework.framework`. No iPhone runtime visual acceptance has been collected for this latest retune.
- E7 gradient/gloss continuous-ramp no-build evidence: multi-agent follow-up split gradient and gloss analysis. Kepler recommended replacing the gradient's thresholded `gradientMask/fullCore` composition with a continuous ramp and adding guard metrics for transition width, strength ratio, adjacent delta, and center jump. Locke recommended preserving red gloss pigment first, boosting only localized A-channel wet-line shine, and deferring normal/view-direction shading until runtime photos prove static validation-level gloss is insufficient. Source changes keep `matte_lip` preset/branch untouched, use continuous gradient density/ramp only for `gradient_lip`, strengthen `gloss_lip` red base and tinted wet-line highlight, and add buildless guards for gradient continuity plus gloss red-base preservation. Verification passed with `git diff --check`, atlas guard, soft-SDF verifier, RN Jest (`13`), TypeScript, RN lint, Python compile, preview regeneration, and Unity batchmode import/compile in `evidence/logs/e7-gradient-gloss-continuous-ramp-unity-batchmode-20260626.log`. Preview metrics: `softGradientLumaStdRatio=1.3368`, `softGradientLumaCorrelation=0.9472`, `gradientTransitionWidthToLipWidth=0.9744`, `gradientInnerOuterStrengthRatio=3.2299`, `gradientRampMaxAdjacentDeltaP95=0.0110`, `gradientCenterBoundaryJump=0.1243`, `wetLineActivePixels=493`, `wetLineMeanLumaBoost=0.0767`, `wetLineComponentCount=1`, and `glossRedBasePreservationRatio=1.0452`. This is source/buildless evidence only; no real-device build/install/runtime acceptance was run.
- E7 gradient/gloss AR-runtime expected preview evidence: `scripts/e7_reference_atlas/make_lip_runtime_ar_expected_preview.py` now renders the expected AR result from the actual code path, using the same clean frame, ARFace `screenVertices`/`uvs`/`indices`, `lip-drawn-style-atlas-v1`, RN default lip intensity/opacity/color, Unity material alpha/brightness scaling, R-channel triangle culling, the shader's 13-tap soft sampling, multiply pigment pass, and gloss additive pass. It generated `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_20260626/ar_runtime_expected_sheet.png` plus per-style/layer diagnostics. Verdict is `expected_ar_preview_review`, not Green: mesh culling is tight (`279/2304` accepted, cull ratio `0.8789`), `gradient_lip` looks directionally plausible with source-dilated outside ratio `0.0754` but still trips the conservative AR ramp guard (`adjacentDensityDeltaP95=0.0565`), and `gloss_lip` preserves red pigment (`glossRedBasePreservationRatio=1.0335`) with a localized wet highlight (`componentCount=2`, `meanAdditiveLuma=0.0518`) but its full pigment change footprint still expands beyond the source-dilated lip mask (`outsideSourceDilatedRatio=0.1557`). This is buildless expected-render evidence only; no UnityFramework/RN real-device build, install, or runtime acceptance was run.
- E7 gradient lip density-field atlas no-build correction: the repeated gradient failure is now treated as an input-representation problem rather than more layer-weight tuning. `scripts/e7_reference_atlas/generate_lip_gradient_density_atlas.py` creates `lip-drawn-gradient-density-atlas-v1` by copying R/G/A from the frozen default lip atlas and replacing only B with a distance-transform continuous density seed. RN sends this mask only for `gradient_lip`, so `matte_lip` and `gloss_lip` continue using `lip-drawn-style-atlas-v1`. `SmoothRegionMask.shader` gradient mode now uses a single `singleGradientDensity` curve to drive pigment strength plus an `outerMlbbColor -> innerRedColor` chroma ramp, and the guard fails if additive `outerSoftWash` / `midGradientLayer` / `innerGradientTint` style composition returns. Evidence is under `evidence/e7-reference-atlas/lip-style-atlas-v1/gradient_density_atlas_20260626/` and the regenerated AR expected sheet remains `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_20260626/ar_runtime_expected_sheet.png`. Gradient expected-render guards now pass: source-dilated outside ratio `0.0589`, transition width `0.8261`, inner/outer pigment ratio `1.8954`, adjacent density p95 `0.0456`, and center-line max jump `0.0505`. Overall expected preview is still `review` because gloss footprint is deferred to the next step. Checks passed: Python compile, atlas generation, AR expected preview, soft-SDF preview regeneration, atlas guard, soft-SDF verifier, RN Jest (`13`), TypeScript, RN lint, and `git diff --check`. Unity batchmode import/compile was attempted but stopped at Unity Licensing IPC timeout before compile; no UnityFramework/RN real-device build, install, or runtime acceptance was run.
- E7 gradient lip internal-boundary no-build correction: after visual review, the remaining issue was clarified as a visible boundary between the inner red density and outer tint, not the outer lip edge. `generate_lip_gradient_density_atlas.py` now uses a wider distance-field seed (`seedPixels=327`, `densityMeanActive=0.2433`) so B is less like a thin mouth-crease band. The shader gradient branch replaces threshold-like `smoothstep` pigment/chroma ramps with gamma curves plus `gradientEdgeFade`, so low-density pigment opens gradually from the same `singleGradientDensity` field instead of reading as a second layer. The runtime expected preview now guards `edgeInnerPigmentRatio`; latest gradient metrics are source-dilated outside ratio `0.0007`, transition width `0.7826`, inner/outer pigment ratio `3.0835`, edge/inner pigment ratio `0.0243`, adjacent density p95 `0.0452`, and center-line max jump `0.0495`. The soft-SDF verifier also passes with `gradientEdgeInnerStrengthRatio=0.0138`, `gradientInnerOuterStrengthRatio=2.3794`, `gradientRampMaxAdjacentDeltaP95=0.0115`, `gradientCenterBoundaryJump=0.1285`, and `softGradientLumaCorrelation=0.9743`. Checks passed: bundled Python compile, gradient atlas generation, AR expected preview, soft-SDF preview regeneration, atlas guard, soft-SDF verifier, RN Jest (`13`), TypeScript, RN lint, and `git diff --check`. No Unity batchmode compile, UnityFramework/RN real-device build, install, or runtime acceptance was run for this follow-up.
- E7 gradient internal-boundary UnityFramework build/sync: after user Build Gate approval, `speed-up-unity-builds` audit confirmed the Unity project root is `unity/MakeupARUnityValidation`, Unity `Library/` is retained, and `scripts/build_m3_unityframework.sh` already uses the stable `unity-builds/xcode-derived-data/UnityFramework` cache. A stale Unity Licensing Client process was stopped before build. `TIMESTAMP=e7-gradient-internal-boundary-20260626 BUILD_LOG_MODE=full UNITY_BIN="/Users/yeoduchi/Library/Application Support/UnityHub/downloads/6000.3.18f1/Unity.app/Contents/MacOS/Unity" /usr/bin/time -p bash scripts/build_m3_unityframework.sh` succeeded in `real 165.15s`, with Unity export `CompileScripts: 4391.813ms`, required ARKit/Vision/MetalPerformanceShaders links verified, Xcode `UnityFramework` recording `** BUILD SUCCEEDED **`, and both RN/package-local frameworks synced as arm64 Mach-O frameworks (`114M`, Unity `Data` `19M`). Evidence logs: `evidence/logs/m3-repro-unity-export-e7-gradient-internal-boundary-20260626.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-gradient-internal-boundary-20260626.log`, and `evidence/logs/m3-repro-artifact-verification-e7-gradient-internal-boundary-20260626.log`. This approves compile/link/package correctness for the gradient internal-boundary fix; no RN real-device install, iPhone photo/log review, or E7.3 visual acceptance was collected.
- E7 gradient_lip same-color density proof/no-build correction: user clarified the real goal was not a different red, but the same selected tint reading differently by application style; the failure mode was that strengthening the gradient made inner and outer look like different color layers. The active correction keeps `matte_lip` frozen and does not add another layer. `gradient_lip` now uses a wider shader-side B-channel density blur (`GradientDensityBlur`) blended with raw density, a gradient-only feather allowance (`0.38`), and a stronger pigment ceiling, but the gradient branch derives both `outerMlbbColor` and `innerRedColor` from the same `_RegionColor`; color variation is removed and only pigment density/strength changes across the lip. RED 100% buildless AR expected metrics prove the same-color contract with `pigmentColorRangeMax=0.0` and `pigmentColorStdMax=0.000123`, while improving color strength and boundary smoothness versus the earlier continuous-density path: pigment p95 `0.3961 -> 0.5156`, mean redness `0.3090 -> 0.3205`, inner/outer pigment ratio `3.0835 -> 2.2810`, adjacent density p95 `0.0452 -> 0.0351`, and center-line max jump `0.0495 -> 0.0279`; outside-source-dilated ratio is `0.0341` for RED 100% and `0.0248` for default rose. Evidence regenerated at `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_20260626/ar_runtime_expected_sheet.png` and `evidence/e7-reference-atlas/lip-style-atlas-v1/soft_sdf_multilayer_preview_20260626/soft_sdf_multilayer_sheet.png`; RED 100% scratch preview is `/private/tmp/e7-gradient-red-samecolor-proof/expected_gradient_lip.png`. The new bright-light user photo is saved as `evidence/e7-reference-atlas/lip-style-atlas-v1/lighting_references/bright_lighting_reference_20260626.png` for lighting/color reference only; it is not used as AR expected-render evidence because it has no same-frame ARFace `screenVertices`/`uvs`/`indices`, and the photo-only auto-mask render attempt was visually misaligned and removed. Checks passed: bundled Python compile, AR expected preview, soft-SDF preview regeneration, soft-SDF verifier, RN Jest (`13`), TypeScript, RN lint, and `git diff --check`. No Unity batchmode compile, UnityFramework/RN real-device build, install, or runtime acceptance was run for this follow-up.
- E7 expected-render presentation rule: future expected-render responses should show both `ARFace-based expected render` and `Bright photo-aligned lighting preview` together. `scripts/e7_reference_atlas/make_lip_runtime_ar_expected_preview.py` now generates a `lightingReferencePreview` block and `gradient_two_reference_preview.png` by default when the saved bright-light reference exists. The ARFace sheet remains the only AR alignment/attachment evidence; the bright preview is explicitly `notArfaceEvidence=true` and is for lighting/color/density review only. RED 100% regenerated outputs: `evidence/e7-reference-atlas/lip-style-atlas-v1/ar_runtime_expected_red100_20260626/ar_runtime_expected_sheet.png`, `bright_lighting_reference_gradient_lip_photo_aligned_sheet.png`, and `gradient_two_reference_preview.png`. Checks passed: Python compile, default expected preview, RED 100% expected preview, and `git diff --check`. No Unity/RN real-device build was run.
- E7 gradient_lip matte-derived Build Gate / RN device-gate evidence: `evidence/logs/e7-gradient-matte-derived-buildgate-unity-batchmode-20260626.log` records Unity batchmode import/compile success (`CompileScripts: 4307.529ms`, no shader/C# compile errors, successful batchmode exit). `evidence/logs/m3-repro-unity-export-e7-gradient-matte-derived-20260626-1805.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-gradient-matte-derived-20260626-1805.log`, and `evidence/logs/m3-repro-artifact-verification-e7-gradient-matte-derived-20260626-1805.log` record Unity iOS export, ARKit/Vision/MetalPerformanceShaders link verification, UnityFramework `** BUILD SUCCEEDED **`, and synced RN/package arm64 frameworks (`114M`, Unity `Data` `19M`). `evidence/logs/e7-gradient-matte-derived-rn-ios-device-20260626-1808.log` records the signed RN install attempt failing because the target UDID was unavailable to RN/Xcode (`xcodebuild` code `70`). `evidence/logs/e7-gradient-matte-derived-rn-ios-unsigned-build-20260626-1810.log` records a successful unsigned generic iOS build that embedded `UnityFramework.framework` into `MakeupARValidation.app`; local artifact check shows a `194M` app with embedded `114M` UnityFramework and `19M` Unity `Data`. This proves compile/link/package correctness only; no install/runtime visual acceptance was collected.
- E7 gradient_lip user acceptance/freeze evidence: `evidence/screenshots/e7-gradient-lip-accepted-2026-06-26/IMG_5268.PNG` is the retained user-provided iPhone HUD screenshot for the accepted gradient result. The screenshot shows RED `#C21F3A`, intensity `100%`, `finish gradient_lip`, `active=lip`, `focus=lip`, Tracking, mesh `v=1220/i=6912/uv=1220`, FPS `59.9`, frame `16.7ms`, and latency `21.0ms`. User decision text was "이거야 그라데이션 픽스하자. 이제 수정 안할거야"; record `gradient_lip` as frozen for E7.3 validation unless explicitly reopened. This acceptance does not cover `gloss_lip`, cheek, eye, full E7.3 Green, or product-quality makeup readiness.
- E7 bright lighting preview alignment correction: user noted the bright photo-aligned preview was still off the lips and therefore could not be trusted for color judgment. The preview-only bright-reference mask in `make_lip_runtime_ar_expected_preview.py` is now conservative: upper-lip points are pulled further inside, feather is reduced, and actual render alpha is attenuated where the continuous density is low so upper/outer edges do not receive full pigment. This affects only the saved bright-light photo preview; it does not change `matte_lip`, ARFace capture-pair expected rendering, Unity runtime masking, or RN presets. Important limitation: if the bright preview boundary is still visually questionable, do not use it as color proof; require a same-frame ARFace capture or an explicit lip mask. Checks passed: Python compile, default expected preview, RED 100% expected preview, and `git diff --check`; no Unity/RN real-device build was run.
- E7 lip implementation Korean report: `docs/roadmaps/active/E7_LIP_IMPLEMENTATION_REPORT_KO.md` is the Korean integrated report for the current lip implementation state. It records `matte_lip` as Done/frozen, `gradient_lip` as Done/user accepted/frozen, `gloss_lip` as implemented candidate with runtime acceptance pending, and E7.3 overall as Yellow.
- 2026-06-27 E7 cheek blush v1 buildless implementation: cheek validation is now the active next renderer candidate while frozen `matte_lip` / accepted `gradient_lip` remain unchanged. RN starts with `active=none`, `focus=cheek`, default `blush_daily`, and collapsed controls; selecting one of `blush_daily`, `blush_lovely`, `blush_sunkissed1`, `blush_sunkissed2`, or `blush_under_eye` enables exactly one cheek layer inside the existing three-layer batch. Unity/RNBridge whitelist/defaults now accept the five cheek mask IDs, use `cheek-blush-v1` / `cheek_blush_validation_v1`, and runtime diagnostics identify `maskSource=cheek_blush_v1_uv_back_projection` plus `boundaryRenderer=rgba_cheek_blush_density_feather_powder`. The five Unity mask PNGs are regenerated as `512x512` RGBA with `R/A=soft alpha`, `G=reserved zero`, and `B=per-shape density`; expected-render evidence is under `evidence/e7-reference-atlas/cheek-blush-mask-textures-v1/`. Checks passed: cheek generator, expected preview generator, cheek contract verifier, Python compile, RN Jest (`15` tests), TypeScript, RN lint, scoped `git diff --check`, and Unity DLL-referenced Roslyn compile with only existing JSON payload field warnings. This is buildless/static evidence only: no UnityFramework build/sync, RN install, iPhone runtime photos/logs, FPS/frame-time/thermal/memory, motion, or face-proportion evidence was collected, so cheek remains Yellow and waits at Build Gate.
- 2026-06-27 E7 cheek blush runtime Build Gate packet: `evidence/logs/e7-cheek-blush-runtime-buildgate-20260627.md` records the required approval question, UnityFramework/RN primary path, compare-only paths, cheek-only validation contract, evidence matrix, device/signing gate, frozen lip scope, and out-of-scope items. This is a gate packet only; no UnityFramework regeneration, RN device build, install, runtime diagnostics, screenshots, motion frames, or cheek visual acceptance has been run for this checkpoint.
- 2026-06-27 E7 cheek blush completion audit: `evidence/logs/e7-cheek-blush-completion-audit-20260627.md` maps every goal requirement to current evidence. It confirms source/buildless items are satisfied, including external placement-reference alignment, single cheek mask selection, channel contract, shader branch separation, RNBridge v1 naming/defaults, verifier/test coverage, temp-plan cleanup, and `TECH_VALIDATION_RESULT.md` recording. It also confirms the goal is not complete because real-device runtime shape sweep, color/intensity latency, startup observation, FPS/frame-time/latency/thermal/memory, yaw/pitch/smile/near-far motion, and face-proportion evidence are missing pending Build Gate approval.
- 2026-06-27 E7 cheek blush pre-build QA approval: `evidence/logs/e7-cheek-blush-prebuild-qa-20260627.md` records multi-agent read-only QA before the long build. UI/RN QA and Evidence QA passed; Runtime QA initially failed on missing B-channel density runtime diagnostics and `maskTextureId` fallback/default behavior. The fixes add layer/top-level/default `maskTextureId` fallback in `RNBridge`, B-channel density diagnostics (`maskTextureDensityGt8Pixels`, coverage, bbox, max) in Unity/RN logs and JSON events, and verifier guards for those contracts. Post-fix checks passed: cheek verifier, Python compile, RN Jest (`15`), TypeScript, RN lint, scoped `git diff --check`, and Unity Roslyn compile-only with only existing JSON payload `CS0649` warnings. A goal-resume recheck on 2026-06-27 KST repeated the bundled-Python cheek verifier, Python compile, RN Jest (`15`), TypeScript, RN lint, scoped `git diff --check`, and Unity Roslyn compile-only, all passing with only the same JSON payload `CS0649` warnings. Final quality approver decision is `APPROVE_BUILD_GATE`; this approves starting the build after explicit user approval, not runtime Green.
- 2026-06-27 E7 cheek blush Build Gate execution: after user approval, UnityFramework regeneration/sync succeeded with `TIMESTAMP=e7-cheek-blush-runtime-20260627` and `BUILD_LOG_MODE=full`. Unity export imported all five cheek v1 mask Resources, recorded `CompileScripts: 3358.894ms`, and included ARKit/Vision/MetalPerformanceShaders links. The UnityFramework Xcode build recorded `** BUILD SUCCEEDED **`; RN/package-local framework artifacts are arm64 Mach-O frameworks (`119M`, Unity `Data` `24M`). RN real-device run on UDID `00008110-0001794E0CD9801E` failed before install/runtime because the iPhone was unavailable to Xcode/RN (`xcodebuild` code `70`; `devicectl` showed `202268054(???)` as `unavailable`). A compare-only unsigned generic iOS build then succeeded, producing a `199M` `MakeupARValidation.app` embedding the new `119M` UnityFramework and `24M` Unity `Data`. Evidence logs: `evidence/logs/m3-repro-unity-export-e7-cheek-blush-runtime-20260627.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-cheek-blush-runtime-20260627.log`, `evidence/logs/m3-repro-artifact-verification-e7-cheek-blush-runtime-20260627.log`, `evidence/logs/e7-cheek-blush-rn-ios-device-20260627.log`, `evidence/logs/e7-cheek-blush-rn-ios-unsigned-build-20260627.log`, and `evidence/logs/e7-cheek-blush-rn-ios-unsigned-artifact-20260627.log`. Cheek remains Yellow because no runtime screenshots/log diagnostics, FPS/frame-time/latency/thermal/memory, motion, face-proportion, or five-shape visual acceptance evidence was collected.
- 2026-06-27 E7 cheek blush skin-fade feedback correction: user-provided iPhone screenshots at `rose #D94B74` / `intensity=95%` showed plausible cheek placement but an unnatural pink perimeter, especially on `blush_lovely`, confirming that the edge should resolve toward actual camera skin color rather than a low-alpha pink patch. A follow-up user review also noted that shape-specific density intent was not visible enough. Evidence is stored under `evidence/screenshots/e7-cheek-blush-skin-fade-feedback-2026-06-27/`, and the correction log is `evidence/logs/e7-cheek-blush-skin-fade-feedback-20260627.md`. The cheek shader now makes pigment strength density/core driven, uses a neutral multiply tint at the perimeter so edges fade back toward unchanged skin, and keeps a lower cheek pigment cap than lip. The cheek mask B-channel density was regenerated so `blush_daily` peaks outer/high cheekbone, `blush_lovely` peaks apple center, `blush_sunkissed1` keeps cheek strongest with nose capped low, `blush_sunkissed2` keeps cheekbone ends strongest with low bridge, and `blush_under_eye` peaks outer under-eye/high cheek while restraining inner lower eyelid. The no-build expected sheet was regenerated at `evidence/e7-reference-atlas/cheek-blush-mask-textures-v1/expected_render_20260627/cheek_blush_expected_render_sheet.png`, and the density contact sheet is `evidence/e7-reference-atlas/cheek-blush-mask-textures-v1/cheek_blush_mask_contact_sheet.png`. RN HUD now keeps `recipe_applied` events by region so cheek review is not overwritten by a later disabled `eye` acknowledgement. Follow-up no-build QA passed; `blush_sunkissed2` remains the highest runtime banding risk. This is no-build/static correction only; a fresh Build Gate-approved UnityFramework/RN run is required before runtime acceptance or Green.
- 2026-06-28 E7 cheek blush milk-pink/three-stage correction: user-provided expected color was sampled as `#F0CBD5` and added to the RN cheek palette as `milk_pink`. The cheek texture generator now expands all five blush masks for a wider soft outer falloff, treats `blush_under_eye` specially so it starts directly below the lower eye area and fades into the high cheek instead of floating awkwardly lower, and encodes a per-shape three-stage density map (`outer` / `mid` / `core`) for smoother gradients. The cheek shader now resolves those logical layers as `cheekOuterLayer`, `cheekMidLayer`, and `cheekCoreLayer`, keeping neutral skin fade at the outer edge while letting the core carry stronger pigment. No-build checks passed: cheek contract verifier, `git diff --check`, RN Jest (`16`), TypeScript, and RN lint. After build approval, UnityFramework regeneration/sync succeeded with `TIMESTAMP=e7-cheek-blush-three-stage-20260628`; RN real-device install still failed before runtime because Xcode has no provisioning profile/account for `com.yeoseojin.makeupar.validation202268054` / team `9G4K6N63MK`. A compare-only unsigned device-targeted RN/Xcode build succeeded and produced a `199M` app embedding an arm64 `119M` UnityFramework with `24M` Unity `Data`; required Unity metadata and ARKit subsystem files are present. Evidence logs: `evidence/logs/m3-repro-unity-export-e7-cheek-blush-three-stage-20260628.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-cheek-blush-three-stage-20260628.log`, `evidence/logs/m3-repro-artifact-verification-e7-cheek-blush-three-stage-20260628.log`, `evidence/logs/e7-cheek-blush-three-stage-rn-ios-device-20260628.log`, `evidence/logs/e7-cheek-blush-three-stage-rn-ios-device-auto-sign-20260628.log`, `evidence/logs/e7-cheek-blush-three-stage-rn-ios-unsigned-build-20260628.log`, and `evidence/logs/e7-cheek-blush-three-stage-rn-ios-unsigned-artifact-verification-20260628.log`. Cheek remains Yellow until runtime screenshots/logs, motion, performance, and five-shape visual acceptance evidence are collected on the iPhone.
- 2026-06-28 E7 cheek blush natural edge/color no-build correction: user screenshots showed visible cheek perimeter seams, weak color differentiation, nearly invisible `milk_pink`, and awkward side-face clipping on `blush_sunkissed1` / `blush_sunkissed2`. The follow-up correction keeps the wide user-drawn/source region as a safety outer alpha, but moves visible pigment inward by regenerating the B-channel density with a stricter inner gate, component-edge fade, and UV side fade so face-edge mesh clipping is less likely to leave a colored vertical boundary. `blush_sunkissed1` now rebuilds the screen source from filled round cheek blobs plus nose blush instead of preserving the half-moon source cutout; the latest tweak extends only the cheek blobs' inner horizontal span toward the cheek center while keeping vertical growth restrained. Unity cheek shader blending now treats outer as a near no-color safety wash, mid as the veil, and core as pigment; `cheekLightColorBoost` / `cheekVisiblePrimary` make bright pink colors validation-visible without changing saturated red/rose behavior as much. `E3RegionMaskOverlay` raises cheek material alpha scaling so light colors are not swallowed by multiply blending. The expected-render sheet is regenerated at `evidence/e7-reference-atlas/cheek-blush-mask-textures-v1/expected_render_20260628_natural_v5/cheek_blush_expected_render_sheet.png`, and the cheek contract verifier passes with density now smaller than alpha for all five masks. This is no-build/static correction only; UnityFramework rebuild, RN install, and iPhone runtime visual acceptance have not been rerun.
- E7 Vision ARFace UV-bake no-build evidence: the latest Vision debug/compare path change bakes the stabilized Vision lip boundary into a transient ARFace UV mask instead of shader-sampling a live screen-space mask. Source evidence: `E3RegionMaskOverlay.cs` now creates `VisionUvMaskTexture`, back-projects screen samples through ARFace triangle barycentric coordinates, writes a soft-falloff UV splat (`VisionUvMaskSoftSplatRadius=3`) instead of a hard `3x3` stamp, applies the dynamic texture with `_UseScreenSpaceMask=0`, and reports `apple_vision_lip_landmark_arface_uv_baked` / `vision_arface_uv_baked_outer_minus_inner_soft_falloff` diagnostics. Verification passed with scoped `git diff --check`, Unity DLL-referenced Roslyn compile for all runtime scripts, Unity batchmode import/compile, RN Jest (`11` tests), TypeScript, and RN lint. The Unity log `evidence/logs/e7-vision-arface-uv-bake-diag-unity-batchmode-20260626.log` records `CompileScripts: 2965.738ms`, no `Shader error` / `error CS`, and `Exiting batchmode successfully now!`. No UnityFramework build, RN install, or iPhone runtime visual acceptance was collected for this checkpoint.
- E7 soft-SDF multilayer offline preview evidence: `scripts/e7_reference_atlas/make_lip_soft_sdf_multilayer_preview.py` passed Python compile and generated `evidence/e7-reference-atlas/lip-style-atlas-v1/soft_sdf_multilayer_preview_20260626/soft_sdf_multilayer_sheet.png`, `soft_sdf_layer_diagnostic.png`, and `summary.json`. The preview uses the same reference frame and lip atlas round-trip mask to approximate the shader intent for no-build review: hard alpha vs soft matte vs soft gradient vs a single thin soft-white wet-line. It is preview-only evidence, not runtime acceptance.
- E7 soft-SDF completion audit evidence: `evidence/logs/e7-soft-sdf-completion-audit-20260626.md` records requirement-by-requirement status for Vision smoothing/transition, large-motion compensation, ARFace UV stabilization, shader logical layers, RN diagnostics, and missing runtime visual acceptance.
- E7 soft-SDF runtime acceptance checklist: `evidence/logs/e7-soft-sdf-runtime-acceptance-checklist-20260626.md` records the required post-build iPhone photos/logs and pass/fail criteria for matte/glow/gradient, active ARFace atlas default, wide feather runtime radius, and Vision debug diagnostics; `scripts/e7_reference_atlas/verify_soft_sdf_runtime_evidence.py` verifies the runtime log fields after a Build Gate-approved run, including default failure when the first active lip recipe is `lip-vision-boundary-v1` or when wide feather radius diagnostics are missing/too narrow, and `scripts/e7_reference_atlas/verify_lip_soft_sdf_multilayer.py` now guards that verifier contract.
- E7 lip makeup validation v1 implementation/build evidence: `evidence/logs/e7-lip-makeup-validation-v1.md` records scope, dirty-state gate, atlas generation, RN/Unity contract changes, stop conditions, no-build results, Build Gate execution, `202268054(여서진)` device retry, unsigned device build success, signing blocker, lip-first photo feedback, UV back-projection atlas tightening, lip atlas mesh culling, and screen round-trip preview. The latest atlas checkpoint records `projectionUsed=true`, runtime mask source `arface_uv_back_projection`, `223` hit triangles, `14581` hit samples, `2114` raw UV pixels, projected UV mask coverage `0.012253`, and bbox `left=207 top=292 right=302 bottom=347 width=96 height=56`; it also records that the previous smooth footprint was materially broader. The no-build guard `scripts/e7_reference_atlas/verify_lip_style_atlas.py` now verifies this contract and passes with old/new `>8` pixel ratio `2.972571`, lip threshold `0.08`, lip feather `0.18`, mesh culling stats of `2304` source triangles, `269` accepted triangles, `2035` culled triangles, cull ratio `0.883247`, screen round-trip IoU `0.833169`, outside-source ratio `0.148632`, and missing-source ratio `0.025015`. Unity runtime metadata now labels the lip atlas source as `lip_style_atlas_v1_uv_back_projection`; static guards confirm RNBridge passes `layer.Enabled`, default-disabled cheek/eye recipes hide region views, user-enabled cheek/eye recipes render for placement validation, and enabled recipes are the only ones rendered during Unity updates, and runtime `recipe_applied` / `region_mask_apply` diagnostics expose mask texture size, `>8` footprint, threshold footprint, bbox, `sourceTriangles`, `culledTriangles`, and `meshCullingMode` fields so stale resource loads or full-face fallback can be identified from device logs. RN Jest now covers a fake Unity `recipe_applied` event and verifies the HUD summary surfaces `src`, `texGt8`, `texBbox`, and `cull=2035/2304`; final no-build checks pass with `11` Jest tests, TypeScript, RN lint, Python compile, atlas guard, static delimiter/token guard, screen round-trip guard, and full `git diff --check`. `evidence/e7-reference-atlas/lip-style-atlas-v1/summary.md` and `summary.json` record the lip marking candidate (`1179x2556`, retained coverage `0.004882`, `14711` retained pixels, `2` stray pixels removed), `trueUvBackProjection=true`, and `greenEvidenceEligible=false`; `lip_style_atlas_v1_channels.png` previews RGBA channels, and `lip_atlas_roundtrip_overlay.png` previews same-frame atlas projection over the reference face. Build Gate evidence: `evidence/logs/e7-lip-v1-unityframework-wrapper-20260625T1755Z.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-lip-v1-20260625-1755.log`, and `evidence/logs/m3-repro-artifact-verification-e7-lip-v1-20260625-1755.log` record UnityFramework `** BUILD SUCCEEDED **` and synced arm64 RN/package frameworks (`108M`, Unity Data `12M`). Signed RN install attempts are recorded in `evidence/logs/e7-lip-v1-rn-ios-device-202268054-20260625T1815Z.log` and `evidence/logs/e7-lip-v1-rn-ios-device-202268054-auto-2834-20260625T1818Z.log`; they show RN CLI mapping `202268054(여서진)` to `00008110-0001794E0CD9801E`, then missing profile / no local Xcode account. Device-targeted compile/link proof is recorded in `evidence/logs/e7-lip-v1-xcodebuild-device-nosign-202268054-20260625T1823Z.log`, which records `** BUILD SUCCEEDED **` with `CODE_SIGNING_ALLOWED=NO`; this is not install/runtime evidence because the built app is unsigned.
- E7 matte/gloss/gradient no-build tuning evidence: `evidence/logs/e7-lip-makeup-validation-v1.md` records the latest recipe/shader/UI pass. The atlas guard re-ran with bundled Python and wrote `evidence/e7-reference-atlas/user-drawn-mask-textures-v1/prebuild-verification/gradient_gloss_roundtrip_20260625.json`, `gradient_gloss_roundtrip_overlay_20260625.png`, and `gradient_gloss_roundtrip_mask_20260625.png`; it passed with old/new `>8` pixel ratio `2.972571`, `269/2304` accepted mesh triangles, screen round-trip IoU `0.833169`, outside-source ratio `0.148632`, and missing-source ratio `0.025015`. RN Jest (`11` tests), TypeScript, lint, Python compile, and `git diff --check` pass for the matte/gloss/gradient tuning.
- E7 matte/gloss/gradient UnityFramework-only build evidence: `evidence/logs/m3-repro-unity-export-e7-matte-gloss-gradient-20260625-2230.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-matte-gloss-gradient-20260625-2230.log`, and `evidence/logs/m3-repro-artifact-verification-e7-matte-gloss-gradient-20260625-2230.log` record Unity export, `** BUILD SUCCEEDED **`, ARKit link verification, and synced arm64 `UnityFramework.framework` artifacts in both `rn/MakeupARValidation/unity/builds/ios/` and `rn/MakeupARValidation/node_modules/@azesmway/react-native-unity/ios/`. No RN/Xcode install was run for this checkpoint.
- 2026-06-25 AR black-screen diagnosis/hotfix: `evidence/logs/ar-blush-runtime-console-unityview-patch-retry2-20260625T071831Z.log` records Unity startup, ARKit subsystem registration, face tracking, and the missing `Unlit/ARKitBackground` exception; `evidence/logs/m3-repro-xcodebuild-unityframework-20260625T072220Z.log` records `** BUILD SUCCEEDED **`; `evidence/logs/m3-repro-artifact-verification-20260625T072220Z.log` records synced RN/package UnityFramework artifacts with Unity Data; `evidence/logs/ar-blush-rn-ios-device-arkit-shader-20260625T072855Z.log` records successful build/install/launch on `202268054(여서진)` with bundle id `com.yeoseojin.makeupar.validation202268054`; `evidence/logs/ar-blush-runtime-console-arkit-shader-start-ar-20260625T073300Z.log` records the blocked launch while the iPhone was locked; `evidence/logs/ar-blush-runtime-console-arkit-shader-unlocked-20260625T073500Z.log` records a later unlocked launch that did not reach the AR screen before console stop.
- E7.3 region precision implementation/build/video review: `evidence/logs/m3-repro-unity-export-e7-region-precision-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-region-precision-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-region-precision-2026-06-22.log`, `evidence/logs/e7-region-precision-rn-ios-run-default-candidate-2026-06-22.log`, `evidence/logs/e7-region-precision-summary-2026-06-22.md`, `evidence/logs/e7-region-precision-video-analysis-2026-06-22.md`, `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/contact-sheet-1fps.jpg`.
- E7.03 Phase 1 UI/evidence hygiene real-device check: `evidence/logs/m3-repro-unity-export-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/m3-repro-artifact-verification-e7-phase1-ui-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-rn-ios-run-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-runtime-console-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-capture-tooling-2026-06-22-141533.log`, `evidence/logs/e7-phase1-ui-display-info-2026-06-22-141533.log`, `evidence/screenshots/e7-phase1-ui-display-info-2026-06-22-141533.json`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/clean-view.jpg`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/compact-hud.jpg`, `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/full-debug.jpg`.
- E7.03 Phase 2 ARFace authored atlas MVP build/install check: `evidence/logs/m3-repro-unity-export-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/m3-repro-artifact-verification-e7-phase2-atlas-2026-06-22-151537.log`, `evidence/logs/e7-phase2-atlas-2026-06-22-151537-rn-ios-device.log`.
- E7.03 P2 synchronized reference capture: `evidence/logs/m3-repro-unity-export-2026-06-22-230402.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-2026-06-22-230402.log`, `evidence/logs/m3-repro-artifact-verification-2026-06-22-230402.log`, `evidence/logs/e7-reference-capture-rn-ios-run-2026-06-22-233049.log`, `evidence/logs/e7-reference-capture-runtime-console-2026-06-22-233202.log`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143300Z_01/`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143305Z_02/`, `evidence/e7-reference-atlas/capture_pairs/pair_face_20260622T143334Z_03/`.
- E7.03 A/B UV atlas fast gate: `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/summary.md`, `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/summary.json`, plus per-region `round_trip/`, `cross_frame/`, `uv_overlap/`, `candidate_preview/`, and `atlases/` artifacts. Earlier fast-gate runs are superseded because they used sparse sampling or were debug/smoke runs.
- E7.03 P8 runtime sweep implementation prep: frozen Unity Resources under `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/` include 9 probability PNGs, 9 PNG-byte Resources, and `runtime_candidates.json`. Static checks passed: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check`. This is implementation/static evidence only, not real-device runtime evidence.
- E7.2 build/runtime: `evidence/logs/m3-repro-unity-export-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-baseline-instrumentation-ack-2026-06-22.log`, `evidence/logs/e7-baseline-rn-ios-run-ack-2026-06-22.log`, `evidence/logs/e7-baseline-runtime-console-ack-2026-06-22.log`, `evidence/logs/e7-baseline-summary-2026-06-22.md`, `evidence/screenshots/e7-baseline-status-panel-2026-06-22.jpg`.
- E7.0/E7.1 preflight: `evidence/logs/e7-unity-process-cleanup-2026-06-22.log`, `evidence/logs/m3-repro-unity-export-e7-preflight-2026-06-22.log`, `evidence/logs/m3-repro-xcodebuild-unityframework-e7-preflight-2026-06-22.log`, `evidence/logs/m3-repro-artifact-verification-e7-preflight-2026-06-22.log`, `evidence/logs/e7-build-install-run-2026-06-22.log`, `evidence/logs/e7-runtime-event-preflight-2026-06-22.log`.
- E5 no-inference snapshot: `evidence/logs/e5-ai-feature-readiness-runtime-2026-06-21.log`, `evidence/screenshots/e5-ai-feature-readiness-rn-status-2026-06-21.jpg`.
- E4 texture samples: `evidence/logs/e4-texture-samples-runtime-2026-06-21.log`, `evidence/screen-recordings/e4-texture-samples-matte-shimmer-blush-2026-06-21.mp4`, `evidence/screenshots/e4-texture-samples-comparison-2026-06-21.jpg`.
- E3 region mask: `evidence/logs/e3-region-mask-layer-dispatch-2026-06-21.log`, `evidence/screen-recordings/e3-region-mask-lip-cheek-eye-2026-06-21.mp4`, `evidence/screenshots/e3-region-mask-debug-colors-2026-06-21.jpg`.
- M6 Unity -> RN events: `evidence/logs/m6-face-state-fix-runtime-2026-06-21.log`, `evidence/screen-recordings/m6-unity-to-rn-events-2026-06-21.mp4`, `evidence/screenshots/m6-rn-face-lost-latest-event-2026-06-21.jpg`, `evidence/screenshots/m6-rn-face-tracked-recipe-event-2026-06-21.jpg`.

## E7.3 Region Precision

Status: Yellow overall / blocked from E7.4. Implementation/build/install evidence is present and the screen recording confirms ARFace mesh/UV candidate mode runs on-device, but the legacy procedural candidate fails visual precision for `lip` and `eye`; `cheek` remains Yellow because the recording does not include a clean E7 candidate sample. P8 reference UV atlas runtime evidence is now collected for `lip`, `cheek`, and `eye`; all three remain Yellow because attachment improved but silhouette/material quality and required motion scenarios are not Q3 overlay-ready yet.

Decision:

- E7.03 Phase 0 Contract Reset is complete. `docs/roadmaps/active/E7_REGION_PRECISION_SUBSPIKE_PLAN.md` v2.1 is the active E7.03 boundary engine plan.
- E7.03 v2.1 Phase 1 Validation UI/Evidence Hygiene is implemented in the RN validation UI, JS-verified, and smoke-verified through a fresh real-device build/install/launch/runtime-log pass.
- E7.03 v2.1 Phase 2 ARFace authored atlas MVP is implemented in Unity and RN, with manual vertex label groups for `lip`, `cheek`, and `eye`, topology/UV audit evidence metadata, RN-dispatchable atlas variants, and preserved E3/E4 baseline plus current procedural candidate paths.
- E7 reference-driven UV atlas pre-gold setup was initialized locally before P2 capture: RN display/evidence wording identifies the current atlas path as a manual heuristic baseline, and `evidence/e7-reference-atlas/` contains the ignored local skeleton, manifest schema, manifest, and earlier pending `pair_lip_0001` capture-pair README. Current P2 evidence uses the newer common `pair_face_*` capture folders.
- E7.03 P2 synchronized reference capture is complete for input collection: clean `frame.png`, same-moment `arface_export.json`, `projected_mesh_overlay.png`, and `capture_summary.json` were exported and copied for three unique `pair_face_*` attempts. The best current drawing input is `pair_face_20260622T143334Z_03`.
- P2 remains an input-acquisition step only. It does not accept a gold mask, prove UV round-trip, change region G/Y/R, start MediaPipe runtime, or promote E7.03 Green.
- E7.03 A/B UV atlas fast gate is complete for offline viability only. After changing the script default to 1px back-projection sampling, `lip`, `cheek`, and `eye` all pass the fast gate; this keeps the reference-driven UV path alive and allows preparing a small P8 runtime sweep candidate set behind a Build Gate.
- E7.03 P8 runtime sweep implementation and first iPhone runtime pass are complete. It freezes 9 fast-gate-derived reference UV variants (`core`, `balanced`, `soft-wide` per region), adds `e7-reference-uv-atlas` as the primary runtime sweep renderer, keeps `e3e4-baseline`, `e7-arface-uv-candidate`, and `e7-arface-authored-atlas` as compare-only paths, and records real-device runtime compare logs plus representative Compact HUD photos.
- The previous "better than E3/E4 baseline" / "demo-plausible" Green standard is superseded by Q3 overlay-ready validation for `lip`, `cheek`, and `eye`.
- `TECH_VALIDATION_TEST_PLAN.md` was not changed for Phase 0 because no stable validation contract correction was required.
- Keep E3/E4 baseline renderer available as `e3e4-baseline`.
- Keep the previous E7 validation candidate renderer as `e7-arface-uv-candidate`; it remains a rejected/negative procedural baseline unless future evidence says otherwise.
- Add the Phase 2 authored atlas renderer as `e7-arface-authored-atlas`, default-selected for E7.3 atlas validation.
- Keep ARFace mesh/UV as a viable runtime substrate, but reject the current procedural candidate mask output for `lip` and `eye` until the candidate basis is replaced or tuned.
- Continue RN + Unity + ARKit validation only. Do not claim product-readiness, product-quality makeup, M7 Green, E7.4/E7.5/E7.6 readiness, AI/backend/upload, MediaPipe live runtime, SDK, or Android scope from this result.

Confirmed implementation:

- Unity `E3RegionMaskOverlay` now supports baseline-vs-E7 candidate modes plus `e7-arface-authored-atlas` while preserving the E3/E4 centroid baseline path.
- E7 candidate mode uses ARFace mesh availability plus UV-aware candidate evidence fields and face-local mesh regions for `lip`, `cheek`, and `eye`.
- Phase 2 authored atlas mode uses manual face-local vertex label groups (`lip_ring`, `cheekbone_soft_cheek`, `eyelid_band`) and RN-selectable variants for tight/balanced/wide or high/extended atlas sweeps.
- P8 reference UV atlas mode uses frozen probability atlas assets generated from `fast-gate-20260622T160307Z`. Unity samples the selected atlas through ARFace UV centroids using runtime-loaded Resources textures, while RN can sweep `core`, `balanced`, and `soft-wide` variants per `lip`, `cheek`, and `eye` in Compact HUD or Full Debug.
- Runtime comparison/evidence metadata now includes candidate id, variant id, atlas version, label map, label group, topology audit status, vertex/UV/index counts, labeled vertex counts, config hash, fallback flag, and fallback reason.
- P8 reference UV atlas metadata also logs source frame count, gold mask count, silver reference count, UV resolution, threshold, feather UV pixels/normalized value, combine mode, morphology, and calibration score summary in the Unity runtime comparison stream.
- Unity emits E7 comparison/state fields for renderer mode, mask source, tracking state, state action, mesh/UV counts, baseline triangle count, candidate triangle count, and applied triangle count.
- Tracking, Limited, lost, and recovered states now map to explicit actions such as render, short hold, fade, extended hide, and recovered restore.
- RN sends `rendererMode`, `candidateId`, and `variantId`, exposes a Baseline/E7 UV/Atlas segmented control, defaults E7.3 to `Atlas`, and surfaces phase/mask/UV/triangle/state/atlas fields in the E7 status panel and event summaries.
- RN recipe dispatch preserves the existing `ApplyRecipeJson` path and `recipe_applied` event while adding atlas metadata at the recipe/layer/ack/evidence levels.
- RN validation UI now supports `Clean`, `Compact HUD`, and `Full Debug` modes so large logs can be hidden during visual region review.
- RN P2 capture UI now starts in `Clean`, hides Unity face renderers/candidate masks/debug overlays for clean-frame capture, uses unique timestamped `pair_face_*` IDs for repeated attempts, disables the capture button while an export is pending, and places the capture button near the bottom of the screen so tapping does not cover the iPhone front camera.
- `Compact HUD` shows candidate id, selected region, tracking/face count, mesh counts, FPS/frame-time, state action, and recipe latency without covering the full AR view.
- `Full Debug` exposes evidence metadata lines for plan/version, evidence mode, candidate id, region, tracking state, face count, mesh counts, blendshape field status, FPS/frame-time, latency, state action, privacy flags, orientation/device fields where available, and latest Unity event.
- Candidate catalog is visible in Full Debug for planning hygiene. Dispatchable validation candidates are `e3e4-baseline`, current procedural `e7-arface-uv-candidate`, and Phase 2 `e7-arface-authored-atlas`; vertex/blendshape, Apple Vision, MediaPipe, parsing, and hybrid candidates remain pending metadata only.
- RN validation app suppresses the React Native dev LogBox warning overlay so the bottom warning bar does not cover AR evidence or recipe controls. Console/runtime evidence logging remains available through captured device logs.
- Current validation cleanup path fixes the renderer to `smooth-region-mask`, removes candidate/variant UI from the user-facing app, keeps `lip`/`cheek`/`eye` enabled by default, supports independent ON/OFF region toggles, and sends exactly three recipe layers in one RN -> Unity batch.
- Unity rejects legacy/non-batch recipe payloads, unsupported renderer modes, missing/mismatched texture samples, missing/mismatched smooth mask texture ids, and invalid region names instead of silently falling back to an old or wrong region. If parsing fails, Unity clears current recipes and hides overlays to avoid stale masks.
- Compact HUD status now lives inside the bottom `Regions` panel, so it no longer covers the center of the face during validation.

Evidence:

- Phase 1 JS verification: `./node_modules/.bin/tsc --noEmit` passed from `rn/MakeupARValidation`.
- Phase 1 lint verification: `npm run lint` passed from `rn/MakeupARValidation`.
- Phase 1 LogBox cleanup JS verification after screenshot review: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- Phase 1 JS-only pass intentionally did not run `bash scripts/build_m3_unityframework.sh`, `xcodebuild`, `npm run ios`, or a real-device build.
- Phase 2 JS verification after atlas implementation: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- P2 synchronized capture JS verification after capture UI fixes: `./node_modules/.bin/tsc --noEmit` and `npm run lint` passed from `rn/MakeupARValidation`.
- Reference-driven UV atlas pre-gold setup verification: `python3 -m json.tool evidence/e7-reference-atlas/manifest.json` passed; `python3 -m json.tool evidence/e7-reference-atlas/manifest.schema.json` passed; `find evidence/e7-reference-atlas -maxdepth 3 -type f` lists `README.md`, `manifest.json`, `manifest.schema.json`, and `capture_pairs/pair_lip_0001/README.md`; `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check` passed. This verification is no-build/no-device only.
- Phase 2 diff hygiene: `git diff --check` passed after reverting the Unity scene reserialization produced by the build.
- Phase 1 real-device follow-up regenerated UnityFramework with `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase1-ui-2026-06-22-141533.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-phase1-ui-2026-06-22-141533.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data.
- Phase 2 UnityFramework regeneration/sync used `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-phase2-atlas-2026-06-22-151537.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-phase2-atlas-2026-06-22-151537.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data and copied Unity Data.
- P2 UnityFramework regeneration/sync used `bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-2026-06-22-230402.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-2026-06-22-230402.log` records arm64 Mach-O RN/package frameworks, both `105M`, with `9.2M` Unity Data and copied Unity Data.
- Phase 1 RN iOS build/install/launch: `evidence/logs/e7-phase1-ui-rn-ios-run-2026-06-22-141533.log` records the Debug build using device id `00008140-000924DE21BB801C`, successful build, install, bundle id `com.makeupar.rnvalidation`, and successful launch on `위승철의 iPhone`.
- Phase 2 RN iOS build/install: `evidence/logs/e7-phase2-atlas-2026-06-22-151537-rn-ios-device.log` records successful Debug build and install on `위승철의 iPhone`; launch was denied because the device was locked, so this is build/install evidence only.
- P2 RN iOS build/install/launch after unique capture-id and bottom-button fix: `evidence/logs/e7-reference-capture-rn-ios-run-2026-06-22-233049.log` records successful Debug build, install, and launch on `위승철의 iPhone`.
- P2 runtime console: `evidence/logs/e7-reference-capture-runtime-console-2026-06-22-233202.log` records three `reference_capture_requested` / `reference_capture_exported` pairs with exported capture IDs `pair_face_20260622T143300Z_01`, `pair_face_20260622T143305Z_02`, and `pair_face_20260622T143334Z_03`.
- P2 copied capture artifacts: each of `pair_face_20260622T143300Z_01`, `pair_face_20260622T143305Z_02`, and `pair_face_20260622T143334Z_03` contains `frame.png`, `arface_export.json`, `projected_mesh_overlay.png`, and `capture_summary.json` under `evidence/e7-reference-atlas/capture_pairs/`.
- P2 artifact verification: all three captures have `frame.png` and `projected_mesh_overlay.png` at `1179x2556`, `trackingState=Tracking`, mesh counts `1220` vertices / `6912` indices / `1220` UVs, `screenVertices=1220`, `annotationFrameClean=true`, `hudIncludedInFrame=false`, `meshOverlayIncludedInFrame=false`, `candidateOverlayIncludedInFrame=false`, and `blendShapes.available=false`.
- P2 visual review: `pair_face_20260622T143300Z_01/frame.png`, `pair_face_20260622T143305Z_02/frame.png`, and `pair_face_20260622T143334Z_03/frame.png` are clean frames with no white face model, colored candidate mask, Unity debug panel, or RN capture controls visible. `pair_face_20260622T143300Z_01/projected_mesh_overlay.png` and `pair_face_20260622T143334Z_03/projected_mesh_overlay.png` visually align to the same frame; `_03` is the best current gold-mask drawing candidate because the face is frontal and calm.
- Fast gate script verification: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py` passed.
- Fast gate run: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/e7_reference_atlas/fast_uv_atlas_gate.py` completed with overall decision `continue` and output root `evidence/e7-reference-atlas/fast-gate-20260622T160307Z/`.
- Fast gate region metrics: `lip` same-frame IoU `0.9720` / `0.9693`, cross-frame IoU `0.8193` / `0.8325`, UV core/union `0.7046`; `cheek` same-frame IoU `0.9912` / `0.9906`, cross-frame IoU `0.8363` / `0.8302`, UV core/union `0.7106`; `eye` same-frame IoU `0.8604` / `0.8457`, cross-frame IoU `0.6948` / `0.7129`, UV core/union `0.8060`.
- Fast gate visual review: `lip_candidate_preview.png`, `cheek_candidate_preview.png`, and `eye_candidate_preview.png` show plausible face-attached transfer between A/B for offline viability. This supports candidate preparation for runtime sweep, but still needs real-device visual/runtime evidence before any E7.03 Green claim.
- P8 runtime sweep static implementation verification: frozen Resources were generated from `fast-gate-20260622T160307Z` into `unity/MakeupARUnityValidation/Assets/Resources/E7ReferenceAtlas/e7ref-fastgate-20260622T160307Z-v0/`; `runtime_candidates.json` lists `rendererMode="e7-reference-uv-atlas"`, `candidateId="arface-reference-uv-atlas"`, 2 source frames, 2 gold masks, UV resolution `512x512`, and 9 promoted variants. Static checks passed with no Unity/RN device build: `/Users/wiseungcheol/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/e7_reference_atlas/fast_uv_atlas_gate.py`, `./node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check`.
- P8 UnityFramework regeneration/sync after Build Gate approval used `TIMESTAMP=e7-p8-runtime-sweep-2026-06-23-013524 bash scripts/build_m3_unityframework.sh`: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-p8-runtime-sweep-2026-06-23-013524.log` records `** BUILD SUCCEEDED **`, and `evidence/logs/m3-repro-artifact-verification-e7-p8-runtime-sweep-2026-06-23-013524.log` records synced RN/package UnityFramework artifacts and Unity Data.
- P8 RN iOS build/install/launch used `npm run ios -- --device "위승철의 iPhone" --no-packager --extra-params DEVELOPMENT_TEAM=9G4K6N63MK` from `rn/MakeupARValidation`: `evidence/logs/e7-p8-runtime-sweep-rn-ios-run-2026-06-23-013524.log` records successful install/launch on device id `00008140-000924DE21BB801C`.
- P8 runtime console stream: `evidence/logs/e7-03-p8-runtime-console-2026-06-23-013524.log` captures the full `devicectl --console` stream until manual stop. Compact HUD overlay visibility is logged as `validationViewMode=compact`, `visible=true`, and `faceRenderersSuppressed=false`; later Clean segments intentionally log `visible=false` / `suppressed_for_clean_reference_capture`.
- P8 runtime region compare evidence: the console stream records `lip-uvref-v0-balanced`, `cheek-uvref-v0-balanced`, and `eye-uvref-v0-balanced` with `rendererMode=e7-reference-uv-atlas`, `candidateId=arface-reference-uv-atlas`, `trackingState=Tracking`, `stateAction=tracking_render`, mesh/UV counts `1220/6912/1220`, `uvResolution=512`, `threshold=0.45`, `featherUvPixels=2`, `atlasVersion=e7ref-fastgate-20260622T160307Z-v0`, `topologyAuditStatus=pass_uv_topology_ready`, and `usedFallback=false`.
- P8 representative visual evidence: user-provided Compact HUD photos are stored under `evidence/screenshots/e7-03-p8-runtime-sweep-2026-06-23-013524/`. They show `Ref UV` selected, `candidate=arface-reference-uv-atlas`, Compact HUD visible, and face-attached masks for `lip`, `cheek`, and `eye` across `eye core`, `eye balanced`, and `eye soft-wide` variant checks. Visual preference for `eye` is `balanced` as the current default; `core` is safer but too broken/under-covered for the main candidate, and `soft-wide` is too broad.
- E7.03 smooth-mask cleanup acceptance screenshots: `evidence/screenshots/e7-smooth-mask-accepted-2026-06-23/01-frontal-hud.jpg`, `02-left-angle-hud.jpg`, and `03-right-angle-hud.jpg`. These are the only retained media for this acceptance pass per user request. They show Compact HUD moved to the bottom controls, visible subtle eye/cheek/lip color, `active=lip,cheek,eye`, `focus=eye`, `opacity=0.48`, `intensity=0.58`, `applied=true`, tracking face count `1`, mesh counts `1220/6912/1220`, FPS range `52.2-60.2`, and recipe latency `36.0ms`. User evaluation: acceptable for closing this step, but visually still too subtle overall; the eye boundary remains a little weak; future validation should make region coverage easier to judge, and the remaining issue is deferred into cosmetic expression/modeling.
- Phase 1 runtime console: `evidence/logs/e7-phase1-ui-runtime-console-2026-06-22-141533.log` has `93,186` lines captured through `devicectl --console`; the capture was manually stopped after sufficient evidence, so exit code `130` is capture termination, not an observed app crash.
- Runtime evidence includes UnityFramework load success, Unity root view attachment, M6 Unity -> RN events, E1/E2 tracked face lifecycle, E5 snapshots with `rawCameraFrameStored=false` and `offDeviceUpload=false`, and E7 metric samples.
- Runtime counts from the Phase 1 console stream: `[E7] metric_sample` `15`, `e7_metric_sample` RN events `15`, `[M6] unity_to_rn_send` `1189`, `face_feature_snapshot_created` `1193`, `SessionTracking` `3179`.
- Representative Phase 1 metric sample: tracking face count `1`, mesh `1220` vertices / `6912` indices / `1220` UVs, `hasStableUv=true`, average FPS samples observed at `57.1`, `60.0`, and `60.2` after cold start, memory metric available, thermal still manual-device-heat capped Yellow.
- Capture tooling check: `idevicescreenshot`, `ios-deploy`, `cfgutil`, `tidevice`, `pymobiledevice3`, and `idb` were not installed; `devicectl device` and `xcdevice` exposed no screenshot command. Display metadata was captured instead: active portrait LCD, `1179x2556`, point scale `3`.
- User-provided iPhone screenshots were saved under `evidence/screenshots/e7-phase1-ui-2026-06-22-141533/` and verify the Phase 1 modes at `590x1280`: `clean-view.jpg` shows Clean mode with the large debug panels hidden, `compact-hud.jpg` shows Compact HUD with candidate/region controls, and `full-debug.jpg` shows Full Debug evidence metadata, latest Unity event, face state, snapshot, and candidate catalog.
- The screenshot review exposed the React Native dev warning bar (`Open debugger to view warnings`) covering bottom controls. `LogBox.ignoreAllLogs(true)` was added in `rn/MakeupARValidation/App.tsx` to suppress that UI overlay in the validation app. Per user decision, the follow-up reinstall was stopped/skipped because this was a small JS-only cleanup.
- UnityFramework build/sync: `evidence/logs/m3-repro-xcodebuild-unityframework-e7-region-precision-2026-06-22.log` line `2264` records `** BUILD SUCCEEDED **`.
- Artifact verification: `evidence/logs/m3-repro-artifact-verification-e7-region-precision-2026-06-22.log` records arm64 Mach-O RN/package frameworks and copied Unity Data.
- Real-device RN build/install/launch after E7 default renderer change: `evidence/logs/e7-region-precision-rn-ios-run-default-candidate-2026-06-22.log` records successful build, install, and launch on `위승철의 iPhone`.
- Summary: `evidence/logs/e7-region-precision-summary-2026-06-22.md`.
- User-provided recording analysis: `evidence/logs/e7-region-precision-video-analysis-2026-06-22.md` records 28.46s / 59.45 FPS / 1180x2556 metadata and region verdicts.
- Contact sheet: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/contact-sheet-1fps.jpg`.
- Representative visual evidence:
  - `eye`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_001_01.35s.jpg` shows `E7 UV` + `eye` + `phase=region_precision`, but the mask is broad face/lower-face coverage instead of an eye zone.
  - `cheek`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_015_15.35s.jpg` shows cheek selection, but the metric is `phase=baseline`, `mask=centroid_broad`, so this is not a clean E7 candidate sample.
  - `lip`: `evidence/screenshots/e7-region-precision-video-analysis-2026-06-22/frame_020_20.35s.jpg` and `frame_028_28.35s.jpg` show `E7 UV` + `lip` + `phase=region_precision`, but the mask spills across lower-face/cheek/chin-side skin.

Region G/Y/R:

- Legacy procedural `lip`: Red. E7 procedural candidate mode is live, but the selected `lip` candidate repeatedly paints surrounding lower-face skin in normal tracking frames.
- Legacy procedural `cheek`: Yellow / insufficient E7-candidate sample. Cheek selection is present, but the visible metric is baseline or transition-stale, so no region Green/Red is justified yet.
- Legacy procedural `eye`: Red. E7 procedural candidate mode is live, but the selected `eye` candidate is not confined to an eyeshadow/eye region and covers broad non-eye areas.
- P8 reference UV `lip`: Yellow. Runtime log confirms ARFace UV sampling with fallback disabled, and photos show the lip mask stays mouth-local, but the silhouette is jagged/flat and not Q3 overlay-ready.
- P8 reference UV `cheek`: Yellow. Runtime log confirms tracking render with fallback disabled, and photos show cheek-local masks, but the region is still blocky/low on the face and needs yaw/pitch/near-far scenario review.
- P8 reference UV `eye`: Yellow. Runtime log confirms tracking render with fallback disabled, and photos show eye-local masks across core/balanced/soft-wide checks. `eye balanced` is the best current candidate among the three because it preserves eyelid coverage better than `core`; `soft-wide` is too broad. The remaining blocker is jagged/overbroad silhouette quality, not runtime attachment.

Known limitations:

- The earlier E7.3 screen-recording review still lacks a full runtime console stream; the fresh Phase 1 follow-up has a runtime console stream, but no fresh visual screenshot/recording.
- No `[E7] region_precision_compare` runtime stream was captured during this pass.
- P8 runtime evidence is a first representative pass, not a complete required-scenario sweep. Neutral/front-facing photos are present, but smile/open-close/pucker/yaw for `lip`, near/far/yaw/pitch for `cheek`, and blink/repeated blink/squint/head pitch for `eye` still need structured review before any region can become Green.
- P8 Compact HUD photos still show an opaque white full-face debug surface behind the colored regions. That is useful for validation visibility, but it blocks a Q3 overlay-ready makeup claim until the material/debug-surface behavior is separated from region mask evidence.
- The accepted smooth-mask cleanup screenshots are user-accepted for the current app optimization goal, not a formal E7.03 Green decision. They do not cover the full required motion matrix, diverse lighting/users, E7.4 cosmetic material realism, or the user's noted visibility/eye-boundary weakness.
- Phase 2 atlas runtime launch limitation is superseded for P2 capture only: the app now launches and captures synchronized reference pairs, but this still is not runtime atlas visual scoring and does not change Phase 2 region G/Y/R.
- The reference-driven UV atlas path has captured synchronized P2 inputs, but `goldMaskWork.status` is still `not_started`; there is no official user-authored gold mask, UV back-projection, same-frame round-trip render, P3 acceptance, or P4 validation yet.
- The A/B fast gate used user-authored mask PNGs already present in the capture folders, but it does not normalize `manifest.json` into official multi-frame gold-mask status and does not replace the full P3/P4/P5/P6 pipeline.
- The fast gate excludes `triangleVisibility`, occlusion/depth conflict handling, and blendshape correction because the current export has no triangle visibility records and `blendShapes.available=false`.
- The canonical fast gate summary is `fast-gate-20260622T160307Z`. Earlier debug/sparse-sampling/smoke outputs were removed or superseded and should not be used for decisions.
- P2 export records `blendShapes.available=false` because blendshapes are not exposed in the current capture exporter.
- P2 export records `coordinateSpaceValidated=false` / `pending_projected_mesh_overlay_review`; visual overlay review is positive for `_01` and `_03`, but no automated pixel-level coordinate acceptance threshold has been implemented.
- Cheek still needs a clean `phase=region_precision` sample before final region scoring.
- Lost, Limited, and recovered behavior were not exercised in this recording.
- On-screen FPS/frame-time values are visible, but this is not E7.6 performance evidence.
- The candidate is a validation candidate, not product-quality region segmentation or cosmetic rendering.
- Phase 2 manual atlas labels are authored validation heuristics over ARFace mesh/UV data. They are not MediaPipe, Apple Vision, face parsing, product segmentation, or a product-ready makeup asset source.
- Timestamped P2 capture IDs intentionally differ from the original fixed `pair_face_0001` request so repeated capture attempts do not overwrite each other. This is an evidence-management adjustment, not a renderer behavior change.
- E7.3 evidence does not resolve the accepted M7 Yellow lifecycle risk.
- Phase 1 UI modes are visually screenshot-verified from user-provided iPhone captures, and Phase 2 installed the LogBox-suppressed RN build on-device, but post-LogBox visual confirmation is still pending because launch was blocked by device lock. The local toolchain still has no automated iPhone screenshot CLI, and installed `devicectl`/`xcdevice` exposes no screenshot command.
- Non-atlas future candidates shown in the candidate catalog are not runtime implementations; they are pending labels for later E7.03 phases.
- `Full Debug` can still cover part of the AR view by design; use `Clean` or `Compact HUD` for visual evidence capture.
- Phase 1 does not change region G/Y/R: `lip` remains Red, `cheek` remains Yellow / insufficient E7 candidate sample, and `eye` remains Red.
- Phase 1 runtime evidence is dominated by the default `lip` candidate path; it does not provide clean `cheek` or `eye` visual region evidence.
- Phase 2 does not claim M7 Green, E7.4/E7.5/E7.6 readiness, AI/backend/upload readiness, SDK readiness, Android readiness, product readiness, or M7 lifecycle closure.

Next boundary:

- The current smooth-mask validation app cleanup/optimization goal is closed by user acceptance, with screenshots retained as the only media artifact for that pass.
- If continuing E7.3 strictly, use the accepted smooth-mask cleanup as the current usable validation app state and run structured region scenarios before any Green decision.
- If entering the next makeup-modeling scope, first create or update `E7_COSMETIC_RENDERING_CORE_SUBSPIKE_PLAN.md` and explicitly record that E7.4 starts on top of an accepted-for-validation but still E7.3-Yellow boundary state.
- The next real proof must be a follow-up runtime comparison with full metadata and representative visual evidence for the missing scenarios: smile/open-close/pucker/yaw for `lip`, near/far/yaw/pitch for `cheek`, and blink/repeated blink/squint/head pitch for `eye`.
- Keep the current procedural ARFace mesh/UV candidate and current manual heuristic baseline as compare-only paths unless future evidence promotes a new reference-driven atlas candidate.
- Do not enter E7.4/E7.5/E7.6 until `lip`, `cheek`, and `eye` have Q3 overlay-ready evidence, or until the team explicitly accepts E7.3 Yellow risk and records that later E7.4/E7.5 evidence is conditional and full E7 visual product-readiness remains capped at Yellow.

## E7.2 Baseline Instrumentation

Status: Green for E7.2 baseline instrumentation.

Confirmed implementation:

- Unity emits `[E7] metric_sample` with FPS, frame-time, worst frame-time, sustained-sub-20 flag, tracking state, mesh counts, active region/texture baseline state, memory availability/values, and thermal/manual-heat fields.
- Unity emits `[E7] metric_unavailable` for unavailable thermal native API evidence and keeps `thermalMetricYellowCap=true`.
- Unity recipe apply logging records RN `sentAtMs`, Unity `appliedAtMs`, and Unity `appliedFrame` while preserving the E3/E4 `ApplyRegionRecipe` path.
- RN sends a log-only ack after receiving `recipe_applied`, so Unity console evidence records RN `receivedAtMs`, `sendToAckLatencyMs`, `unityApplyLatencyMs`, and `unityToRnReceiveLatencyMs`.
- RN UI includes an E7 status panel with latest baseline metric and recipe latency fields.

Runtime summary:

- Runtime log line count: `65,428`.
- `[E7] metric_sample`: `9`.
- `[E7] metric_unavailable`: `1`.
- `[E7] recipe_latency source=unity_applied`: `14`.
- `[E7] recipe_latency source=rn_ack`: `14`.
- Tracking-state metric samples: `7`; average FPS mean/min/max `57.6 / 51.4 / 60.1`; average frame-time mean `17.4 ms`; allocated memory range `64.3-64.9 MB`; reserved memory max `82.2 MB`.
- RN ack latency samples: `14`; RN sent -> RN received mean/min/max `137.6 / 33.0 / 1473.0 ms`; Unity applied -> RN received mean/min/max `28.4 / 7.0 / 208.0 ms`.

Known limitations:

- First metric and latency samples include cold-start overhead and must not be used as final E7.6 performance evidence.
- Native thermal API integration is not configured; thermal is manual-device-heat evidence with a Yellow cap.
- E7.2 relies on a user-provided representative frame instead of a screen recording. This waiver is scoped only to E7.2.

## Workspace Document State

Active root documents:

- `AGENTS.md`: repository working rules, milestone scope boundaries, document policy, and evidence/cleanup requirements.
- `TECH_VALIDATION_TEST_PLAN.md`: stable validation contract. Do not update for progress/status unless correcting the contract.
- `TECH_VALIDATION_RESULT.md`: current snapshot, latest milestone decisions, and next boundary.

History archive:

- Full historical details, requirement matrices, special observations, environment/device snapshots, and the full evidence index were moved to `docs/roadmaps/archive/TECH_VALIDATION_HISTORY_2026-06-22.md`.
- Use the archive only when auditing old milestone evidence or reconstructing past decisions.

Document policy:

- New `M*_..._PLAN.md` or `E*_..._PLAN.md` files are temporary session plans.
- After a session completes, absorb the result into this file and delete temporary plans unless the user asks to keep them.
- Put reusable procedures in `docs/runbooks/`.
- Keep roadmap/research docs under `docs/roadmaps/`.

## Cleanup And Token Management

Local cleanup policy:

- Use `bash scripts/cleanup_local_generated.sh --profile balanced --dry-run` before applying cleanup.
- The balanced profile removes large reproducible generated artifacts while preserving `node_modules`, iOS `Pods`, Unity `Library`, logs, screenshots, and existing evidence directories. Use `--profile share` before handing the directory to teammates; it removes ignored evidence, dependency installs, editor caches, and build products.
- To prevent workspace growth, keep `evidence/`, `unity-builds/`, RN `node_modules`, iOS `Pods`, Xcode DerivedData/build folders, Unity `Library`/`Logs`/`UserSettings`, raw recordings, and full build logs out of source control and remove them with the cleanup profiles when they are no longer active evidence/debug state.
- `scripts/build_m3_unityframework.sh` now defaults to temporary DerivedData plus temporary Unity/Xcode full logs and keeps only the small artifact verification summary. Use `BUILD_LOG_MODE=full` or `KEEP_DERIVED_DATA=1` only for a specific evidence/debug pass.
- After cleanup, the next real-device Unity/RN session must regenerate UnityFramework with `bash scripts/build_m3_unityframework.sh`.
- Detailed cleanup procedure lives in `docs/runbooks/LOCAL_WORKSPACE_CLEANUP_RUNBOOK.md`.
- Team onboarding, share execution requirements, directory structure, and local-only values live in `docs/runbooks/TEAM_SHARE_REQUIREMENTS_KO.md`.

Token policy:

- Future sessions should start from `AGENTS.md` plus `TECH_VALIDATION_RESULT.md` > `Current Session Snapshot`.
- Do not load all research reports by default.
- Load only the research axis that matches the current E7 milestone.
- Do not load `docs/roadmaps/active/`, `docs/roadmaps/research/`, or `docs/roadmaps/archive/` as whole folders. Use `docs/roadmaps/README.md` as the menu and open only the selected file.
- For `_KO.md` / `_KR.md` companion pairs, agents should read the `_KO.md` primary implementation plan and skip `_KR.md` unless user-facing wording is specifically needed.
- For broad search, exclude lockfiles and generated project/config files unless the task is dependency or native project troubleshooting: `package-lock.json`, `Podfile.lock`, Xcode `project.pbxproj`, and Unity `ProjectSettings/*.asset`.
- For large code hotspots, use symbol search plus targeted ranges instead of opening whole files by default: `rn/MakeupARValidation/App.tsx`, `unity/MakeupARUnityValidation/Assets/Scripts/RNBridge.cs`, `unity/MakeupARUnityValidation/Assets/Scripts/FaceTrackingStatusReporter.cs`, and `unity/MakeupARUnityValidation/Assets/Scripts/E3RegionMaskOverlay.cs`.

Result growth policy:

- Keep this file as a compact routing and decision record, not a full log dump.
- Record only the latest decision, evidence paths, known limitations, and next boundary. Do not paste full build logs, full console streams, raw JSON dumps, frame lists, or long analysis transcripts into this file.
- When a milestone accumulates old details, move historical evidence lists and superseded narrative into `docs/roadmaps/archive/TECH_VALIDATION_HISTORY_YYYY-MM-DD.md`, then keep only the current high-signal summary here.
- If `Current Session Snapshot` becomes hard to scan, compress older bullets into one status bullet plus archive references before adding new work.
- Prefer metadata, contact sheets, representative frames, and short summary logs over raw recordings or full logs. Keep raw/heavy artifacts local under ignored `evidence/` only while they are active decision evidence.

## Next Milestone Boundary

E1 AR Alignment, E2 Trackable Lifecycle Diagnostics, E3 Region Mask, E4 Texture Sample, E5 AI Feature Readiness Snapshot, E6 Engine Decision, E7.0/E7.1 preflight, E7.2 Baseline Instrumentation, and the accepted E7 lip implementation slice are complete/frozen for their recorded evidence scope. The current default next work is E7 cheek/blush validation only. The 2026-06-28 cheek blush `milk_pink` / expanded three-stage gradient correction has follow-up no-build natural edge/color tuning for perimeter seams, light-color visibility, and side-face clipping, but this latest source change has not yet been rebuilt into a fresh UnityFramework/RN package. Cheek remains Yellow until a fresh build/install/run collects runtime evidence. M0-M6 remain Green. M7 remains Yellow / skipped by decision / risk accepted.

Next boundary decision:

- Primary path: make `202268054(여서진)` / `00008110-0001794E0CD9801E` available to Xcode/RN, then rerun RN from `rn/MakeupARValidation` with `npm run ios -- --udid 00008110-0001794E0CD9801E --no-packager --extra-params DEVELOPMENT_TEAM=9G4K6N63MK`. If source or Unity output changes before retry, regenerate/sync `UnityFramework.framework` again with `bash scripts/build_m3_unityframework.sh`.
- Validation contract: cheek/blush only; prove natural face attachment, center-strong/edge-feathered density, style-specific density maps, ARFace/mesh no-delay motion, and scaling across face proportions. Keep `matte_lip` and accepted `gradient_lip` frozen unless explicitly reopened.
- Evidence required before cheek Green: runtime logs, Compact HUD screenshots or motion frames, FPS/frame-time, latency, thermal, memory, region G/Y/R decision, and representative demo-look evidence for the five cheek shapes.
- Compare-only paths: use the buildless mask contact sheet, expected-render sheet, verifier output, RN tests, TypeScript/lint, scoped diff checks, Unity compile checks, UnityFramework build/sync proof, and unsigned RN package proof only as compile/package evidence; they do not replace iPhone runtime acceptance.
- Out of scope: product-quality makeup rendering claims, AI inference/recommendation/backend/upload, Android, commercial SDKs, raw camera frame storage, and E7.4/E7.5/E7.6 renderer/demo readiness unless the user explicitly opens that boundary.
